# DECISIONS — 关键决策日志

> 每个关键选择记下「为什么这么定 / 考虑过什么 / 何时复盘」。换任何大模型对接都能秒懂上下文。
> 最后更新：2026-07-20

---

## 决策索引

| # | 决策 | 日期 | 状态 |
|---|---|---|---|
| D01 | 任务形态：推荐/打分排序（非二分类） | 2026-07-02 | ✅ 已定 |
| D02 | 主模型：树模型+描述符（非 GNN） | 2026-07-02 | ✅ 已定 |
| D03 | 反应条件：首次作为特征纳入 | 2026-07-02 | ✅ 已定 |
| D04 | 标签体系：继承旧项目四分类连续（1.0/0.8/0.7/0.0） | 2026-07-02 | ✅ 已定 |
| D05 | 规则向量：作为特征（非硬过滤），继承旧项目34维 | 2026-07-02 | ✅ 已定 |
| D06 | 文档体系：五份活文档替代静态CLAUDE.md | 2026-07-02 | ✅ 已定 |
| D07 | 旧项目数据/代码：只读引用，不修改 | 2026-07-02 | ✅ 已定 |
| D08 | GNN：降级为对比基线（PR-AUC 0.784 待超越） | 2026-07-02 | ✅ 已定 |
| D11 | 记忆系统建设优先于模型优化 | 2026-07-15 | ✅ 已定 |
| D12 | 日报双轨制（AI 日报 + 人日报） | 2026-07-15 | ✅ 已定 |
| D13 | 反应条件补全优先于 3D 描述符接入 | 2026-07-15 | ✅ 已定 |
| D14 | 精简化学规则至 9 条核心硬性约束 | 2026-07-15 | ✅ 已定 |
| D15 | 移除 `hard_rule_sampled` 负样本 | 2026-07-15 | ✅ 已定 |
| D16 | 主模型不启用反应条件特征 | 2026-07-15 | ✅ 已定 |
| D17 | 阶段 2 接入 3D/二聚体描述符 | 2026-07-15 | ✅ 已定 |
| D18 | 启用 joblib 文件级构象缓存 | 2026-07-16 | ✅ 已定 |
| D19 | tree_v3 采用单体 3D，不启用二聚体 3D | 2026-07-16 | ✅ 已定 |
| D20 | App/predictor 默认模型切换为 tree_v3，特征开关从 pkl 自动恢复 | 2026-07-17 | ✅ 已定 |
| D21 | 双留出评估协议 + 统计先验入模（tree_v4）；默认模型保持 tree_v3 | 2026-07-20 | ✅ 已定 |
| D22 | 双模型路由上线（tree_v4 池内 + tree_v4_noTE 外推）；指纹/校准路线关闭 | 2026-07-20 | ✅ 已定 |
| D23 | 路由规则切换为 routed_strict；双留出逐折报告标配化 | 2026-07-20 | ✅ 已定 |
| D24 | GNN embedding 迁移阶段 A 通过 → 立项阶段 B（逐折重训 GNN）；mono_emb 证伪 | 2026-07-21 | ✅ 已定 |
| D25 | GNN embedding 迁移阶段 B 闭卷证伪，路线关闭；阶段 A 增益判为配对标签记忆 | 2026-07-21 | ✅ 已定 |
| D26 | 真实回测（14 组已做实验）：新模型显著比旧 GNN 诚实；H1–H4 结构待用户确认；B3 按名称采信 | 2026-07-21 | ✅ 已定 |

---

## D01 — 任务形态：推荐/打分排序

**决定**：项目核心任务从"二分类（能否成膜）"改为"推荐/打分排序"。

**为什么**：
- 用户问题 1：样本严重失衡（正样本远多于负样本），本质是发表偏差导致负样本 MNAR（非随机缺失）。
- 强行二分类 → 必须造负样本 → 负样本不可信 → 标签噪声放大。
- 推荐任务用"打分+排序+多样性约束"替代二分类，天然规避负样本缺失困境，更贴合"实验指导工具"的最终交付。

**考虑过的替代方案**：
- 条件分类（预测"某组合+给定条件下成膜概率"）：保留作为模型内部机制，但对外形态仍是打分排序。
- 纯二分类：被否决，负样本问题是死结。

**何时复盘**：阶段 4 完成首次建模后，评估排序指标（NDCG/Hit@K）是否能有效区分好坏组合。

---

## D02 — 主模型：树模型+描述符（GNN 并行）

**决定**：以 **XGBoost/LightGBM + 化学描述符**为**主可解释模型**；**GNN 作为并行准确率模型**，两者不是替代关系。项目成功标准为：
- (a) 树模型在留一单体 CV 上超越 GNN 基线（PR-AUC 0.784）；或
- (b) 树模型接近 GNN 但能给出不可替代的化学解释（SHAP 揭示规则）。

**为什么**：
- 用户问题 2、8：旧项目走 GNN 路线，泛化仍差，RF 又"像相似度排序"。
- 核心判断：**泛化差和相似度排序不是模型架构问题，是描述符尺度问题 + 样本量问题**。
  - GNN 在 ~6000 样本上学到的仍是表面相似性 → 体现为筛选阶段泛化差。
  - RF "像相似度排序"是因为描述符都是绝对量（分子量/原子数），小单体特征空间天然更近 → 退化成最近邻。
- **但旧项目 GNN 已达 PR-AUC 0.784，这是强基线**。在没证明树模型能超越之前，不应把 GNN 降级为"陪跑"，而应保留为并行模型。
- 树模型在条件特征利用和可解释性上有天然优势，GNN 在端到端学习已验证的结构模式上有优势。两者可以互补：树模型用于解释+条件推理，GNN 用于最终打分或验证。

**考虑过的替代方案**：
- 树模型为主，GNN 仅作对比基线：被否决，风险过高（若树模型未达 0.784，项目无退路）。
- GNN 为主，树模型辅助：被否决，无法解决条件特征未利用和可解释性需求。

**何时复盘**：阶段 4 首次建模，对比树模型 vs GNN 在留一单体交叉验证（leave-one-monomer-out）下的泛化能力和解释性。

---

## D03 — 反应条件：首次作为特征纳入

**决定**：把反应条件（温度/溶剂/催化剂/合成路线/界面类型）作为特征纳入模型。

**为什么**：
- 用户问题 3：粉末≠不能成膜，成膜与否对反应条件极敏感。丢掉条件等于丢掉一半信息。
- **关键审阅发现**：旧项目 `v5_train_stage1.csv` 已有完整反应条件字段（16 列），但 **GNN 从未使用过它们**（只用结构+规则向量）。旧项目规划过 Stage 2 条件预测（FiLM 调制）但未实现。
- 因此"重新纳入条件"实际上是**首次真正使用条件特征**——这是新项目相对旧项目最大的增量突破点。

**考虑过的替代方案**：
- 不纳入条件（纯结构）：被否决，用户明确选择纳入，且化学上必要。
- 纳入但作为辅助任务（旧项目 ConditionHead 方案）：被否决，多任务在小数据上梯度冲突（v3 崩溃教训）。

**实现要点**：
- 条件字段编码：温度（数值归一化）、溶剂（分类 one-hot 或嵌入）、催化剂（分类）、合成路线（分类）、界面类型（分类）。
- 排序时条件作为输入的一部分：输入"组合+条件"，输出成膜概率。

**何时复盘**：阶段 3 特征工程完成后，消融实验对比"有/无条件特征"对模型性能的影响。

---

## D04 — 标签体系：继承四分类连续

**决定**：继承旧项目四分类连续标签体系。

**标签映射**：
| 标签值 | 含义 | 旧项目样本数 |
|---|---|---|
| 1.0 | 文献确认连续成膜 | 429 |
| 0.8 | 粉末合成成功但非连续膜（弱正） | 291 |
| 0.7 | 增广正样本（衍生自文献正，quality_weight=0.3） | 1,308 |
| 0.0 | 负样本（化学规则生成+文献内负样本） | 4,173 |

**为什么**：
- 用户问题 3：粉末不应直接当负样本。旧项目已用 0.8 弱正处理，比二元更合理。
- 连续标签让模型学到"程度"而非"是否"，与推荐任务（打分排序）天然契合。

**何时复盘**：阶段 1 数据审计时，重新评估 0.8 粉末样本的质量和数量，可能需要细分（成膜条件不当 vs 结构性失败）。

---

## D05 — 规则向量：作为特征（非硬过滤）

**决定**：继承旧项目 34 维规则向量作为树模型特征，而非推理后硬过滤。

**为什么**：
- 用户问题 5："把规则嵌进描述符对不对"。答案：**取决于规则来源**。
  - ✅ 领域规则（化学/文献依据，如"Schiff base 需胺醛官能团匹配"）当特征 = 注入可靠归纳偏置。
  - ❌ 拟合规则（从训练集拟合的）当特征 = 数据泄露/过拟合。
- 旧项目 34 维规则中：23 维是化学领域规则（苯环必需/官能团对称等），11 维是 F/CF3 描述符，**都是领域规则，当特征合理**。
- 旧项目已验证：规则注入 FilmHead（非硬过滤）让模型自行学习规则权重，比硬过滤效果好。

**何时复盘**：阶段 4，对比"有/无规则特征"的模型表现，以及 SHAP 能否独立学到这些规则模式（如果 SHAP 自动学到，说明规则可去掉；学不到，说明规则特征必要）。

---

## D06 — 文档体系：五份活文档

**决定**：用五份活文档替代静态 CLAUDE.md。

**为什么**：用户问题 6、7——上下文随对话加长而丢失，CLAUDE.md 不随项目演化更新、难对接其他大模型。

**文档职责**：
| 文档 | 职责 | 更新时机 |
|---|---|---|
| PROJECT_STATE.md | 当前状态/阶段/下一步/阻塞点 | 每会话首读尾写 |
| DECISIONS.md（本文件） | 关键决策记录 | 每次决策时 |
| DATA_DICT.md | 数据字典 | 每次数据变更时 |
| DAILY_LOG/日期.md | 每日工作记录 | 每会话/每日 |
| EXPERIMENTS/exp_XXX.md | 实验记录 | 每次建模实验 |

**工作流**：每次会话开头读 PROJECT_STATE+最新日报 → 结尾更新 PROJECT_STATE+写日报 → 决策记入 DECISIONS。

---

## D07 — 旧项目：只读引用，不修改

**决定**：旧项目 `C:\Users\ckx\Desktop\tianxuan seek\` 作为只读数据/代码源，新项目通过引用或复制复用，绝不修改旧项目。

**为什么**：旧项目是用户前期大量工作的积累，保留原样作为参考和回退基线，避免破坏性操作。

**复用方式**：
- 数据：复制 `v5_train_stage1.csv` 到新项目 `data/raw/`（或软链接，待用户确认）。
- 代码：描述符代码通过 `sys.path` 引用旧项目 `src/chemistry/`，或复制需要的函数到新项目 `src/features/`。

---

## D08 — GNN：作为并行准确率模型（非仅对比基线）

**决定**：旧项目 GNN v5（PR-AUC 0.784）作为新项目的**并行准确率模型**，与树模型共同发展，不是等待被超越的基线。

**为什么**：
- 旧项目 GNN 不是"完全没做好"，PR-AUC 0.784 是强基线。
- 新项目同时追求：树模型利用条件特征 + 提供可解释性；GNN 保持准确率上限。
- 用户感受到的"泛化差/相似度排序"主要在**筛选阶段**（笛卡尔积+多样性过滤），需区分训练性能 vs 筛选性能。
- 最终交付可以是两者结合：树模型做初筛/解释，GNN 做精排/验证，或ensemble。

**成功标准**（待阶段 4 细化）：
- 树模型在留一单体 CV 上 PR-AUC 接近或超越 GNN；或
- 两者 ensemble 后优于任一单一模型；或
- 树模型提供 GNN 无法给出的化学可解释洞察。

---

## D09 — 阶段节奏：分阶段严谨推进

**决定**：先完整数据审计（阶段 1），再做特征工程和建模，不跳过数据质量检查直接跑基线。

**为什么**：用户选择分阶段严谨推进，避免"快速跑基线"可能掩盖的数据问题（如标签噪声、SMILES 缺失、跨实验假配对）。

**阶段 1 目标**：
- 导入旧项目 `v5_train_stage1.csv` 到 `data/raw/`
- 输出数据审计报告（覆盖率、缺失率、标签冲突、重复、异常值）
- 量化：SMILES 缺失率、反应条件缺失率、标签噪声率
- 为后续阶段提供干净的数据基线

**何时复盘**：阶段 1 审计报告完成后，根据数据质量决定是否需要先补数据清洗阶段。

---

## D10 — 交付形态：Jupyter notebook 优先

**决定**：第一阶段实验指导工具以 Jupyter notebook 形式交付，而非 CLI 或 Web 界面。

**为什么**：
- Notebook 最适合探索性分析和可视化（SHAP 图、分子结构图、相似案例）
- 开发速度快，便于调试
- 可后续封装为 CLI 脚本或 Web 界面

**交付标准**：
- 输入：候选组合 DataFrame（含醛/胺 SMILES + 条件）
- 输出：打分排序 DataFrame + SHAP 解释 + 相似案例
- 可视化：分子结构、特征重要性、Top-K 推荐卡片

**何时复盘**：阶段 4 建模完成后，根据使用习惯决定是否封装为 CLI/Web。

---

## D11 — 记忆系统建设优先于模型优化

**决定**：优先完成 `.agents/` 记忆系统、AGENTS.md、session_index/state、状态检查脚本和日报双轨制，再进入模型优化。

**为什么**：
- 用户明确要求记忆系统优先。
- 上下文管理是后续所有模型优化工作的基础，可避免重复解释项目背景和决策。

**考虑过的替代方案**：
- 边做模型边补记忆系统：被否决，上下文丢失风险高。

**何时复盘**：记忆系统运行稳定后（约一周后）。

---

## D12 — 日报双轨制（AI 日报 + 人日报）

**决定**：每天工作结束后生成两份日报：一份给 AI 看（技术细节、代码变更、指标），一份给人看（工作摘要、关键成果、下一步建议）。

**为什么**：
- AI 需要技术细节快速进入状态；人需要高层次的摘要和决策建议。
- 双轨制减少上下文丢失，方便不同 AI 实例和用户本人对接。

**何时复盘**：一周后评估是否有效减少上下文丢失。

---

## D13 — 反应条件补全优先于 3D 描述符接入

**决定**：在接入 3D 描述符之前，先用 YAML→CSV 补全反应条件字段。

**为什么**：
- `v5_train_stage1.csv` 条件缺失率高达 86-90%，但并非原始数据缺失，而是映射丢失。
- 补全后条件覆盖率从 13% 提升到 64%，可直接作为特征；3D 描述符计算成本高且收益不确定。

**何时复盘**：重新训练树模型后对比 PR-AUC 变化。

---

## D14 — 精简化学规则至 9 条核心硬性约束

**决定**：化学规则只保留用户确认的 9 条核心硬性约束（官能团有效性 + 对称/对位要求）+ 11 维 F/CF3 描述符，其余 25 条启发式规则移除。

**为什么**：
- 旧 34 维规则包含大量启发式约束，过滤过强且易与标签泄漏耦合。
- 核心规则是真正的硬性化学约束，作为主特征更稳健；其余规则可作为后续消融对照。

**何时复盘**：3D 描述符接入后，看规则贡献是否进一步下降。

---

## D15 — 移除 `hard_rule_sampled` 负样本

**决定**：训练模型 v2 时移除 `source_db == "hard_rule_sampled"` 的负样本。

**为什么**：
- 该批负样本由旧规则硬过滤生成，与标签强相关，保留会导致模型学到规则模式而非化学规律。
- 消融实验 B/C 在移除后 PR-AUC 稳定在 0.73-0.75，验证该策略有效。

**何时复盘**：新数据源加入时重新评估负样本策略。

---

## D16 — 主模型不启用反应条件特征

**决定**：当前主模型 `tree_v2` 不启用反应条件特征。

**为什么**：
- 消融实验 D（精简规则 + 交互 + 条件特征）PR-AUC 高达 0.9112，异常高，疑似推断条件与标签强相关或存在泄漏。
- 用户明确反应条件非刚需，模型应关注单体本身性质。

**何时复盘**：诊断条件泄漏原因后，若可排除泄漏再评估是否纳入。

---

## D17 — 阶段 2 接入 3D/二聚体描述符

**决定**：按用户确认的“分两步走”方案，阶段 1 完成规则精简与交互特征后，阶段 2 接入单体 3D 描述符和二聚体 3D 描述符，与当前 v2 做对比和消融。

**为什么**：
- 当前 v2 已验证规则精简 + Hadamard 交互的收益（PR-AUC 0.727→0.7484）。
- 3D/二聚体描述符是下一波可解释增量，可能捕捉醛-胺配对的几何相容性。

**何时复盘**：完成对比实验后决定是否进主模型。

---

## D18 — 启用 joblib 文件级构象缓存

**决定**：为 3D/二聚体描述符计算启用文件级缓存，默认路径 `data/interim/3d_cache/`。

**为什么**：
- 全量 6201 对计算 3D+二聚体约需 42 分钟；同一单体在多个配对中复用，单体缓存可显著减少重复计算。
- 二次读取缓存后，10 行特征化从数秒降到 0.02 秒，全量特征化可从分钟级降到秒级。
- joblib dict 格式轻量，写入时先写 `.tmp` 再重命名，可防止中断损坏。

**缓存策略**：
- 单体 3D：key = SMILES 字符串
- 二聚体 3D：key = (ald_smiles, amine_smiles) 元组
- 缓存文件不进入 git（路径在 `data/interim/` 下，已被 gitignore 覆盖）

**何时复盘**：500 对验证后，若缓存文件过大或读写成为瓶颈，可改为按分子哈希分片的 `.npy`/`.parquet`。

---

## D19 — tree_v3 采用单体 3D 描述符，不启用二聚体 3D

**决定**：将 `tree_v3_monomer3d_full` 选为新主模型 `tree_v3`，即启用单体 3D 描述符，但不启用二聚体 3D 描述符。

**为什么**：
- 全量验证显示 `monomer3d_full` PR-AUC 0.7785、MAE 0.2344，均优于 `dimer3d_full`（PR-AUC 0.7716、MAE 0.2395）。
- `dimer3d_full` 的 NDCG@10 更高（0.9216 vs 0.9055），但 PR-AUC 和 MAE 略差，说明二聚体几何信息部分已被单体 3D + Hadamard 交互覆盖，存在冗余。
- 项目核心目标之一是输出准确成膜概率，PR-AUC 和 MAE 是更直接的指标。

**何时复盘**：
- 若后续发现排序/Top-K 场景更重要，可重新评估二聚体 3D 或仅使用二聚体 3D 做排序模型。
- 可进一步做仅二聚体 3D（无单体 3D）的消融，确认其独立贡献。

---

## D20 — App/predictor 默认模型切换为 tree_v3，特征开关从 pkl 自动恢复

**决定**：
1. `FilmPredictor` 默认树模型从 `tree_baseline.pkl` 切换为 `models/tree_v3.pkl`。
2. `TreeFilmPredictor.load()` 从 pkl 内的 `metrics` 恢复特征开关（`use_rules` / `reduced_rules` / `use_interaction` / `use_3d` / `use_dimer` / `n_confs`），预测时按训练配置生成特征；旧 pkl 无 `metrics` 则回退默认特征。
3. 预测特征矩阵用 `reindex(columns=feature_cols)` 对齐，3D 计算失败的样本补 0 而不是报错。

**为什么**：
- tree_v3 有 142 维特征（含单体 3D），若预测时仍按默认开关生成特征，会因缺列直接报 KeyError；开关存进 pkl 后模型自描述，切换模型不需要改代码。
- App 用户可输入任意 SMILES，3D 构象生成可能失败，补 0 比中断更稳健。
- `TreeFilmPredictor` 的默认路径保持 `tree_baseline.pkl` 不变：`tests/test_app_pipeline.py` 的快速训练测试会保存到默认路径，避免测试覆盖主模型。

**何时复盘**：若后续切换主模型（如 tree_v4），只需改 `src/predictor/__init__.py` 中的 `DEFAULT_TREE_MODEL`。

---

## D21 — 双留出评估协议 + 统计先验入模（tree_v4）；默认模型保持 tree_v3

**决定**：
1. **评估协议升级**：LOGO（GroupKFold-10 按醛）与"醛胺双留出"（自定义网格分组 5 折，验证集的醛和胺均不出现在训练折）同时报告；双留出须报多分组种子（单种子噪声 ±0.03）。
2. **统计先验入模**：`te_ald_film_rate` / `te_amine_film_rate` 两个 target encoding 特征（`src/features/target_encoding.py`）。防泄漏铁律：CV 每折只用训练折拟合映射表；最终模型全量拟合并把 `te_rates` 存入 pkl。
3. **频率降权**：`w = 1/sqrt(醛频次 × 胺频次)` 均值归一化。频次不含标签，全局计算无泄漏。选 1/sqrt 而非 1/count：温和降权，避免 69% 仅出现 1 次的单体获得悬殊权重后有效样本量坍缩。
4. **tree_v4 = 144 维（142 + 2 TE）+ 降权 + mild 正则**，已保存 `models/tree_v4.pkl`（自描述格式，predictor 已兼容 `te_rates`，旧 pkl 向后兼容）。
5. **App 默认模型不切换，保持 tree_v3**：切换门槛"双留出 PR-AUC 明确优于 v3"未达成——3 种子均值 v4 0.642 vs v3 0.653（seed=42 单点 +0.033 不可复现）。

**为什么**：
- exp_004 证明胺历史成膜率（0.864）强于 142 维模型（0.772），且仅留醛的评估默许胺统计泄漏。双留出下 F1 基线坍缩至正例率（0.52），证实泄漏判断。
- TE 入模让 LOGO PR-AUC 0.761 → **0.878**（首次超过频率基线），in-sample 间隙 0.215 → **0.121**——对"已知单体池内推荐"的 App 主场景是实质进步。
- 但双留出下 TE 退化为常数列，且训练时模型重度依赖 TE（重要性合计 36.3%），挤占化学特征容量，外推不升反略降（3 种子均值 -0.011，MAE 一致偏差）。
- 消融显示**频率降权是双留出的真正贡献者**（+0.052）；noTE 变体 3 个种子双留出均优于 v3（均值 +0.029），但 LOGO 仅 0.790（牺牲先验场景）。

**考虑过的替代方案**：
- 直接切换 tree_v4 为默认：被门槛否决（双留出不明确优于 v3，MAE 一致更差）。
- 切换为 noTE 变体：双留出更优但放弃 LOGO +0.088 的先验收益；App 当前场景（已知单体池推荐）更接近 LOGO 态，暂不采纳。
- 更强正则（medium/strong）：双留出单调更差，瓶颈在特征信息而非模型容量。

**何时复盘**：
- 若 App 使用场景转向"大量未见单体筛选"，重估 noTE 变体或 LOGO/双留出双模型策略。
- 新数据（更多单体覆盖）进入后重跑 `scripts/stage11_dual_holdout.py` 全量对比。
- 前端 SHAP 打分理由接入 tree_v4 前，需让 attribution 从 pkl `te_rates` 填充 TE 特征（当前默认 v3 不受影响）。

---

## D22 — 双模型路由上线（tree_v4 池内 + tree_v4_noTE 外推）；指纹/校准路线关闭

**决定**：
1. **App 树模型从"单一 tree_v3"改为双模型路由**（`src/predictor/routing.py` + `FilmPredictor` 路由模式）：
   - 路由键：输入醛/胺 SMILES 是否都在训练单体池（`models/monomer_pool.json`，由训练数据生成、与 tree_v4 的 te_rates 键交叉校验一致，醛 896 / 胺 1366，加载后常驻内存不读 CSV）。
   - 醛和胺都已见 → **tree_v4**（TE 先验有效，LOGO 0.8784 池内最强）；任一为未见 → **tree_v4_noTE**（频率降权+弱正则，双留出 3 种子均值 0.6824 全体最强）。
   - 预测结果标注实际使用模型与路由原因（如「已知单体组合 → tree_v4」「含未见单体 → tree_v4_noTE（外推模式）」），App 概率区与打分理由双处展示。
   - 打分理由跟随实际路由的模型（v4 走 te_rates 填充、noTE 走原 v3 路径，两者 attribution.py 均已支持）。
   - 向后兼容：显式传 `tree_model_path` 或 `use_routing=False` → 旧单模型行为；`TreeFilmPredictor`、`DEFAULT_TREE_MODEL`（tree_v3）、GNN 并行输出不变；路由资产缺失时自动回退单模型 tree_v3。
2. **训练并保存 `models/tree_v4_noTE.pkl`**（exp_005 的 CV 变体落地为最终模型）：v4_mild 参数 + 频率降权、无 TE 列，全量 3094 行训练，自描述 pkl（metrics 含特征开关 + LOGO/双留出多种子指标，不含 te_rates）。
3. **池内模型维持 v4_int（含 Hadamard 交互），不切换 v4_noint**：
   - v4_noint LOGO 0.8933 vs v4_int 0.8784（+0.015）——逐折配对检验：差值均值仅 +0.010、差值 std 0.019，10 折中仅 6 折胜，**在 fold 级噪声内**；
   - 双留出 3 个种子 v4_noint 一致 −0.014（0.6278 vs 0.6418），方向稳定——去交互损失的是真实可泛化信号（exp_007）；
   - 交互项是「打分理由」配对级解释（"醛-胺交互匹配"分组）的来源，去交互会削弱 App 核心交付的解释价值。
4. **关闭两条已证伪路线**：Morgan/MACCS 指纹入模（exp_006：LOGO 无增量、双留出净负担，tree_v5 仅存档不纳入路由）；概率校准（双留出 MAE 与 PR-AUC 背离的修复尝试证伪，见 `reports/calibration.json` / `scripts/calibrate_oof.py`）。

**为什么**：
- D21 复盘点已到：双模型策略的全部候选（v4 / noTE / v3 / v5）已在同口径多种子协议下测完，场景-模型映射证据齐全——池内 TE 先验 +0.088、双未见 noTE +0.041 且 MAE 更优，单一模型无法两头占。
- 路由验证（exp_008）：routed 对原默认 v3 **全线不劣**（随机 KFold 整体 +0.005、双已见桶 +0.007；LOGO 整体 +0.031、胺未见桶 +0.056），是帕累托改进。

**考虑过的替代方案**：
- 单一 tree_v4 为默认：exp_008 显示它在随机 KFold（0.9513）与 LOGO（0.8814）整体最高，但在 D21 金标准外推协议（双留出网格）0.6418 < noTE 0.6824 且 MAE 一致偏差、逐种子 std 最大——双未见查询无兜底，否决（与 D21 同一门槛逻辑）。
- 单一 noTE 为默认：放弃池内 TE 先验收益（LOGO 0.790 vs 0.878），否决。
- **routed_strict（仅双未见走 noTE，"一新一熟"也走 v4）**：exp_008 证据显示它在全部已测桶上不劣于上线规则（混合桶 +0.024~0.031）——是证据明确的改进候选，但"一新一熟"在真实 App 查询中的占比未知，且双未见场景 v4 vs noTE 两协议方向冲突（噪声量级），暂不擅自改任务书规定键，列为第一复盘点。
- 池内用 v4_noint：见上第 3 条，噪声内的 LOGO 收益不抵双留出稳定损失与解释价值损失。

**何时复盘**：
- ~~**第一优先**：App 查询日志积累后，按"一新一熟"查询占比评审是否切换 routed_strict（exp_008 已给出切换收益上限 +0.024~0.031 on 混合桶）。~~ **已复盘（2026-07-20，D23）**：评审通过，路由规则已切换为 routed_strict——复盘发现该评审不依赖查询占比（占比只影响收益大小、不改变方向），故不待日志直接切换。
- 新数据（更多单体覆盖）进入后：重跑 `stage11_dual_holdout.py` + `stage12_routing_eval.py`，单体池需重新生成（`stage12_train_noTE.py`）。
- 表征升级（GNN embedding 迁移）若落地，路由两臂都应在新表征下重测。

---

## D23 — 路由规则切换为 routed_strict（仅双未见 → noTE，其余 → v4）；双留出逐折报告标配化

**决定**：
1. **App 路由规则由 routed（任一未见 → noTE）切换为 routed_strict**：仅醛/胺**均**未见于训练池（双未见）→ tree_v4_noTE（外推模式）；其余（双已见 / 一新一熟）→ tree_v4。D22 第一复盘点关闭。
   - `src/predictor/routing.py`：仅 `ROUTE_BOTH_UNSEEN` 映射外推臂；路由原因文案同步（「含未见单体（醛/胺）但非双未见 → tree_v4（沿用池内模型）」「双未见单体 → tree_v4_noTE（外推模式）」），App 概率区与打分理由经 `route_reason` 自动同步。
   - `tests/test_routing.py` 跟随新规则并补边界用例（14 → 17 个：新增混合桶路由、四键映射、混合对归因跟随）。
2. **双留出逐折报告标配化**：`scripts/stage11_dual_holdout.py` 的 `run_cv` 输出标配 `per_fold`（逐折 n/pos_rate/PR-AUC/MAE）+ `fold_summary`（fold 级均值±std + 最难折）；`fold_summary()` 为共享公式，`stage11_hadamard_ablation.py`（新增 `--summarize-existing` 免训练补汇总）与 `stage12_fingerprint.py` 继承同一标配。

**为什么**：
- **routed_strict 评审（exp_008 既有证据，`reports/routing_eval.json`）**：
  - PR-AUC 全桶不劣于 routed：协议 A 混合桶 **+0.024**（3 个种子逐一胜：+0.020/+0.022/+0.031，非噪声）、含未见桶 +0.026、整体 0.9480 vs 0.9383（逼近单一 v4 的 0.9513）；协议 B 胺已见桶 **+0.031**、整体 +0.014。
  - 协议 B 胺已见桶 MAE 亦优（0.159 vs 0.187）；唯一回退是协议 A 混合桶 MAE **−0.0075**（0.1222 vs 0.1147），幅度约为 PR-AUC 收益的 1/3，且项目主指标为 PR-AUC（推荐排序场景）。
  - 双已见/双未见两桶两规则构造上相同——切换不动池内收益与双未见兜底。
  - D22 暂缓理由（"一新一熟"真实占比未知）**不构成分布依赖**：routed_strict 在每个已测桶上都不劣，占比只影响整体收益大小、不改变方向；随机流量下混合查询约占 48%，本就是高频形态。故不待 App 日志，直接切换。
- **逐折标配**：fold3 诊断（exp_007）证明合并指标掩盖难折——v3_ref 双留出 fold 级 std 实测 **0.209**，最难折 fold3 仅 0.334 而其余折 0.69-0.91；fold 级均值±std + 最难折是今后一切外推评估的最低报告口径。

**考虑过的替代方案**：
- 维持 routed 待 App 查询日志：否决——证据已显示弱占优且方向不依赖占比，等待只推迟确定收益。
- 混合桶折中（按单侧 TE 置信度动态选臂）：证据不足且引入新超参，列为表征升级后再议。
- 逐折数据重跑全部历史实验补齐：否决——hadamard 既有逐折数据免训练补汇总，stage11 以单 variant 单协议（v3_ref dual）验证格式，stage12_fingerprint 既有报告保留原样、下次运行自动继承，避免重复训练。

**何时复盘**：
- 协议 A 混合桶 MAE 小幅回退（−0.0075）：若下游出现对概率绝对值敏感的用法（如固定阈值筛选），复评是否需对该桶做校准或单独看板。
- 双未见场景 v4 vs noTE 两协议方向冲突（双留出网格 noTE 胜、LOGO 胺未见桶 v4 胜，均噪声量级）仍未解：表征升级（GNN embedding 迁移）后两臂同口径复测。
- App 查询日志继续积累：观察双未见真实占比与 noTE 臂 MAE 优势是否兑现。

---

## D24 — GNN embedding 迁移：阶段 A 通过门槛 → 立项阶段 B（逐折重训 GNN）；mono_emb 证伪

**决定**：
1. **GNN embedding 迁移路线立项继续**（exp_009，阶段 13 阶段 A）：配对级 embedding（`[ea‖eb‖ea⊙eb‖e_pair]` 512 维，交叉注意力后）入 XGBoost 使双留出 3 种子均值 **0.6824 → 0.844**（pair_only 0.8431 / pair_plus 0.8442，逐种子一致大胜，门槛 +0.03 的 16 倍）——按事先约定通过阶段 A 门槛，**立项阶段 B：逐折重训 GNN**（每个双留出折只用训练折数据重训 → 提取 embedding → 同口径评估），排除"GNN 见过验证折单体"的记忆泄漏后仍保住显著收益才算确认。
2. **mono_emb（注意力前纯单体 256 维）证伪关闭**：双留出 −0.01 且逐种子方向不一致——价值集中在配对/界面级表示，不在单体级。
3. 阶段 A 期间不改 App/predictor/routing；embedding 资产（`data/interim/gnn_emb_v53_*.npy`）不入 git。

**为什么**：
- 双未见泛化 0.63–0.68 是项目第一瓶颈，手工描述符触顶、Morgan 指纹证伪（exp_006）后，GNN 表征迁移是唯一未试且理论对症的路线。
- 阶段 A 设计为**便宜证伪**：全量 GNN embedding 是乐观上界——无提升即干净关闭；实测大幅提升，故按预案进入阶段 B。
- **+0.16 与泄漏形态量级上不可区分**：GNN v5.3 训练集与本数据单体几乎完全重叠，embedding 可能携带配对标签记忆（gain 分组诊断 embedding 维占 81.1%）。阶段 A 不能证明泛化收益，阶段 B 是唯一的鉴别手段。
- 附带发现：seed42 fold3（区域漂移难折，n=203）0.364 → 0.65–0.68，各最难折大幅抬升——若阶段 B 确认，任务 2（fold3 型切片）同步受益。

**考虑过的替代方案**：
- 直接相信阶段 A 结果上线：否决——泄漏未排除，违反 D21 以来的外推金标准纪律。
- 阶段 A 无提升就关闭全路线：预案如此，实测未触发。
- 指纹/单体级表征（mono_emb、Morgan）：均已证伪，关闭。

**何时复盘**：
- 阶段 B 完成（先 1 种子 5 折摸底训练成本，再 3 种子全量）：收益保住 → 接入路由两臂重测（D23 遗留的双未见 v4/noTE 两协议冲突同口径复测）+ App 预测时 embedding 提取方案；收益消失 → 路线关闭，转向外部数据预训练/配对级手工表征。
- pair_only 与 pair_plus 打平（0.8431 vs 0.8442）：阶段 B 确认后可评"纯 embedding 替代描述符"简化方案。

## D25 — GNN embedding 迁移阶段 B（闭卷）证伪，路线关闭；阶段 A 增益判为配对标签记忆

**决定**：
1. **GNN embedding 迁移路线关闭**（exp_010）：逐折从零重训 GNN（闭卷，验证折单体从未见于 GNN）后，pair embedding 入树模型双留出 3 种子均值 **0.6480 vs 闭卷基线 0.6824（−0.034，逐种子 −0.039/−0.061/−0.004 全部负）**——按 D24 事先约定的对称门槛判定 **FALSIFIED**。阶段 A 的 +0.1618 判定为**配对标签记忆**（GNN 训练集与本数据单体重叠），不作任何泛化收益解读。
2. **闭卷对照基准确立**：严格双未见口径下，GNN 端到端直接预测 0.6317 < 描述符树模型 0.6824——"树 vs GNN"在双未见场景**树模型胜**，手工描述符（142 维）仍是当前最强外推表征。
3. **双未见瓶颈剩余方向重排**：(a) 外部数据预训练表征（与本数据无重叠，天然闭卷，唯一干净的学习型表征来源）；(b) 配对/界面级手工表征；(c) 接受 noTE 臂 0.6824 为当前外推上限，优先做 fold3 型区域漂移切片与检测。
4. 附带基础设施修复：`dual_holdout_folds` 折分组依赖 pandas `value_counts` 顺序、跨 pandas 版本漂移（dphuanjing py3.8 vs .venv py3.12 实测不一致）——折定义单一真源改为 .venv 侧 stage11 原版函数落盘 `data/interim/stage13b_folds.json`，跨环境脚本一律只读该文件。

**为什么**：
- D24 已明确阶段 A 是乐观上界、阶段 B 是唯一鉴别手段；闭卷 3 种子一致为负，证据干净彻底，无需再试蒸馏/降维等变体（同源训练的信息瓶颈在表征本身，不在迁移方式）。
- 旧项目"GNN 筛选阶段泛化差"在严格协议下复现并量化（0.6317 < 0.6824），为"树为主、GNN 并行"（D02/D08）补上最后一块证据：双未见场景 GNN 也不占优。

**考虑过的替代方案**：
- 只跑单种子就下结论：否决——−0.039 在单种子噪声（±0.03）边缘，逐种子一致性是判定前提（实测 3 种子全负，结论才成立）。
- 换蒸馏/对比学习等迁移变体再试：否决——闭卷 embedding 与描述符拼接、单独使用均负，且 GNN 端到端同样负，瓶颈在表征而非迁移技术。
- 把阶段 A 的 0.844 当"池内场景可用"保留：否决——池内场景 tree_v4（TE 先验，LOGO 0.8784）已更强且合规，无需引入含记忆成分的表征。

**何时复盘**：
- 外部预训练表征若立项（如 MolFormer/ChemBERTa 类与本数据无重叠的预训练模型），用阶段 B 同一闭卷管线（`stage13b_*` + `stage13b_folds.json`）直接对比，闭卷 embedding 资产（15 折 GNN）可作"同源 GNN"对照组。
- fold3 型切片（任务 2）落地后，复评是否需要为区域漂移单体设计专门的外推检测/降级策略。

## D26 — 真实回测（14 组已做实验）：新模型显著比旧 GNN 诚实；单体解析纪律与两处采信留痕

**决定**：
1. **真实回测完成并采信结论**（exp_011，对齐建议第 1 条落地）：当前路由模型对 14 组已做真实实验的回测显著优于旧 GNN v5.3 当年预测——6 组配对（A/A2/B/D/E/F，真实全失败）平均分 0.687→0.254、Brier 0.489→0.112（4.4 倍改善）；旧模型 5/7 组 ≥0.59 虚高全灭，新模型 13 组可预测实验中仅 2 组 ≥0.5（A1/A8），且与唯一相对成功组 A5 同属 TAPT+A6 组合（该对并列第 1/13）。低分主导信号为 TE 文献先验（B5 −0.573、A7 −0.590 等），非对用户实验的事后拟合（用户实验从未进入训练数据）。论文叙事口径定为「不虚高 + 文献先验独立看衰 + 排序命中」，**不宣称「预测了失败」**（失败主因是条件/操作，模型对条件盲）。
2. **单体解析纪律（宁缺毋滥）**：SMILES 仅由 docx 明确化学名构建 + RDKit MW 校验（|Δ|≤0.5）+ 旧池 canonical 互证；解析不了的列待确认清单，不猜。本次 17/21 通过；**H1–H4 全氟链酰肼待用户确认**（方案化学名构建结构计算 MW 与记录 CAS 标注 MW 冲突：H3 690.28 vs 918.37），确认前 G5 与实验 C 不出预测。
3. **两处采信留痕**：(a) B3 记录 CAS（316-64-3）与旧池条目 CAS（448-97-5）不一致，按名称 + MW 双重吻合采信；(b) G–N 组合标题节点（TAPB）与正文（TAPT）不一致，按标题口径计分，备查口径存 `alt_node_prediction`。
4. **base rate 陷阱对冲方法入库**：14 组几乎全失败时，「全打低分」会显得准——用未做组合（方案 13 组合 + 计划 G–N）的分数分布证伪「只会打低分」（新模型 0.03–0.72 全区间，13 方案组合 8 个 ≥0.5）。此后一切真实回测报告必须附分布证据。
5. `data/experimental_refs/实验ABCDEF.docx`（9.6MB）不入 git（本地保留副本）；其余产物（报告 + 4 个 stage14 脚本 + 方案 docx）入 git。

**为什么**：
- WORKFLOW_ALIGNMENT 判定的第一偏移就是「验证错位——项目从未用真实实验回测过」；`实验ABCDEF.docx` 是现成的最有说服力验证集，回测直接产出论文核心对比（新模型 vs 旧 GNN 虚高）。
- 旧 GNN 当年对 A/B 等池内常见单体组合打 0.81–0.82 仍全灭，新模型的 TE 先验恰在池内臂最强——回测同时验证了 D21（TE 入模）与 D22/D23（路由）在真实场景的价值。
- H 系列 MW 冲突若靠猜结构强行出分，会污染「宁缺毋滥」纪律与论文可信度；列为待确认的成本远低于猜错的风险。

**考虑过的替代方案**：
- 重跑 GNN subprocess 补齐「综合概率」：否决——旧 GNN 当年预测已从记录提取，重跑同一 v5.3 无信息增量，且 subprocess 慢。
- H3 按方案化学名直接出分：否决——MW 冲突说明记录结构与方案结构可能不是同一物，猜结构违反解析纪律。
- 把 A5 的 partial 当正例算「命中率」：部分采信——阈值口径分开报 fail/partial，排序口径单独报（A5 并列第 1），不混入单一指标准确率。

**何时复盘**：
- 用户确认 H1–H4 结构后：补 C 与 G5 四组预测（预期 both_unseen 外推臂 + OOD 提示），更新回测报告。
- 后续实验出正例（尤其 G2/G4 高分组：TAPT+A2 0.699、TFPT+B1/B2 0.65）后：双向验证，更新真实命中率。
- OOD/不确定性正式输出（对齐建议第 3 条）立项时：C(TFPT+H3) 作为现实需求案例引用。

---

## D27 — 不确定性 = 多种子 bagging；OOD 三级制（none/warning/out）；「打分非概率」口径

**决定**：
1. **不确定性方案 = 多种子 bagging**（exp_012）：同一 v4_mild 配置仅变 random_state 训 5 个子模型（种子 [42,123,7,2026,555]），预测输出 mean ± std，std 作为认知不确定度。双臂均切换为集成版：`models/tree_v4_ens.pkl`（池内臂，含 TE）/ `tree_v4_noTE_ens.pkl`（外推臂），自描述 pkl 含 "ensemble" 键；`TreeFilmPredictor` 扩展后单模型 pkl 向后兼容（std=0）。路由默认资产切换为集成版，单模型 pkl 保留可显式回退。
2. **OOD 三级制**（`src/predictor/ood.py`）：等级 none / warning / out + 中文原因，取三类检测最高级——(a) 官能团适配性：胺侧酰肼/肼/羟胺等非标准成键基团、无伯胺/仲胺、醛侧无醛基 → **out**（"模型不适用"，对应 H3 案例）；(b) 单体新颖性：双未见 → **warning**（与路由 noTE 臂联动）；(c) 特征区域漂移：16 项关键特征超训练 5%–95% 包络（`models/feature_envelope.json`，入库）比例 >10% → **warning**（对应 fold3 诊断）。App：warning 黄条照出分数，**out 红条不出分数**（GNN 同挂），且不提供打分理由。
3. **「打分非概率」口径（论文要求）**：界面与报告的"成膜概率"统一改为「成膜打分（倾向性）」+ 语义说明"四级软标签上的倾向性打分，非严格概率；对反应条件不敏感"。

**为什么**：
- exp_011 证明 OOD 缺口是现实需求（C 组 TFPT+H3 酰肼）：H 组暂不参与预测是用户决策，但 App 必须能识别并明示，而不是硬报一个数——旧 GNN 当年还知道标"模型不适合非标准官能团"。
- bagging 是成本最低的认知不确定度来源：训练 10 个子模型仅 ~10s，冷启动加载 3.4s，预测无额外成本；比 MC dropout / 深度集成改造小一个量级。
- 集成精度验证（`reports/stage15_ensemble_cv.json`）：LOGO 两臂 ensemble ≥ 单模型（+0.007/+0.001）；双留出与单模型 s42 的差异 8 格中 7 格 ≤0.011（噪声量级）且 ≥成员均值；唯一例外 v4/双留出 s42 −0.047，经逐成员诊断确认为 **seed42 单模型在该划分的偏运气单点**（该划分 5 成员均值 0.609，换 s7/s123 划分差异收窄至 −0.002/−0.011）——按"不低于单模型平均水平"口径通过，如实披露单点口径不通过。

**考虑过的替代方案**：
- MC dropout / 深度网络不确定度：否决——主模型是 XGBoost，无 dropout 可言；换模型代价远大于收益。
- 共形预测（conformal）：暂缓——需要校准集划分且输出区间而非 std，与"± 不确定度"展示形态不符；std 不满足覆盖保证但排序提示够用。
- OOD 用马氏距离/孤立森林等通用检测：否决——化学语义检测（官能团 SMARTS）更准更可解释（直接产出中文原因）；包络法对应 fold3 诊断的具体形态，比通用检测更贴需求。
- out 时仍出分数但加警示：否决——exp_011 的教训就是"硬报一个数"误导实验师；不适用就是不出数。
- SHAP 归因改为多成员平均：暂缓——成员[0] 近似（成员间 std ~0.01，归因结构一致），5 倍成本暂不值得；测试容差放宽到 0.05 对应此近似。

**何时复盘**：
- H1–H4 结构经用户确认后：C/G5 预测自动走 OOD out 路径，验证闭环体验；若有成功实验正例，复测 warning 等级的校准（warning 样本的真实命中率是否低于 none）。
- bagging std 的校准性未验证（std 大是否真的错得多）——积累 App 真实预测日志后做 std vs 误差相关性分析。
- 包络阈值 10% 与 5%–95% 分位为启发式：若 warning 过多/过少，按 App 日志调阈值。

---


## 附：用户原始 8 个问题 → 决策映射





| 用户问题 | 对应决策 | 解决方式 |
|---|---|---|
| 1. 样本失衡 | D01 | 改推荐任务，不强行造负样本 |
| 2. GNN 泛化差 | D02 | 树模型负责解释+条件，GNN 并行保准确率 |
| 3. 粉末≠不能成膜/条件丢失 | D03+D04 | 条件作特征；粉末=0.8弱正 |
| 4. LLM标签噪声 | D09 | 数据审计中建立多层置信度+量化噪声率 |
| 5. 规则嵌描述符对不对 | D05 | 领域规则当特征合理，继承34维 |
| 6. 上下文丢失 | D06 | 五份活文档+首读尾写工作流 |
| 7. CLAUDE.md不更新 | D06 | 活文档替代，随项目演化 |
| 8. RF像相似度排序 | D02 | 描述符尺度归一化，非模型问题 |

---

## D28：两仓库合并（subtree 保留历史）+ 启动器单实例重启语义 + gradio 6.20 theme font 规避

**日期**：2026-07-21
**决策**：
1. minimax（shiyandiedai）以 `git subtree add --prefix=minimax` 并入主仓库，保留全部 25 个提交历史；物理资产（知识库 850MB、向量索引 7GB+）复制但不入库；shiyandiedai 挂迁移公告归档
2. App 启动器改为单实例（命名 mutex）+ 重启语义（PID 文件，双击=杀旧启新），杜绝旧代码进程占位与双击竞态
3. gradio 6.20 主题不使用 `font=` 字符串参数（launch() 内置主题比较会崩），中文字体栈放 CUSTOM_CSS

**原因**：两项目经 data/rag_export/ 契约已事实耦合，分仓维护成本高；启动器事故（旧进程占位+竞态）导致用户 App 不可用；theme font bug 是新代码起不来的第二根因。
**何时复盘**：gradio 大版本升级时复查 theme 兼容；旧 minimax 文件夹删除后 remove minimax-local 远程。
