# UI 美化方案（v1.9.3 问题 6）

> 目标：在不改变任何功能、数据流与交互语义的前提下，把界面从「功能可用」提升到
> 「精致、统一、有呼吸感」。所有改动限于样式与布局层（Tailwind class / CSS 变量 /
> 局部组件结构），**不动 API、不动状态逻辑、不动 Electron 壳**。

## 〇、真实基座（2026-09-12 复核，修正早前误判）

走查后发现本项目**已有相当成熟的设计基座**，早前「设计令牌零散、紫金单主题」的
判断不准确：

- `webapp/src/index.css` 已有 **3 套配色主题 × 明暗 = 6 套** CSS 变量：
  `warm-paper`（暖纸松石，默认）/ `graphite-lab`（石墨仪器）/ `purple-gold`（学术紫金），
  由 `src/hooks/use-theme.ts` 管理；深色下语义色自动换为更亮取值。
- `tailwind.config.js` 已把 `primary/gold/muted/accent/border/card/popover` 与
  **语义色 `success / warning / info / destructive`** 令牌化，圆角/阴影/字体栈齐备。
- 组件库已含 `Button`（含 `loading` 态、`gold` CTA 变体、统一 focus ring）、
  `Card / Badge / Tabs / Dialog / Table` 等。

**因此 U1 的真实短板不是「没有令牌」，而是「有令牌却没用」**：全前端仍有
**217 处硬编码调色板类**（`emerald/amber/red/yellow/green/...`，分布在 22 个文件），
它们绕开语义令牌，导致切换主题/深色时状态色失控（浅底深字在深色下不可读）。
这是「界面丑」的主要来源；其次是留白节奏与层级表达。

硬编码分布（前 12，2026-09-12 实测）：

| 文件 | 处数 |
|------|------|
| `pages/Dft.tsx` | 45 |
| `components/dft/DftGlobalChip.tsx` | 18 |
| `pages/Settings.tsx` | 18 |
| `components/mine/LiteratureIntakeSection.tsx` | 16 |
| `components/query/ResultCard.tsx` | 16 |
| `components/settings/GnnEvolutionPanel.tsx` | 16 |
| `pages/Query.tsx` | 12 |
| `components/mine/LiteratureKnowledgeSection.tsx` | 11 |
| `components/dft/DftResultPanel.tsx` | 10 |
| `components/mine/FavoritesSection.tsx` | 10 |
| `components/assistant/DailyBriefCard.tsx` | 8 |
| `components/assistant/NudgeBar.tsx` | 8 |

按颜色：`amber 84 / red 60 / emerald 30 / yellow 22 / green 18 / 其他 3`。

## 一、U1（进行中）：语义令牌迁移 + 状态色统一

- 规则：`emerald|green|teal|lime → success`、`amber|yellow|orange → warning`、
  `red|rose → destructive`、`blue|sky|cyan|indigo → info`；
  浅底 → `bg-<令牌>/10`、边框 → `border-<令牌>/30`、文字 → `text-<令牌>`、
  实心 → `bg-<令牌> text-<令牌>-foreground`。
- 例外：同一图表内为区分 ≥3 个数据系列而选的多种色相（能级/光谱/多曲线图例）
  保留，不做语义化。
- 验收：迁移后硬编码计数只剩例外；`tsc -b` + `vite build` 通过；逐个主题目视检查。

## 二、U2/U3（进行中）：截图驱动的页面级打磨

**关键手段**：`scripts/ui_screenshot.cjs`（Electron 离屏截图，支持整页 / 局部特写 /
交互后截图）。它把「界面丑」变成可验证问题——本轮已借此修掉 6 处真实缺陷：

| # | 缺陷 | 影响面 | 状态 |
|---|------|--------|------|
| 1 | 侧栏 `bg-sidebar-background` 类未生成（tailwind `sidebar` 缺 `background` 键）→ 浅色主题下侧栏透明、浅灰字不可读 | 全站 | 已修 |
| 2 | `payload.confidence` 是对象却按数值 `*100` → 「置信度 NaN%」 | 首页 | 已修 |
| 3 | 收藏/方案/历史的 ISO 时间戳裸渲（`2026-08-19T08:52:00+08:00`，另有带秒/不带秒混用） | 5 处 | 已修 |
| 4 | 收藏卡高矮不齐 + SMILES `break-all` 折三行 | 我的 | 已修 |
| 5 | `prediction_snapshot.ood` 对象直渲 → `OOD：[Object object]` | 实验记录列表/详情 | 已修 |
| 6 | 知识库无摘要提示文案易误解 | 科研知识库 | 已修 |

**页面复核进度**：首页 / 查询打分 / 批量排序 / 实验记录 / 方案迭代 / DFT / 我的（含
科研知识库）/ 科研助手 / 设置 —— **9 页全覆盖，其余未发现缺陷**。
其中 DFT 右侧大面积留白经查为「结果区未计算时的引导占位」，属设计意图，未改。

**问题 1 的界面级验证**（借助 SHOOT_JS 交互截图）：打开含深度研究报告的会话后，
页面渲染 14 个外链、**0 个脏链接**，含成对括号的复杂 DOI
（`.../Websites_(Inorganic_Chemistry)/...`）完整保留 —— 证明链接修复在真实界面生效。

**仍可选（等你选基调再动）**：
- 全局密度开关 —— **已实现**（设置页「界面密度」：舒适 / 紧凑，写 localStorage 并通过
  `<html data-density>` 生效；紧凑档收紧卡片内边距与所有表格行高。实现要点：hook 挂在
  常驻的 AppLayout 上（否则其他页面不会应用该属性——首版就踩了这个坑），并广播事件做
  跨组件同步；截图对比确认紧凑档同屏可多显示约一条记录）。
- 默认主题是否由 `warm-paper` 改为 `purple-gold`（仍等你定）。

---

## 三、原诊断（保留备查，部分已被上文修正）

| 问题 | 具体表现 | 影响 |
|------|----------|------|
| 令牌用不起来 | 状态色 217 处硬编码，绕开 6 套主题的语义令牌 | 换主题/深色时状态色失控 |
| 层级表达弱 | 页面 = 一串同权重 Card；标题、副标题、说明文字字号/颜色梯度不明显 | 视线无落点 |
| 留白节奏乱 | `p-3 / p-4 / px-4 py-2` 混用；卡片间距 `gap-2 / gap-3 / gap-4` 随机 | 页面显得拥挤不齐 |
| 状态样式不统一 | 空态、加载、错误、成功提示各页各写一套 | 体验碎片化 |
| 交互反馈单薄 | focus ring / hover / disabled 样式不统一 | 不像成品软件 |
| 表格/图表密度 | 收藏表格、记录列表行高与字号不统一 | 数据页浏览效率低 |

---

## 二、设计原则（本次改版的边界）

1. **信息架构不动**：不新增/删除功能入口，不调整页面路由与字段。
2. **沿用现有 3 主题体系**（warm-paper / graphite-lab / purple-gold × 明暗），
   不改主色相；所有状态色一律走语义令牌，保证 6 种组合下都成立。
3. **令牌先行**：先让既有令牌被正确使用（迁移硬编码色），再谈组件与页面级打磨。
4. **克制**：不使用大面积渐变、发光、重阴影；科研工具应显专业与安静。
5. **可回退**：每批改动独立提交，任何一批可单独回滚。

---

## 三、令牌化（第一批，基础层）

### 1. 颜色与语义变量（`webapp/src/index.css` + `tailwind.config`）

- 收敛语义色：`--surface`（卡片）/`--surface-muted`（次级面板）/`--border`/`--border-strong`/`--fg`/`--fg-muted`/`--fg-subtle`；
- 状态色统一：`success / warning / danger / info` 四组（各含 `bg / border / fg` 三个代表色），替换散落的 `emerald-*`、`amber-*`、`red-*`、`green-*`；
- 金色只用于「重点/成就/科学强调」（如成膜成功、金色分隔线、主 CTA 描边），不再大面积铺底。

### 2. 尺寸与节奏

- 圆角阶梯：`sm=6 / md=10 / lg=14 / xl=18`（现有 `rounded-lg` 语义映射到 md）；
- 阴影三级：`shadow-card`（静态卡片）/`shadow-pop`（悬浮、弹窗）/`shadow-none`；
- 间距阶梯：`4 / 8 / 12 / 16 / 24 / 32`，卡片内边距统一 `p-4`（紧凑场景 `p-3`）；
- 字号阶梯：`text-[11px] 辅助 / text-xs 说明 / text-sm 正文 / text-base 小标题 / text-lg 页面标题`，并用字重（`font-medium/semibold`）表达层级而非仅靠字号。

### 3. 交互统一

- focus：统一 `focus-visible:ring-2 ring-gold/50 ring-offset-1 ring-offset-background`；
- hover：卡片 `hover:border-border-strong hover:shadow-card`，行 `hover:bg-accent/40`；
- disabled：`opacity-50 cursor-not-allowed` 全局一致；
- 过渡：统一 `transition-colors duration-150`，避免各组件 `transition-all`。

---

## 四、组件层（第二批）

| 组件 | 改动 |
|------|------|
| `Card / CardHeader / CardContent` | 增加 `variant: default / muted / interactive`；标题区统一「图标 + 标题 + 右侧动作」三栏 |
| `Button` | 统一 4 个 variant 的 hover/active/focus；新增 `loading` 态（内置 Loader2 + 文案保持） |
| `Badge` | 语义色对齐状态令牌；新增 `dot` 前缀样式（状态点 + 文本） |
| `Tabs` | 下划线指示器统一金色细线；页签内边距对齐 |
| `Dialog` | 标题区留白加大、底部按钮右对齐间距统一、滚动区加顶部淡出遮罩 |
| `Table` | 表头 `bg-surface-muted/60 sticky`、行高 40/36 两档、数字列右对齐 |
| `Skeleton / EmptyState`（新增） | 统一骨架屏节奏与空态插画位（图标 + 一句话 + 可选主行动按钮） |
| `Toast` | 统一图标颜色与最大宽度，成功/失败/警告视觉分级 |
| `Resizer`（本版已加） | 加入 hover 高亮与键盘提示，视觉与主题一致 |

---

## 五、页面级（第三批，按收益排序）

1. **科研知识库（重点）**：文献列表 → 左列表右详情两栏；条目按组分块（组标题吸顶）；条目徽章统一；附件/SI 区域做成「文件卡」；解析预览的组与勾选层级更清晰。
2. **科研助手**：工具卡（tool_call/result）统一为「单行摘要 + 折叠详情」；报告卡与引用列表视觉分级；输入区加聚焦态与快捷键提示；侧栏与对话区宽度可拖（本版已实现，纳入统一视觉）。
3. **首页仪表盘**：卡片网格统一间距与高度；数字型信息用大字号 + 单位小字；空态统一。
4. **实验记录**：时间线改为「竖向时间轴 + 节点」（左侧时间、右侧描述、附件小图标）；草稿/正式状态用左边框色区分；详情页分区标题统一。
5. **方案迭代**：建议卡统一为「标题 + 置信度条 + 依据摘要 + 行动按钮」；批次分组吸顶。
6. **设置页**：分组卡片化（LLM/检索/GNN/文献解析/界面），组内 label + 控件两列对齐；危险操作红色分区。

---

## 六、可选的体验增强（不含功能变更）

- **密度开关**：右上角「紧凑 / 舒适」切换（写 localStorage，调整 `--density` 影响行高与内边距），适合数据密集的收藏/记录页；
- **主题**：补全深色模式变量（现有 `next-themes` 已接入），使明暗切换无死角；
- **动效**：仅保留「卡片进入 120ms 淡入 + 上移 2px」「折叠展开高度过渡」，不使用弹跳/旋转等花哨动效；
- **图标**：统一 lucide 尺寸（14/16/20）与描边色，避免大小混杂。

---

## 七、交付批次与验收

| 批次 | 内容 | 预估工时 | 验收标准 |
|------|------|----------|----------|
| U1 基础层 | 设计令牌 + Button/Card/Badge/Tabs/Dialog/Table/Skeleton/EmptyState 统一 | 4–5 h | 全站组件视觉一致；tsc + vite build 通过；无功能回归 |
| U2 助手 + 知识库 | 两个核心页面的布局与卡片层级重做 | 4–5 h | 交互路径不变；关键操作（解析/入库/研究/上传）实测可用 |
| U3 其余页面 + 打磨 | 首页/记录/迭代/设置 + 密度开关 + 深色补全 | 4–5 h | 明暗两种主题逐页检查；表单/表格可用性不降低 |

**统一验收清单**：① 间距只出现 4/8/12/16/24/32；② 颜色只来自语义令牌；③ focus 可见且统一；
④ 每页有明确主行动；⑤ 空态/加载/错误三态齐全；⑥ `tsc`+`vite build` 通过、`pytest` 全绿；
⑦ 桌面（≥1280）与窄屏（≤768）逐页检查；⑧ 不改动 API/状态/路由（diff 只含样式与结构）。

**明确不做**：不引入新的 UI 框架或重型组件库；不做全站重构式改写；不改 Electron 主进程与打包配置；不做信息架构调整。

---

## 八、需要用户拍板的两点

1. **默认主题与密度**：默认主题保持 `warm-paper`（暖纸松石）还是改为
   `purple-gold`（学术紫金）？密度默认「舒适」还是「紧凑」？
2. **实施节奏**：U1（令牌迁移，进行中）→ U2（助手 + 知识库布局）→ U3（其余页面 +
   密度开关）分三批验收，还是 U1+U2 合并一批。
   （U1 已按推荐规则执行，因其可逆且不改功能；U2/U3 等你选定风格基调后动。）
