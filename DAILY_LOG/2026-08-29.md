# 日报 2026-08-29（AI 版）

## 今日工作：接续 k3 完成 v1.2.0 发版收尾

### 背景

k3（kimi k3）昨日（08-28）已完成 v1.2.0 的构建链与 GitHub Release 三资产上传，
但在「整理推送远端」时额度耗尽，遗留部分未收尾。本会话接续完成收尾，并核对
发版纪律（详见 `DAILY_LOG/2026-08-20.md`）。

### k3 已完成（核实无误，不重做）

- 构建链三步全跑：`vite build`（dist 17:52）→ PyInstaller（dist-backend 18:03）→
  electron-builder NSIS（exe 18:15，产物在 `webapp/release/`）
- GitHub Release v1.2.0 已创建并上传三资产：
  - `cof-film-recommend-setup-1.2.0.exe`（463,769,941 B）
  - `COF-Assistant-Setup-1.2.0-win-x64.zip`（463,770,107 B）
  - `latest.yml`（v1.2.0，sha512/size 正确，**最后传** 10:29:38Z）
- 远端 latest.yml 内容核验：version 1.2.0、sha512 `/dNZbx...` 与我本地计算逐字符一致

### 本会话修正的 k3 遗留（2 处）

1. **版本号 bump 未提交**：k3 改了 README/api/__init__.py/webapp/package.json 三处
   1.1.0→1.2.0 但未 commit。本会话提交 `d3417f5`（`release: v1.2.0 版本号`）并
   push master（`8fa9e0c..d3417f5`）。
2. **tag v1.2.0 指向错误提交**：k3 推的 tag 指向 `8fa9e0c`（阶段 32 收尾，不含版本
   bump），与历史惯例（v1.0.1/v1.0.2/v1.1.0 均指向「release: vX 版本号」提交）不符，
   且 `git checkout v1.2.0` 会拿到 1.1.0 代码。本会话 `git push origin v1.2.0 --force`
   修正为指向 `d3417f5`。

### 自证清单（打包版后端真机验证，全过）

- 产物时间戳新（dist/dist-backend/release 均 08-28 新）
- 包内关键词命中：psi4 / pdf_extract / crossref / literature；pymupdf、docx、lxml
  原生 DLL（libxml2/libxslt/libexslt/iconv/zlib/zlib-ng2）齐全
- 打包版 `cof-backend.exe` 真实启动 curl 验证：
  - `/api/health` → `version:1.2.0` + tree/gnn/routing 均 available
  - `/` 首页 `Cache-Control: no-cache` + 新 bundle `index-DqBDS-9_.js`
  - `/api/assistant/status` → enabled
  - `/api/dft/backends` → xtb（gfnff/gfn2）+ psi4 1.11（wb97xd3bj_svp precision +
    b3lyp_631gdp literature）双档就绪
  - `/api/literature/records` → 200（文献模块修复生效）
  - DFT 真实任务：苯甲醛+苯胺 self_stack gfnff → done，E_bind −0.091 kcal/mol
- 资产完整性：本地 exe/zip 的 sha256 与 GitHub Release 记录 digest 逐字节一致

### 遗留 / 待用户决策

- **zip 资产命名**：k3 用了英文名 `COF-Assistant-Setup-1.2.0-win-x64.zip`（Release
  body 同步），而 v1.1.0 与 README 用的是中文名 `COF科研助手-Setup-x.x.x-win-x64.zip`。
  功能无影响（latest.yml 只引用 exe，与 zip 名无关）。**已拍板（2026-08-29 用户确认）：
  保留英文名**，README 分发流程引用已同步改为 `COF-Assistant-Setup-x.x.x-win-x64.zip`。
- 13 条文献 DOI 待人工（同前）。
- 用户删除 GitHub PAT（安全收尾，仍挂在 Windows 凭据管理器 `x-access-token@github.com`）。
- BDE-47 literature 档未跑（用户已决定不重跑，同 08-28 日报）。

---

## 追加（下午）：发版一致性核对 + 文档致谢（由 DeepSeek 协助完成）

- 核对最新 Release v1.2.0 与 master 最新提交 `98a6937`：Release tag 指向 `d3417f5`
  （release: v1.2.0 版本号），master 较其领先 2 个**纯文档**提交（`0f379c0` 阶段 33
  双日报、`98a6937` zip 英文名 README 引用同步），无代码/二进制变更 → **无需新发版**。
- 文档更新：README 新增「致谢 DeepSeek」条目；本日报记录本次操作。
- 本操作由 DeepSeek 协助完成。

---

## 追加（晚间）：DFT 计算页状态保持修复（由 DeepSeek 协助完成）

### 问题

DFT 计算耗时较长，用户切页（结果查看页/用户中心等）再返回后，表单填写与计算进度
全部丢失——根因：前端表单/任务状态全部是组件内存态（`Dft.tsx` 的 useState，路由
卸载即销毁）；后端任务注册表纯内存（`_JOBS`，重启即丢）；API 响应不返回输入参数。

### 方案（本机落盘，无服务器、纯本机、零负担）

- 后端任务落盘：`_JOBS` 快照写 `user_data_root()/dft_jobs.json`（tmp+replace 原子写、
  独立持久化锁、失败静默）；FastAPI lifespan 启动时 `load_persisted_jobs()` 恢复，
  遗留 pending/running 标为 **interrupted**（附中文原因）。
- 计算页草稿接口：`PUT/GET /api/dft/draft`（前端定义结构、后端原样存取，落
  `dft_draft.json`）——替代 localStorage（打包版端口自动选择，origin 变化则
  localStorage 读不回，故草稿跟随 user_data_root 更稳）。
- `_public_job` 透出 `input`（原始输入参数，前端据此恢复表单）。
- 前端 `Dft.tsx`：挂载时（无 URL 预填意图时）读草稿恢复表单 + `currentJobId`，
  按任务状态续查/续轮询/展示结果；表单/任务变化防抖 500ms 自动保存；提交后
  **立即**保存（防切页竞态）；`interrupted` 状态中文提示可重提；溶剂默认值改
  `prev || list[0].id` 防覆盖恢复的草稿。

### 测试

- 新增 `tests/test_dft_persistence.py` 8 条（落盘/重启恢复/running→interrupted/
  input 透出/草稿往返与空读）；全量 **792 passed, 1 skipped**；tsc -b + vite build 通过。
- 踩坑修复：任务工作线程在单个测试的 fixture 撤销**之后**完成 → `_persist()` 写到
  真实 `data/dft_jobs.json`（测试污染开发数据目录）→ 根治为 **session 级**隔离
  fixture（`tests/conftest.py`），晚完成线程也写临时目录；`data/dft_jobs.json(.tmp)`、
  `data/dft_draft.json(.tmp)` 补入 .gitignore。
- 文档：`docs/DFT状态保持修复方案.md`（方案备份）。

### 遗留

- 发版到安装包需重跑构建链（vite → PyInstaller → electron-builder，发版纪律）。
- 可选后续：任务列表接口、SSE 实时进度、取消任务按钮（方案文档第五节）。

---

## 追加（2026-08-31 会话续）：v1.2.1 发布（由 DeepSeek 协助完成）

- **版本号**：三处同步 1.2.0 → 1.2.1（README / api/__init__.py / webapp/package.json），
  提交 `4240397`（release: v1.2.1 版本号）。
- **本机验证**：uvicorn 8000 + Vite 3000 dev 模式，草稿端点经代理全链路往返 + 真实
  xTB 任务 input 透出验证通过；用户手动验收通过后拍板发版。
- **构建链三步全跑**：vite build（25.9s）→ PyInstaller（--distpath dist-backend）→
  electron-builder NSIS（`cof-film-recommend-setup-1.2.1.exe`，465,855,406 B）。
- **构建踩坑（新，记入发版纪律）**：Watt Toolkit（Steam++）加速器运行中，其 HTTPS
  中间人证书使 Node 报 `unable to verify the first certificate`（electron-builder 下载
  组件失败）→ 修复：`$env:NODE_OPTIONS="--use-system-ca"` 让 Node 信任 Windows 证书库
  （保留 TLS 校验，优于关闭校验）。PowerShell/curl 不受影响（走系统证书库）。
- **自证清单全过**：包内后端真机启动——health `version:1.2.1`、新 bundle
  `index-Bwio4Cto.js`、no-cache 头、草稿端点 PUT/GET 往返、DFT 真实任务
  （苯甲醛+苯胺 gfnff done，input 透出正常）。
- **分发资产**：exe + `COF-Assistant-Setup-1.2.1-win-x64.zip`（含 exe，465,791,561 B）
  + latest.yml（electron-builder 生成，v1.2.1 sha512/size 正确）。
- **tag**：`v1.2.1` → `4240397` 已推送；GitHub Release 378926031 已建，
  三资产按纪律顺序上传（exe → zip → latest.yml 最后）。

---

## 追加（2026-08-31 傍晚）：DFT 全局任务状态与完成通知（本地分支 feat/dft-global-status，未发布，由 DeepSeek 协助完成）

### 背景（用户新一轮需求）

v1.2.1 已修复「切页丢表单/任务状态」核心缺陷；用户补充期望：① 多次切换（5+ 次）
仍稳定恢复；② **在其他页面实时看到计算进度**（全局状态栏/悬浮窗）；③ **完成主动
提示**（浏览器通知/弹窗/徽标）。要求：仅本地开发验证，**不 push / 不打 tag / 不建
Release**，改动保留在本地分支待合并。

### 实施（全部前端，后端零改动——GET /jobs/{id}、草稿接口均已存在）

- 新增 `webapp/src/components/dft/DftTaskContext.tsx`：
  - `DftTaskProvider` 挂在 App 顶层（路由切换不重建）→ 任务状态全局化；
  - 后台 **5s 轮询** `GET /api/dft/jobs/{id}`；`pending/running` 续轮询，
    转终态（done/failed/interrupted）即停轮询并触发通知；
  - 通知双通道：sonner toast + 浏览器 `Notification`（提交时用户手势内请求权限）；
    仅在「非终态→终态」转变时触发一次（StrictMode 安全，无重复弹）；
  - 连续 3 次轮询失败 → 标 interrupted 并提示（后端重启且任务已清理的兜底）；
  - 应用启动只读一次草稿（`resumedRef` 防 StrictMode 双跑），带 currentJobId 则恢复
    全局跟踪（F5 / 重开应用场景）。
- 新增 `webapp/src/components/dft/DftGlobalChip.tsx`：右下角悬浮徽标，全页面可见——
  计算中金色脉冲 + progress_hint；完成绿色「查看结果」；失败/中断红色；点击跳转
  DFT 页（终态同时关闭徽标），终态带 × 手动关闭。
- `App.tsx`：`<DftTaskProvider>` 包裹路由；`AppLayout.tsx`：挂载 `<DftGlobalChip />`。
- `Dft.tsx`：提交后 `trackTask(job_id, {summary, backend, cached, initialStatus})`；
  **移除页面内完成/缓存命中的 toast**（通知统一由全局上下文发出，避免双弹）。
- 多次切换稳定性复核：页面 hydration 每次挂载执行一次（`useEffect([], …)` +
  cancelled 守卫），草稿防抖保存 + 提交即时保存，全局 Provider 不随路由重建——
  重复切换不会触发「覆盖式重置」。

### 验证

- `tsc -b`（0 错误）+ `vite build`（0 失败）通过；
- 后端无改动，全量 pytest 792 passed 基线不变（本轮复跑确认）；
- 本机 dev 双服务已启动（uvicorn 8000 + Vite 3000），待用户手动验收。

### 范围纪律

- 所有改动提交在本地分支 `feat/dft-global-status`（自 master 8bef1c9 拉出），
  **未 push、未打 tag、未建 Release**；待后续合并统一发版。

---

## 追加（2026-08-31 夜）：界面精修（方向 A）+ v1.3.0 发布（由 DeepSeek 协助完成）

### 界面精修（学术紫金 · 精修版，方向 A）

- 调研参考：Primer（语义变量/双主题）、Cherry、AstralUI（主题引擎）、Qi（单源真值）、
  Square UI（shadcn 同源）、Galaxy/Uiverse（交互细节）、ai-frontend-design-bookmarks。
- 实施（提交 `9ffe97f`，8 文件）：
  - `index.css`：主紫降饱和（263 70% 50% → 258 52% 46%）、金转暖（43 62% 45%）；
    新增语义三色 success/warning/info（此前散落写死）；阴影改带紫味柔和三档
    （soft/card/pop）；新增 `.num-data` 工具类；
  - `tailwind.config.js`：映射 success/warning/info、mono 字体栈（Cascadia Mono/
    Consolas）、soft/card/pop 阴影；
  - `button.tsx`：新增 `gold` 变体 + `loading` 属性（spinner + 禁用防重复提交）+
    按压 `active:scale-[0.98]`；
  - 关键 CTA（Dft「开始计算」/Query「开始打分」）换 `variant="gold"` + `loading`；
  - 数据等宽数字：DftResultPanel 主结合能 `font-mono tabular-nums`、批量排序表三列
    分数、收藏 DFT 徽章结合能。
- 验证：tsc 0 错误、vite build 通过、全量 pytest 792 passed。

### v1.3.0 发布（含全局任务状态 + 界面精修）

- 合并：`feat/dft-global-status` fast-forward 入 master（`cf28906`）。
- 版本号三处 1.2.1 → 1.3.0（`0d8dfe8`，release: v1.3.0 版本号）。
- 构建链三步全跑（vite → PyInstaller → electron-builder）；PyInstaller 首跑瞬态崩溃
  （`_collect_submodules('pygments.formatters')` 子进程退出码 3221225622，与历史
  0xC0000005 同族）→ 重跑即过；electron-builder 沿用 `NODE_OPTIONS=--use-system-ca`。
- 自证清单全过：health version=1.3.0、新 bundle `index-83l6nYny.js`、no-cache、
  草稿端点往返、DFT 真实任务 done + input 透出。
- 资产：exe 465,857,904 B + `COF-Assistant-Setup-1.3.0-win-x64.zip` 465,793,940 B
  + latest.yml（v1.3.0，sha512/size 正确）。
- tag `v1.3.0` → `0d8dfe8` 已推送；GitHub Release 379575558，三资产按纪律顺序上传
  （exe → zip → latest.yml 最后）。
