# 日报 2026-07-21（人版）

## 一句话总结

GNN embedding 迁移路线**第一关通过**：把旧项目 GNN 学到的"配对表示"喂给树模型，双未见单体的打分能力从 0.68 跳到 0.84——但这可能只是"开卷考试"（GNN 以前见过这些分子），下一关"闭卷重考"（逐折重训 GNN）才能定真伪。

## 今日成果

1. **打通了 GNN → 树模型的表征迁移管道**：不改旧项目任何文件，从 GNN v5.3 内部提取出两种分子表示（512 维"配对表示" + 256 维"单体表示"），3094 条数据全部提取完成（GPU 仅需 10 秒）。
2. **同口径严格评估完成**：双留出（醛胺都未见）3 个种子 + LOGO 协议，基线复跑与历史值逐位吻合（0.6824），评估可信。
3. **关键结果**：
   - 配对表示：0.6824 → **0.844**（+0.16，三个种子一致大涨，远超 +0.03 的预定门槛）✅
   - 单体表示：无效，路线关闭 ❌
   - 最难的 fold3（区域漂移大分子）：0.36 → 0.65+，改善最猛——正好对着任务清单第 2 条
4. **重要保留意见**：GNN 训练时见过这些分子，0.84 里可能含"背答案"成分。已按预案立项**阶段 B：每个验证折重新训练 GNN 再测**，排除作弊后才能确认真实收益。

## 关键发现

- **价值在"配对/界面级"表示，不在单体级**——这与项目直觉（成膜是醛胺配对的事）一致，也与 Hadamard 交互特征此前的有效性互相印证。
- 单体级 GNN 表示（注意力前）被证伪，以后不用再试这个方向。

## 下一步建议

1. **阶段 B（第一优先）**：逐折重训 GNN 做"闭卷重考"。先小规模摸底训练成本，再全量。若收益保住 → 接入 App 双模型路由；若消失 → 路线关闭，转向外部数据预训练。
2. 今天任务清单第 2 条（fold3 区域漂移切片）可等阶段 B 结论后一起做，避免重复评估。

## 阻塞点

无。测试 57 项全部通过，App 行为未受影响（本次只是离线评估，未动线上模型）。

---

## 追加：阶段 B 结果——"闭卷重考"判定（当天下午）

### 一句话总结

闭卷重考成绩出来了：**上午的 0.844 确实是"开卷"水分**——逐折从零重训 GNN 后，embedding 迁移不但没收益反而小幅拖累（−0.03，三个种子全部为负），这条路线正式关闭。

### 闭卷成绩（双未见场景，3 个种子平均）

| 选手 | 分数 | 结论 |
|---|---|---|
| 树模型（现有描述符） | **0.6824** | 仍是冠军 |
| 树 + 闭卷 GNN 表示 | 0.648 | 倒亏，证伪 |
| GNN 自己上 | 0.632 | 也不如树 |

### 关键结论

1. **双未见场景"树 vs GNN"有了终审判决：树模型胜**。GNN 端到端（0.632）也不如描述符树（0.682），旧项目"GNN 筛选泛化差"在严格协议下复现。
2. **上午阶段 A 的 +0.16 全部判定为"背答案"**（GNN 训练时见过这些分子）——幸好没拿去上线，两阶段证伪设计值回成本。
3. **fold3（区域漂移大分子）闭卷下全线 0.36**——树、GNN、embedding 全灭。这类分子对任何"从已见分子学习"的方法都是死穴，任务清单第 2 条（fold3 切片与检测）升为下一优先。
4. 附带修复一个隐患：双留出折划分在两个 Python 环境下算出来不一致（pandas 版本差异），已改为单一真源文件，杜绝后续实验踩坑。

### 下一步建议

1. **任务 2（fold3 区域漂移切片与检测）**——既然硬提表征的路都证伪了，"识别出这类分子并提示人工谨慎"比"硬算分数"更实际。
2. 可选立项：**外部数据预训练表征**（用与本项目数据无重叠的预训练模型，天然闭卷）——今天搭好的逐折评估管线可直接复用，但今天先不急，建议先看 fold3 切片。

### 阻塞点

无。App 与线上模型全程未受影响。
