# 日报 2026-08-27（AI 版）

## 今日工作：阶段 30 — 打包版导出/文献 500 修复 + 文献 PDF 录入 + DFT 后端替换方案

### 已完成

**1. 打包版导出实验记录"下载失败"修复（Bug A）**
- 根因：lxml.etree 在 PyInstaller frozen 下原生 DLL 加载失败 → 导出 Word 500
- 修复：scripts/cof-backend.spec binaries 补 6 个 DLL（libxml2/libxslt/libexslt/iconv/zlib/zlib-ng2，来源 E:/ANACONDA/Library/bin/，目标 "."）
- 打包版真机验证：export-bundle 200（37,539B）、单条导出 200

**2. 打包版文献录入 DOI"请求失败"修复（Bug B）**
- 根因：`ImportError: cannot import name 'crossref' from 'literature'` —— src/literature/* 惰性导入被 PyInstaller 漏收
- 修复：hiddenimports 补 literature.resolver/crossref/pdf_extract、records.store/export_docx、llm.client、recommend.plan_templates/plan_card、runtime_config（均带 src.* 双命名空间变体）+ fitz/pymupdf/multipart
- 打包版真机验证：lookup 200

**3. 文献 PDF 录入（新功能，agent-118）**
- POST /api/literature/extract-pdf：multipart 上传（≤20MB）→ PyMuPDF 提取前 15000 字符 → LLM 严格 JSON 提取 {title, authors[], journal, year, doi, abstract} → 走同一条审核卡流，用户确认后入库
- 前端「我的 → 文献录入」改三 Tab：DOI / 标题 / 上传 PDF
- 真机验证：extract-pdf 200，真实 PDF 提取出 Angew. 2025 元数据，DOI 10.1002/anie.202421555 与原文完全吻合
- 新文件：src/literature/pdf_extract.py、tests/test_literature_pdf.py
- 测试基线：**736 passed**（718+18）

**4. DFT 后端替换方案（docs/DFT后端替换方案.md）**
- 背景：用户判定 xTB 精度不可接受（3D 吸附位点摆放不准、误差大）
- 调研结论：PySCF 无官方 Windows wheel 排除；ORCA 禁止再分发打包只能远期"用户自装对接"；ANI-2x 被 MACE-OFF23 全面超越
- 方案：分层架构 = 快速档 xTB（保留）+ 精度档 **Psi4**（真 DFT，ωB97X-D3BJ/def2 基组，counterpoise BSSE 结合能，有 fchk 写出器，conda-forge win-64 可再分发，复用 dphuanjing subprocess 隔离模式按需安装）+ 可选 ML 档 MACE-OFF23（arXiv:2312.15211）
- Psi4 顺带解决此前遗留的 fchk 需求
- 待用户确认后实施，预估 3–4 天

### 遗留

- 13 条文献 DOI 待人工（同前）
- 记录无 updated_at 字段（"当日更新"按 mtime）
- 日报卡/提醒条仅 LLM 启用时渲染
- 建议发 **v1.1.1 热修复版**：v1.1.0 打包版的导出 Word 与文献录入两个功能是坏的（本次已修复并打包版真机验证），待用户拍板
- DFT 后端替换方案待用户确认后开工
- GitHub PAT 仍在会话中使用过，提醒用户删除

---

## 下午追加：阶段 31 — DFT Psi4 精度档后端实施（方案已落地）

### 已完成

**1. psi4-env 环境（conda 子进程隔离，复用 dphuanjing 模式）**
- `conda create -n psi4-env -c conda-forge psi4 dftd3-python python=3.11`（E:/ANACONDA 下，清华镜像 + Watt 加速器）
- 装好：**Psi4 1.11 + dftd3-python 1.5.0**（dftd3-python 是 ωB97X-D3BJ 色散校正的必需 Python 接口——psi4 的 s-dftd3 判定走 `which_import("dftd3")`，装二进制 simple-dftd3 不算数，踩坑后记）
- 一键安装脚本 `scripts/install_psi4_env.bat`（定位 conda → 幂等创建 → import psi4+dftd3 验证），供前端"引导安装卡"调用
- runtime_config 新增 `psi4` 能力（COF_PSI4_PYTHON > runtime.local.json pythons.psi4 > 自动探测 <conda>/envs/psi4-env）

**2. src/dft/psi4_backend.py（核心，约 500 行）**
- 管线：二聚体/X 解析复用 engine → 多取向复合物初猜复用 engine → xTB 预优化作 Psi4 初始几何（缺 xTB 退化为 UFF 初猜）→ 生成 Psi4 Python 脚本 → psi4-env 子进程执行
- 方法：ωB97X-D3BJ/def2-SVP，结合能 `psi4.energy(..., bsse_type='cp')` counterpoise BSSE 校正；片段单点能（能量分解表）；复合物 wfn → HOMO-LUMO gap、偶极矩、**fchk 写出**（归档 data/dft_artifacts/）
- 中文 Windows 三连坑全踩平：① py-cpuinfo 在无 wmic 的 Win11 上 import 即崩（一切 qcengine 路由瘫痪）→ 模板里塞 sys.modules 假 cpuinfo 桩；② psi4 schema_wrapper 按 locale 读输出文件遇非 ASCII 炸 gbk → 子进程 PYTHONUTF8=1；③ 子进程中文进度行 → PYTHONIOENCODING=utf-8
- 分子串 `symmetry c1` 固定原子顺序，fragment_ranges 与 xTB 口径一致，3D 展示零改动
- 进度回传：脚本 stdout `@@PROGRESS@@` 行 → on_stage 实时阶段提示；超时默认 1800s（COF_DFT_TIMEOUT_PSI4 覆盖），失败返回中文结构化错误

**3. API / 任务 / 缓存**
- POST /api/dft/jobs 增 `backend: "xtb"|"psi4"`（任务书原文写 /api/dft/calculate，实际端点是 /jobs，等同扩展）；psi4 未装 → 503 + 安装引导文案；响应带 backend 字段
- GET /api/dft/backends：xtb/psi4 可用状态 + 方法档 + install_hint
- jobs.py 按 backend 路由 engine/psi4_backend；缓存 key 含 backend（xtb 保持旧串格式兼容存量缓存，两档互不命中）；历史 dft_log.jsonl 增 backend/method_label 字段

**4. 前端（webapp）**
- DFT 页新增"计算后端"选择器（xTB 快速 / Psi4 精确）；选 Psi4 未装时显示引导安装卡（说明 + 每 15s 自动轮询检测 + 立即重新检测按钮），装好即解锁
- Psi4 档位进度文案（分钟级提示）、进行中不阻塞；结果面板增 Psi4 精度档/BSSE 校正徽标、方法基组、未校正参考值、fchk 已生成标注、**同组合另一档最近结果并排对比行**
- 历史记录条目带 Psi4 徽标；收藏 dft_snapshot/dft_entries 带 backend；FavoritesSection 方法标签兼容 wb97xd3bj_svp
- Ketcher 画板未动

**5. 验证**
- tests/test_psi4_backend.py 新增 29 条（脚本生成/输出解析/假子进程含超时强杀/环境检测降级/缓存隔离/API 生命周期/pair 模式）；真实冒烟默认 skip（COF_TEST_PSI4_SMOKE=1 且环境已装才跑）
- **真实冒烟（苯 + 苯酚 pair，UFF 初猜 + xTB 预优化 + def2-SVP 单点）**：E_bind(cp) = **-0.199 kcal/mol**（未校正 -0.231），gap 10.67 eV，偶极矩 1.33 D，fchk 写出正常，全程 96 s
- 全套测试：**765 passed**（736 + 29），tsc -b 与 vite build 通过

### 遗留

- 冒烟仅验证管线连通：单点 + 初猜几何的结合能偏浅（文献 CBS 约 -4~-5 kcal/mol），方案步骤 5「本机基准验证（3–5 个文献体系）」未做，建议下一步做
- 复合物 gap 10.67 eV 偏大，待基准时一起复核（怀疑是初猜几何未优化到位所致，非代码错误）
- psi4 侧几何优化（optimize=True 默认开）尚未做真实长时验证；def2-TZVP 档未加
- MACE-OFF23 可选档二期未做
- 13 条文献 DOI 待人工（同前）；建议发 v1.1.1 热修复版（同前，用户拍板）
