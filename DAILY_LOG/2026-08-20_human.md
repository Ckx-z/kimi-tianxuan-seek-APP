# 日报 2026-08-20（人版）

## 一句话总结

**v0.3.0 正式发布**——实验记录可以随便改和补复盘了、GraphRAG 迭代真正用上了你自己的实验数据、软件里多了一个会查资料会调模型的「科研助手」，而且从你机器上的 v0.2.5 开始，**以后更新点一下就行，不用再来回装安装包了**。

## 今日成果

1. **实验记录终于能改了**：以前录完只能改时间线，现在所有字段都能编辑，还新增「自我总结」「我认为的失误」两栏——写进去的复盘会一并喂给 AI，下次迭代方案时它会参考你的教训。收藏页里也能直接展开看每组单体的实验记录了，窗口缩放不再难看。
2. **GraphRAG 闭环了**：迭代建议现在真正基于你录入的实验过程和你的总结失误，不再是降级版的演示。评估过 LangChain 等替代方案，结论是 GraphRAG 依然最适合我们，报告放在 docs 里。
3. **新模块「科研助手」上线**：软件里多了一个 AI 助手（ming 人格），你问它问题它会自己去调预测模型、查 GraphRAG 图谱、翻你的实验记录，然后综合回答。已用你配的 deepseek-v4-flash 真跑通过——一次提问它自动调了三轮工具，还会主动提醒"这组单体超出模型适用范围"。方案迭代页也加了"去问问科研助手"的入口。
4. **v0.3.0 发布**：安装包和 zip 都传到 GitHub Release 了。中间命令行上传 3 次超时（网络太慢），最后是你浏览器拖文件上传成功的，我这边补完了自动更新的配置文件。**注意：exe 被浏览器自动改成了英文名 `cof-film-recommend-setup-0.3.0.exe`，这是自动更新能认出它的关键，别改名。**

## 验证

- 自动化测试 **420 项全过**；安装包做了全套自证（新功能都在包内、运行时真实可用）。

## ⚠️ 傍晚的小惊吓：更新后"还是旧界面"

你更新后看到的界面和 v0.2.5 一模一样——查了一个小时，**安装包是干净的，真凶是软件的页面缓存**：软件内嵌的浏览器内核把旧页面存在了本地，升级后地址没变，它图省事直接给你看了存货，根本没去取新页面。

**已处理**：帮你清掉了页面缓存（实验记录、收藏、LLM 配置都原样在），你重开软件就能看到科研助手了；并且**从代码上根治**——以后每个版本升级，页面都会强制取最新，这个问题对所有用户永久免疫。

**教训已写进发版纪律**（日报 AI 版有完整清单）：以后每次发新版，除了构建自证，还要实测"已装应用吐的是不是新页面"，不会再让你白装一次。

## 明天一件事（重要）

重新打开软件确认三处：左侧栏有「科研助手」、设置页版本号 0.3.0、实验记录能编辑且有「自我总结/失误」栏。都正常的话，自动更新端到端验收正式通过 🎉

## 阻塞点

无硬阻塞。老提醒：那个 GitHub PAT 用完了记得删掉（Settings → Developer settings → Personal access tokens）。
