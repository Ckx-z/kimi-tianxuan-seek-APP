# 日报 2026-09-04（AI 版）

## 今日工作：科研助手深度研究 P0+P1 实施（联网/学术检索 + 引用核验 + plan-execute-critic-report 循环）

依据 docs/科研助手深度研究方案.md（2026-09-02 讨论稿）开始实施，本日完成
P0 与 P1 全部内容并提交（af70a0b），**未发版**（构建/发版待用户确认）。

### P0：联网与学术检索工具族 + 设置页配置

1. 新工具（全部遵守 {text, details, is_error} 契约）：
   - `web_search`：tavily / serper provider 抽象；未配置（开关关/无 key）时
     **从工具表整体缺席**（registry.list_tool_schemas / describe_tools 按
     llm_client.web_search_available() 动态裁剪）；401/402/403 与超时转
     中文 is_error。
   - `academic_search`：arXiv（Atom API）/ PubMed（E-utilities）/
     Semantic Scholar / Crossref（复用 src/literature/crossref）四源聚合，
     免费直连；数据源名归一化（大写/空格/别名容错，真机发现 LLM 传
     "Semantic Scholar" 被白拒后修复）；Crossref 过滤 SI 条目（DOI 带
     .s00x 后缀或标题含 Supporting）。
   - `fetch_page`：trafilatura 正文提取（未安装降级标签剥离），
     **公网白名单校验**（拒绝内网/环回/保留地址、file://、>2MB），
     超时/4xx 转中文 is_error。
   - 补齐 openhanako V2 遗留三工具：`get_monomer_props` / `cas_resolve` /
     `lookup_paper_doi`（本机文献库优先 + Crossref 兜底）。
2. 配置：设置页「联网搜索」卡（开关/provider/key，默认关）；`src/llm/client.py`
   新增 get/save_search_settings（与 LLM 配置同文件读改写互不覆盖，key 只回
   显掩码）；`GET/PUT /api/llm/search-settings` + env-status 增 search 口径。

### P0：引用核验器（防幻觉底线）

- `src/assistant/verify.py`：回答中每个 CAS/DOI/URL 必须能回溯到本轮工具
  结果（doi.org 前缀/大小写宽容归一；DOI 只匹配合法字符集，防中文粘连）。
- `loop.py` 双路径接入 `_emit_verified`：违规 → `critic_note` SSE 事件 →
  打回重写一轮（不调工具）→ 重写仍违规/失败 → 原回答 + 「⚠️ 未经系统
  核实，请谨慎采信」显式警示（诚实降级不静默）。

### P1：深度研究循环 + 报告

- `src/assistant/research.py`：plan（LLM 拆 2–5 子问题）→ 逐子问题检索
  （只读工具白名单，预算 3 轮/步）→ critic 批判轮（证据缺口补搜 ≤2 个）
  → writer 生成带引用 Markdown 报告（GB/T 口径 + DOI，引用核验复用 loop）
  → 落盘 `user_data_root/research/report_<id>.json`。
- 端点：`POST /api/assistant/research`（SSE：plan/step_start/step_done/
  tool_*/critic_note/token/report/done/error）+ 报告列表/详情/删除/
  `export.docx`（md→Word，参考文献带 DOI）。
- 前端：助手页「深度研究」模式开关（研究中禁用切换）+ 研究计划卡（步骤
  逐步打勾）+ 报告生成卡（一键下载 Word）+ 页头「研究报告」弹窗（列表/
  下载/二次确认删除）；ToolEventCard 新增 critic_note 与 research_report
  渲染、6 个新工具中文标签；persona/domain_rules.md 增「联网检索纪律」。

### 验证

- 新增测试 +37：tests/test_web_search_tools.py（16：配置读写、provider 双路、
  HTTP 错误、四源检索 mock、fetch 安全校验、registry 动态裁剪、源归一化）、
  tests/test_citation_verify.py（10：refs 收集、DOI/URL 宽容归一、loop 打回
  重写/警示三态）、tests/test_research_loop.py（11：计划容错、步骤执行、
  报告 CRUD/docx、全流程、批判补搜）。
- 全套 pytest：**883 passed, 1 skipped**；tsc + vite build 通过。
- **真机冒烟（dev 后端 8001 + 真实 DeepSeek + 真实学术 API）**：
  academic_search 四源聚合返回真实 DOI（发现并修复 SI 条目混入）；
  fetch_page 公网提取/内网拒绝；search-settings GET/PUT 与 env-status；
  报告 CRUD + docx（PK zip 36KB）；**深度研究全链路**：5 步计划 → 逐步
  academic_search（Crossref/SemanticScholar/arXiv/PubMed）→ 批判轮自动
  补搜 2 个缺口 → 报告生成（GBK 控制台导致验证脚本中断，报告落盘经
  API 冒烟补齐验证）。

### 遗留 / 下一步

- **发版待用户确认**：本日改动在 master（af70a0b）未打版本号；确认后按
  发版纪律走 v1.6.0（版本同步 → 构建 → 打包自证 → tag → Release）。
- web_search 真实联调需用户提供 Tavily/Serper key（设置页配置即可）；
  学术四源已真机可用。
- 打包注意：trafilatura 已装进 E:\ANACONDA（PyInstaller 会自动收集），
  打包自证时需验证 fetch_page 在 frozen 环境可用（依赖兜底已实现）。

## 追加：P2 实施完成（提交 7f06361）

按方案文档 §六 P2 全部落地：

1. **SKILLS 机制**：`src/assistant/skills.py`（YAML-lite frontmatter 解析；
   用户覆盖目录 `user_app_root/skills/*.md` 优先于内置 `persona/skills/`，
   frozen 下改 md 即生效、免重装；开关写 skills.local.json）；内置三技能
   （方案迭代方法论 / 文献检索 SOP / 失败归因框架）；persona 层序注入
   「技能（方法论）」段；端点 GET /skills + PUT /skills/{name}；设置页
   助手记忆卡新增「助手技能」开关区。
2. **按单体组记忆**：`agent_memory/<pair_key>.md`（双 CAS 优先为 key，
   否则 SMILES 摘要；label 头 + 日期条目，60 条上限）；compile_session
   同批要点镜像落组文件（一次 LLM 调用两处落盘）；injection_block(session)
   优先注入该组专属记忆再注全局；端点 pair-memories 列表/读取/删除
   （key 正则防穿越）；设置页「按单体组记忆」列表（查看/一键清除）。
3. **新失误记录主动提醒**：今天 mtime 更新且 mistakes 非空的正式记录 →
   nudge（kind=new_mistake，与连续失败提醒合并、同 dismiss 键）；NudgeBar
   分 kind 渲染，「深度研究」按钮 → 跳转助手页自动开启研究模式并预填
   失败归因问题（TransferState.researchMode）。

验证：tests/test_assistant_p2.py 11 用例；**全套 pytest 895 passed,
1 skipped**；tsc + vite build 通过；dev 后端真机冒烟（skills 列表/开关
toggle/pair-memories 端点）。测试过程中发现并修复一处测试污染（nudge
dismiss 状态未隔离到 tmp，已清理 dev 数据并加固 fixture）。

至此 docs/科研助手深度研究方案.md 的 P0/P1/P2 全部实施完毕，**发版
（v1.6.0）待用户确认**。

## 追加：v1.6.0 发版 ✅（发版纪律全流程）

1. README 更新（版本 + 核心能力增深度研究/联网检索/引用核验/技能/按组
   记忆说明 + 致谢版本号）；三处版本号同步 1.6.0，提交 `e96aa09`
   （`release: v1.6.0 版本号`）。
2. 构建链：vite ✅ → PyInstaller（无 --clean，247s）✅ → electron-builder
   （directories.output 临时改 C:/cof-build/release 绕 EPERM，构建后拷回
   并还原）✅，安装包 **447.7MB**。
3. 打包自证（COF_DATA_DIR 隔离到 %TEMP%，frozen 后端 18778）：
   health version=1.6.0 ✅ / citations 9 方法 ✅ / **skills 3 个内置技能
   （frozen 下 persona/skills datas 就位）**✅ / pair-memories ✅ /
   search-settings（默认关）✅；PYZ 递归清单确认 **trafilatura + courlan +
   htmldate + assistant.research/skills/verify/tools.* 全部入包**（fetch_page
   frozen 依赖链完整，工具自身带无依赖降级兜底）。
4. zip（COF-Assistant-Setup-1.6.0-win-x64.zip，447.6MB；首次压缩被安全
   软件占用 exe，重试成功）→ tag v1.6.0 指向 e96aa09 → 推送 master
   （750dbae..e96aa09）+ tag。
5. GitHub Release v1.6.0（id 382552723）创建，三资产上传：exe
   （469,419,563）→ zip（469,355,629）→ latest.yml 最后。
6. **本机安装升级 v1.5.4 → v1.6.0**：首次静默安装 exit 1223（运行中的
   旧版进程未及时退出、文件占用导致覆盖取消）→ 先 taskkill 全部
   COF科研助手/cof-backend 进程再装 → exit 0，exe FileVersion=1.6.0；
   启动后实机验证：`.running-instance`/`.app-version`=1.6.0、health
   version=1.6.0、skills 3 技能、env-status.search=disabled（未配 key，
   符合默认关口径）。经验：升级安装前先手动杀旧进程，不要只依赖安装器
   customInit 的 taskkill（异步杀进程可能赶不上文件覆盖）。

## 追加：成膜打分后端审查 + GNN 优化方案 + 阶段一实施（v1.6.1 候选）

1. **审查报告**（docs/成膜打分后端审查与GNN优化方案.md，提交 a545b7d）：
   全链路诊断结论——苯甲醛/苯胺高分 = ①训练数据污染（苯甲醛标注成膜
   6 条/53%）+ 硬负样本缺失（根因）②per-site 归一化把「苯环密度」学成
   正信号 ③max 融合放大 ④OOD 无交联度红线。含 GNN v5.4 改进方案
   （全局虚拟节点 + 组合级加权 Focal + Isotonic 校准）与三阶段路线。
2. **阶段一实施**（提交 3295e8a，未改模型训练）：
   - ood.py 新增 networkability 检测（成网必要条件：醛≥2 且胺≥2，或
     任一侧≥3；不满足 → warning）；
   - 新 src/predictor/fusion.py 统一主分口径（api/deps 与 gradio_app
     共用）：红线组合 score=min(0.25, 0.5·min(tree,gnn))；组合未见
     训练集（pair_pool.py 惰性加载 CSV）时 GNN ×0.8 收缩；分歧 >0.25
     仅标注不改分（保排序）；score_policy → max_tree_gnn_redline，
     score_flags 全链路透出（payload/日志/收藏快照/前端）。
   - 真机实测：苯甲醛+苯胺 0.647→**0.224**、苯甲醛+乙二胺 0.778→
     **0.194**、苯甲醛+对苯二胺 0.694→**0.224**；Tp+对苯二胺 0.821
     不变（回归护栏）✓。
   - 测试：+12 用例；全套 pytest **907 passed, 1 skipped**；tsc + vite
     通过。修复过程中发现 can_network 位于 networkability.details 的
     读取口径自伤 bug（fusion/gradio/测试夹具三处同步修正）。
3. **遗留**：阶段三（金标准集月度评估）未实施；v1.6.1 发版待用户确认。

## 追加：成膜打分阶段二实施（v1.6.1 候选）

### 数据层

- `scripts/audit_film_labels.py`：审计「化学上不可能成膜却标注成膜」的
  噪声标签——实测 **613 行**（10% 样本；其中 is_film=1 的 200 行，如
  苯甲醛 6 条成膜率 53%）→ --fix 改写为 0；另 798 行合法负样本。
- `scripts/make_hard_negatives.py`：规则硬负样本（单官能×全库、全库×
  单官能，确定性采样上限 600）→ **v6 训练集 6801 行**（is_film 均值
  0.158，79% 负样本）。

### 树模型 tree_v5（提交 54dc9e5）

- 特征：descriptors.py 新增 `pair_min_functionality / pair_max_functionality /
  pair_can_network`（精确官能度 SMARTS，排除酮/酰胺误计）。
- `scripts/train_tree_v5.py`：v6 数据同协议训练（GroupShuffleSplit 按醛
  分组 5 折对比）——**v5 MAE 0.0260 vs v4 基线 0.0306（-15%），PR-AUC
  持平（0.9895 ≈ 0.9899）**；5 种子 bagging → models/tree_v5_ens.pkl +
  tree_v5_noTE_ens.pkl。
- **护栏评估**（scripts/eval_tree_v5_guard.py）：不可成网组合 n=1885——
  v4 均值 0.489（55% ≥0.5），**v5 均值 0.000（0 个 ≥0.5）**；金标准
  Tp+Pa 0.821→0.868、苯甲醛+乙二胺 0.778→0.008、苯甲醛+苯胺 0.447→0.000。
- 路由切换 tree_v5 双臂（routing.py 路径与文案、测试断言同步）。

### GNN v5.4（旧项目提交 a9ae30d）

- featurizer 全局虚拟节点（索引 0 连全图，6 维单体级成网特征，
  in_dim 37→43）；train_v5_4.py（脏数据过滤 + 多进程并行建图——修复
  病态大分子导致 90 分钟建图不完成的坑；不可成网配对 quality_weight
  ×2.0；验证集 Isotonic 校准）；predict_pair.py 支持 use_global +
  自动加载 calibrator 输出校准分。
- 3 折 CV：**PR-AUC 0.7253 ± 0.0858**（fold 0.604/0.793/0.779）；
  校准/保存步骤修复 forward 调用签名后重跑中。

### 验证（截至树模型部分）

- 实机（dev 8001，GNN 未就绪时自动降级）：苯甲醛+苯胺 tree=0.000、
  苯甲醛+乙二胺 0.008、Tp+对苯二胺 0.868，model=tree_v5_ens ✓。
- 全套 pytest **907 passed, 1 skipped**（tree_v5 切换后回归通过）。
