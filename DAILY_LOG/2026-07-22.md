# 日报 2026-07-22（AI 版）

## 今日工作：阶段 21 — P4a/b 实验记录修复 + LLM 基座 + 性质卡 + 方案卡模板系统

### 背景

用户实测 App 后提出四组改进：①实验记录模块四项问题（记录未按收藏过滤/实验编号藏备注/切换收藏表单不刷新/缺双溶剂与洗脱剂字段）；②查询打分模块接 LLM（单体性质展示 + 方案卡模板可自定义上传 docx 自动提取）；③全前端预留 LLM API 接口 + 用户测试数据不入 git；④页⑤自然语言 RAG 迭代暂缓，等 minimax GraphRAG 成熟。

### 已完成（四个并行任务 + 统一收尾）

1. **任务 A：实验记录后端（契约收紧，D30-1）**
   - `experiment_no` 独立必填字段；`conditions` 标准化九键（solvent_1/solvent_2/eluent/modulator/catalyst/temperature_c/time_days/vessel/addition_order）；旧 `solvent` 键自动映射 `solvent_1`

2. **任务 B：LLM 基座 `src/llm/`（新增）**
   - 三级配置链：`data/llm_config.local.json` → `COF_LLM_*` 环境变量 → minimax secrets 种子（longcat）
   - 磁盘缓存、失败静默降级（未配置/调用失败返回 None，不炸主流程）、`test_connection()` 连通性自检

3. **任务 C：单体性质卡 + 方案卡模板系统**
   - `src/recommend/monomer_props.py`：RDKit 事实（MW/XlogP/TPSA/HBD/HBA/芳环/F 数/可旋转键）+ LLM 中文解读（溶解性/COF 角色/毒性安全/侯老师方案操作提示），带缓存
   - `src/recommend/plan_templates.py`：内置侯老师 v3.9 模板为默认；用户上传 docx → LLM 自动提取 conditions/steps/checklist 存为用户模板

4. **任务 D：前端五页接入**
   - 页④实验记录新表单（九键条件 + 编号必填 + 按收藏过滤 + 切换刷新）；页①性质卡展示；设置页 LLM 配置（含连通性测试按钮）

5. **统一收尾：longcat 推理模型适配（本 session 实测发现并修复）**
   - **根因**：LongCat-2.0 是推理模型——小 max_tokens 全被 `reasoning_content` 消耗，`message.content` 缺失或空串（KeyError: 'content'）；长输出超 30s 读超时
   - **修复**：`_http_chat` 解析容错（顶层 content 回退/列表分片拼接/空串显式报错）；max_tokens 上调（性质卡 3000/模板提取 4000/默认 2000/连通性 256）；TIMEOUT 30s→120s
   - **真实冒烟**：`test_connection()` → (True, "连接成功（LongCat-2.0）：pong")；TAPT 问答返回正确中文定义 ✓
   - **性质卡端到端**：苯胺 → narrative_source=llm，输出覆盖溶解性/席夫碱角色/毒性防护/界面法操作提示 ✓
   - **docx 模板提取端到端**：文献示例 docx → conditions/steps/checklist 完整 JSON ✓

6. **用户数据 gitignore**：`data/favorites/`、`data/experiment_records/`、`data/llm_config.local.json` 等；内置模板与 records/example.json 仍入库

### 验证

- `E:\ANACONDA\python.exe -m pytest tests/ -q`：**299 passed**（77s），无回归
- 真实 LLM 三处冒烟全部通过（见上）

### 遗留 / 明日待办

- [ ] 用户重启 App 实测：页④新表单（九键+编号必填+过滤刷新）、页①性质卡、设置页 LLM 配置
- [ ] 页⑤暂缓：等 minimax GraphRAG 深检索闭环后集中处理（用户决策）
- [ ] shiyandiedai 仓库手动归档（用户自己操作：Settings → Danger Zone → Archive）
- [ ] 模型侧遗留（此前积压）：双留出 fold3 诊断、概率校准、双模型策略 D21 复盘点

---

## 下午追加：阶段 22 — 页⑤ GraphRAG 迭代引擎全量对接

### 上午铺垫
- 页④记录删除（两段确认+收藏解挂）+ 放大查看详情卡；FastAPI 地基（api/ 六路由，未来 React/Tauri 独立前端对接层，主分数口径与 App 一致）

### 体检（3 路并行探测）
- 全功能回归：326 测试全过，五页+API 16 端点+跨模块契约实测通过；发现 5 bug（1 中：游离建议全部无法采纳）
- GraphRAG 检索实测 6 问：条件查找型已够用；**故障诊断型双链失效**（nl2graph 太脆/reaction time 字段全缺/core 索引仅 10 文档）
- 差距分析：failure 专家语料未接入、evidence_refs 无白名单（LLM 可编造引用）、monomer_pool 88 行类型矛盾

### 波次 1 修复包
- 游离建议可采纳（payload 注入单体 + rec 引用白名单+模糊纠正）；采纳幂等（重复采纳返回同一 plan）
- API 三修：props 非法 SMILES 400 不烧 LLM；OOD=out 分量全置 null；实验编号重复警告字段

### 波次 2 全量对接
- **证据链**：failure_criteria/playbook 语料按 failure_class/实验号注入（第三路取证）；evidence_refs 白名单校验（编造引用剔除+unverified_refs 记录）；置信度 high/medium/low（0 证据强制 low）；embedding rerank 点亮验证（HAS_EMBED=True）；多跳 BFS 接入（反应→溶剂→同类反应→文献路径文本）
- **检索底座**：nl2graph 中文解析重写（温度 120度/溶剂 20 词/催化剂 14 词/调制剂/用量/后处理 workup intent/失败诊断 failure_diagnosis intent）；monomer_pool RDKit 对账清洗（修正 253 行/剔除 15N 1 行/标记 review 175 行，审计报告 minimax/reports/）；graph.pkl 重建（9065 节点/23898 边，备份 .bak）；importance 加权排序
- **6 问复测**：全部非零结果；界面法甲苯120度乙酸 top 命中 score 14.3（甲苯+120°C+乙酸组合精准）；失败诊断问 top 全为 powder 反面教材（符合意图）

### 验证
- 测试 **329 passed**；端到端真实冒烟：fav_20260722_001 问「反应时间不够膜不连续怎么调」→ 2 条建议带置信度（high/medium）+白名单内证据+编造引用被剔除记录

### 遗留
- [ ] reaction 节点 time 字段 100% 缺失（建图抽取缺口，随下次大规模重建处理）
- [ ] 缓做项（触发条件钉死）：社区文字摘要/文献 year 权重/llm_config 任务路由/建议-结果回环（≥20 条闭环数据后）/CAS 批量反查
- [ ] 用户重启 App 实测页⑤全流程（提问→建议→采纳→方案 vN）

---

## 傍晚追加：完整度审计 + 前端迁移 Phase 0

### 四路并行审计（结论：项目达标，可进入前端迁移）
- **模型与数据** ✅：双臂路由树+GNN+OOD 三级全链路实测通过；训练/评估资产完整
- **App 功能** ✅：36 项逐条核对全过（H 组排除、论文三件套已落实）
- **GraphRAG** ✅ 基本达标：三问端到端复测，白名单剔除/降级链/key 失效降级全部按预期
- **工程安全** ✅ 无密钥泄漏；大文件策略矛盾已对齐

### 审计必修（已修复并推送 016e14c）
- App 三修：重复实验编号警告透传；页⑤ subprocess 超时 180→300s；页③删除收藏两段确认
- 契约文档同步（Schema 3 补 batch/confidence/unverified_refs/literature）；README 大更新（阶段 22/FastAPI/三解释器/332 测试）；requirements 补齐；修复 python3.12 环境包冲突

### Phase 0（已推送 27d6415，340 测试全过）
- `docs/PREDICT_CONTRACT.md`：predict() 权威契约（React 开发免读源码）
- `api/routers/iterate.py`：页⑤迭代逻辑沉入 API（suggestions/suggest/adopt/plans 四端点）+ 性质卡批量接口——**App 全部功能在 API 层无缺口，后端迁移准备 100% 就绪**

### 明日待办
- [ ] **用户实测当前 App**（重点：页⑤建议质量——真实失败记录回填 failure_class 后语料是否生效；页③两段确认删除；重复编号警告）
- [ ] 实测通过后启动 **Phase 1：React 五页重写**（Vite+React+TS+Tailwind+shadcn/ui，2-3 天，Kimi 设计语言；建议卡片真按钮/方案卡 Modal/异步轮询）
- [ ] Phase 1 后：Phase 2 双版联调对照（1 天）→ Phase 3 Tauri 打包 .exe（1 天）

### 遗留（不阻塞）
- [ ] failure 语料实战生效依赖真实失败记录填 failure_class（App 表单暂无此字段，下次实验记录时人工补 notes 或后续加字段）
- [ ] reaction 节点 time 字段全缺（随下次大规模建图处理）；缓做项触发条件见阶段 22 节
- [ ] minimax/bridge/test_integration.py pytest 误收集（`def test(name)` 缺 fixture），列入后续必修
- [ ] shiyandiedai 仓库手动归档（用户自己操作）
