# 日报 2026-09-06（AI 版）

## 今日工作：需求一 GNN 成膜打分修正机制 + v1.8.0 发版 ✅

按 docs/成膜打分GNN修正与科研助手文献图谱方案.md（用户已确认 4 点：不引
IndexedDB / 双通道分发 / 拆版 v1.8.0 / pilot 逐条复核）实施需求一并发布
**v1.8.0**。

### 反馈闭环（三通道 + 校验）

1. `src/predictor/gnn_feedback.py`：反馈库（jsonl）三通道——打分纠错 /
   文献 PDF（PyMuPDF 全文 → LLM 结构化提取 → RDKit 正则兜底，候选预览
   逐条确认）/ 实验 CSV；confirm 时 can_network 校验 + 基础集（v6）去重
   + 同组合标签冲突检测（status=conflict）。
2. 路由 `/api/gnn/*`：feedback CRUD / confirm(-batch) / reject /
   import-pdf / import-table / retrain job（启动-进度-取消）/ env /
   versions / activate / compare；api/deps 与 FilmPredictor 透出
   `gnn_model_version`，打分结果卡显示 GNN 版本。

### 微调管线（gnn_training/，模型代码单一来源 gnn_runtime/src）

1. `finetune.py`：v5.4 权重起训 → 冻结 input_proj + 前 k 层（默认 2）→
   反馈正/负样本加权（5×/3×，--feedback-pos-w 可调）→ Focal + 早停 →
   **分层校准样本**（反馈行 + 基础集按标签分层 10%）拟合 Isotonic →
   progress.jsonl 阶段进度 → v5_model.pt + calibrator.pkl + retrain_meta。
2. `guard_eval.py` 验证闸门：反馈对（正样本校准后 ≥0.5 / 负样本 ≤0.25）
   + 39 对金标准四项不回退；`run_job.py` 编排（微调 → 闸门 → registry
   更新，不达标 rejected 不激活）；`compare_versions.py` 新旧逐对对比。
3. 版本 registry（user_data_root/gnn_models/ + 随包 models/gnn_feedback
   双通道）：active 指针 + 状态；`gnn_model._resolve_runtime` 优先级 =
   COF_GNN_CHECKPOINT 钩子 → registry 激活版本 → 包内 v5.4 → 旧项目。

### 前端

1. 打分结果卡「反馈打分不合理」弹窗（三档修正 + 理由）。
2. 设置页「GNN 模型演进」面板：环境状态 / 反馈队列（确认-拒绝-改-删）/
   CSV·PDF 导入（候选逐条确认档位）/ 重训按钮 + 五阶段进度 + 日志尾 +
   取消 / 版本列表（激活-回退-对比表格：反馈对逐对 + 金标准四指标）。

### pilot：TFPT+B5（用户确认 label=1.0）→ 首轮微调

1. `scripts/build_pilot_feedback.py` 从实验记录（实验ABCDEF.docx）提取：
   TFPT+B5，证据 D3-1/D3-2「壁上形成连续、光滑的薄膜」——用户逐条复核
   确认后入库（fb_589fef12569b）。
2. **效果**：TFPT+B5 GNN 校准分 **0.000 → 0.854**（原始 0.013 → 0.80，
   MC std 0.036）；39 对金标准 Spearman 0.8628 → 0.8658，其余不回退；
   tree 全程零改动（基准对照成立）。
3. 版本 gnn_v5.5_20260906_094822 激活并**随包分发**（models/gnn_feedback/，
   打包 registry active 指向它）——双通道落地。

### pilot 真机踩坑修复（5 个，全部带回归测试）

1. DETACHED+NO_WINDOW 编排器拉起控制台子进程被 0xC000013A 秒杀 →
   finetune 子进程显式加同款 creationflags；
2. 反馈组合已在基础集（v6 含 TFPT+BD-CF3）时被去重丢弃 → 改为「已在
   基础集也加入 feedback_keys 加权」；反馈行 <5 条时全留训练切分；
3. 校准器单用早停验证集（负样本占优）把中等原始分单调压 0 → 分层校准
   样本；
4. guard/compare 子进程输出只按 UTF-8 解码 → Windows GBK 中文乱码解析
   失败 → 双编码回退；
5. **registry 激活版本被 DEFAULT_CHECKPOINT 覆盖**（版本名显示新版本、
   实际权重仍是 v5.4）→ 仅显式传入的 checkpoint 才算覆盖；
6. 闸门容差实测：同一 v5.4 两次运行 c_max 0.0479 ↔ 0.0615（树 3D 构象
   噪声 ±0.014）→ a_min/c_max 容差 0.03、聚合指标 0.01。

### 验证与发版

- 测试 +14（test_gnn_feedback 13 + 回归 1），全套 **945 passed, 1 skipped**；
  tsc + vite 通过。
- frozen 自证（COF_DATA_DIR 隔离）：health 1.8.0、**TFPT+B5 gnn 0.8545
  （gnn_model_version=gnn_v5.5_20260906_094822）**、Tp+Pa 0.7228 金标准
  不降、苯甲醛+苯胺红线 0.0、反馈 CRUD、随包 registry 激活正确；修复
  frozen 惰性 import `predictor.fusion` 双命名空间 hiddenimports（真机
  500 → 补 12 条 hiddenimports 重建）。
- 版本同步 1.8.0 → PyInstaller → electron-builder（456.2MB）→ zip →
  tag v1.8.0（595c7ac）→ 推送 → GitHub Release v1.8.0（id 383447138）
  三资产上传（exe/zip/latest.yml 最后）。
- 本机安装：UAC 两次取消 → per-user 安装验证（FileVersion 1.8.0、
  health 1.8.0、TFPT+B5 score 1.0 / gnn 0.8545）。

## 追加：E:\COFapp 提权升级收尾 ✅

用户在场批准 UAC 后完成：管理员模式运行 v1.8.0 安装包（exit 0）→
E:\COFapp exe FileVersion 1.7.0→**1.8.0**、HKLM 注册表 DisplayVersion
同步 1.8.0、per-user 验证副本被安装器自动清理（目录不存在）→ 从
E:\COFapp 启动实测：.app-version=1.8.0、health 1.8.0、**TFPT+B5
score=1.0（tree 1.0 / gnn 0.7875，gnn_model_version=
gnn_v5.5_20260906_094822）**、registry active 指向修正版本。
**本机仅剩 E:\COFapp 单份 v1.8.0，无遗留。**

### 遗留 / 下一步

- E:\COFapp 提权升级（用户手动，一步）。
- 后续反馈迭代：更多 TFPT/B5 系实验记录与文献反馈可在「设置 → GNN 模型
  演进」继续导入并重训（版本 gnn_v5.6+），形成文献对标的多轮迭代循环。

## 追加：v1.9.0 科研知识库统一 ✅（讨论两轮定稿 + 全栈实施 + 发版）

### 前置

用户提三件事：①深度研究对话记录丢失（只剩报告）②文献图谱与文献录入
应统一为「科研知识库实时扩充」③设置页配独立 LLM 解析文献结构化信息。
先讨论设计方案（两轮：统一入口/独立 LLM 卡/入图范围/embedding/按需补
解析/多组实验分组坑/分离选择性与 DFT 表征/负样本），产出
docs/科研知识库文献结构化提取与入图方案.md，用户确认后开工。

### ① 深度研究持久化修复（已随本版发布）

根因：研究模式下提问与研究正文只渲染本地、后端仅落一条「深度研究完成」
标记消息 → 刷新/切会话内容全丢。修复：用户提问 + 报告全文 + 检索过程
（tool_events）全部落会话，刷新不丢；侧栏消息计数同步刷新。

### ② 科研知识库（P0-P3）

- **条目库** `src/literature/knowledge.py`：group_id 实验组分组（回应早期
  多组实验混杂的坑）、8 类 kind、15 类表征（含分离选择性 separation_
  selectivity 与 dft）、metrics 数值化、负样本 film_label=0、原子入库、
  跨文献数值范围检索（metric=PLQY&min=20）。
- **文献解析 LLM** `src/literature/llm_extract.py`：独立设置独立 key
  （设置页新卡），prompt 强制先识别几组实验逐组输出、evidence 必填、
  失败重试、未配置降级正则扫描（修复令牌吞并外侧括号导致解析失败的坑，
  gnn_feedback 同款修复）。
- **入图** `src/literature/graph_ingest.py`：LIT-<paper>-<group> 侧车图
  reaction 节点（source=literature）+ M-/S-/C-/O- 节点与边（对齐
  user_graph 方案），组级幂等同步，更新/删除联动撤图；检索侧无需接线
  （助手 query_graphrag 与方案迭代的 load_graph 已 merge 侧车图）。
- **embedding 三态** `src/literature/embedding.py` + worker：off（默认）/
  本地（dphuanjing + bge-m3，**Qwen3-Embedding 因 transformers≥4.51
  不支持 py3.8 而弃用**；HF 镜像 + 禁 symlink 下载到 E:\cof_models\
  bge-m3，约 2.2GB）/ 在线 DashScope；向量入库 + 线性内积检索；
  scripts/install_lit_embedding.bat（bat 中文在 cmd 下字节错位的坑 → 纯
  ASCII 重写）。
- 路由：parse（PDF/全文）/ entries CRUD / 数值检索 / vector-search /
  to-gnn-feedback（film_outcome→GNN 反馈）/ to-dft（预填）/ llm-settings
  / embedding-status。
- 前端：Mine 页合并为统一「科研知识库」（文献列表卡片 + 分组条目 +
  图谱 tab（FiguresPanel fixedPaperId 模式）+ 补解析四步流 + 条目联动
  打分/DFT/GNN反馈/JSON 编辑/删除）；设置页「文献解析 LLM」卡
  （启用/base_url/key/model + embedding 三态 + 测试连接）。
- 打包：worker 以 datas 携带（dphuanjing 按文件执行）+ literature.* 与
  networkx hiddenimports；frozen 自证含本地 embedding 实跑（embedded=1）。

### 验证与发版

- 测试 +18（test_literature_knowledge），全套 **963 passed, 1 skipped**；
  tsc + vite 通过。
- dev 真机全链路：解析（降级扫描）→ 3 条目入库（graph_synced=1、
  embedded=3 本地 bge-m3）→ 向量检索命中（cos 0.64）→ 数值检索 →
  侧车图 LIT-1-V1 节点 → **助手 query_graphrag 检索命中文献条目**。
- 版本同步 1.9.0 → PyInstaller → electron-builder（456.2MB，zip 首压被
  安全软件占用重试成功）→ frozen 自证（health 1.9.0 + 本地 embedding
  实跑）→ tag v1.9.0（56bb4e0）→ 推送 → GitHub Release v1.9.0
  （id 383518217）三资产上传（latest.yml 首传 DNS 抖动失败，复核资产
  清单后补传成功）。
- 本机升级：UAC 批准 → E:\COFapp **1.9.0**（health/papers/图谱端点
  正常）；经 API 配置 embedding=local（手动写文件踩 BOM 坑——PowerShell
  Set-Content UTF8 带 BOM，Python json.loads 不认；改走 API PUT 正确落盘）
  → 安装版 embedding-status local available。

### 遗留 / 下一步

- 文献解析 LLM 的 key 由用户自行在设置页配置（未配时降级正则扫描，全
  功能可用）。
- 建议的后续迭代：①条目检索接入助手（query_graphrag 已可命中，可加
  数值检索工具）；②文献图谱与 DFT 对照回写 score_note。

## 追加：v1.9.1 修复科研知识库「看不到任何东西」（Release 383524841）

用户反馈：老文献明明有结构化信息（作者/期刊/摘要），科研知识库却只显示
标题、正文空白。根因：知识库卡片漏了设计文档的「原文信息」页签，只展示
新提取条目（老文献尚未提取 → 空）。修复：

- 新端点 `GET /api/literature/papers/{paper_id}`（完整元数据：作者/期刊/
  年份/DOI 链接/摘要/来源）；
- 卡片新增「原文信息」页签（默认选中），老文献点开即见结构化元数据；
  卡片头同步显示期刊/年份/DOI；条目页空态提示改为「原文元数据见原文
  信息页」；
- 测试 +1；frozen 自证 paper/1 元数据完整返回（含 DOI 链接）。
- 发版：v1.9.1（tag fb97214，Release id 383524841，三资产上传）→ 本机
  UAC 批准升级 E:\COFapp → **1.9.1**（health/paper detail 实测通过）。

### 过程记录：构建链踩坑（已改正）

- 后台 job 把 PyInstaller 放在 webapp 工作目录执行（spec 路径错）→
  exit 1 且 electron-builder 装进了旧后端——发现后从项目根重跑
  PyInstaller + 重建 electron-builder，zip 前等待安全软件释放文件句柄。

## 追加：v1.9.2 图谱历史条目导入科研知识库（Release 383532273）

用户反馈：知识库条目数为零——图谱里有大量历史结构化信息却看不到。
新增 `POST /api/literature/entries/import-from-graph`：直读随包
`graph_v2.pkl`（不带侧车）→ 每条 reaction 节点生成一条历史条目
（monomer_pair / film_outcome / condition），outcome 映射 film=1.0 /
crystal=0.5 / powder=0.0（unknown 跳过），(paper_id, group_id, kind)
去重幂等、source=graph_import + graph_indexed=True。踩坑：841/6197
节点 group_id 属性为空串 → 从节点 id `R-<paper>-<group>` 解析，尾部空则
合成 `imp-<md5(id)[:8]>`；validate_entry 会重置 graph_indexed → 导入后
显式置回。frozen 自证：首次导入 **11019 条**、重跑 imported=0（幂等）。
发版 v1.9.2（tag e1c519b，三资产上传）→ UAC 批准升级本机 → **1.9.2**，
升级后经 API 触发导入同样 11019 条落库（动态端口 18765 实测）。

## 追加：本轮 7 项问题修复批次（版本号由用户指定，未发版）

分支 `fix/v1.9.3-diagnosis-batch`（起点 tag `pre-v1.9.3-backup`），3 个提交、
43 文件 / +4417 −229。用户先确认方案（含附件留存、元数据回填、实验时间不落盘
只派生、笔误跳过、深度研究上传仅作证据、UI 可调宽 + 美化需先出方案），再逐项实施。

1. **引用跳转（问题 1）**：根因是撰写 prompt 的 `（https://…）` 全角括号被 GFM
   autolink 吞进 href（只修剪 ASCII 尾标点）。新增 `normalize_markdown_links`
   （链接目标/裸 URL 尾部标点剥离、全角括号去壳、成对括号保留），在生成后/落盘/
   读取/Word 导出四处归一化；prompt 改标准 markdown 链接并禁止中文括号包裹 URL；
   前端抽出 `@/lib/markdown`（mdComponents + 归一化），气泡与会话报告弹窗统一渲染、
   所有 http(s) 外链走 openExternal，新增结构化引用兜底列表与「查看报告」入口。
   实测：本机 10 份历史报告 85 个 URL，脏链接 73 → 0。
2. **LLM 提取失败（问题 3）**：实测复现——解析 LLM 是推理模型，`max_tokens=4000`
   被 reasoning 吃光（finish_reason=length、content 为空）→ 0 条降级。改默认
   16000 + content 为空自动翻倍（≤32000）并在 note 说明；新增文献级元数据提取 +
   `PATCH /papers/{id}` 只补空字段（DOI 冲突跳过）；扫描件（字符/页<50）422 明确
   文案（本机 156 篇 PDF 全文本型，故不引入 OCR/Vision）；多段自造组号加段前缀；
   编号 `next_paper_id=1782` 已正确并加测试锁定。prompt 增加 SMILES 纪律
   （未给结构式不得输出 monomer_pair/film_outcome）。
3. **条目试校验 + 表征技术归一化（本轮实测新发现，两个真实缺陷）**：
   - `validate_entry` 把 technique `upper()` 后比对白名单，而白名单里
     `UVVis/contact_angle/separation_flux/separation_selectivity/mechanical/
     photocatalysis/electrochem/dft` 是小写混合 → **这 8 类表征永远校验失败**，
     而解析 prompt 恰好要求这些写法。新增 `normalize_technique`（大小写/连字符/
     空格容错 + 别名 XRD→PXRD、IR→FTIR、UV-Vis→UVVis、DSC→TGA、FE-SEM→SEM、
     N2 adsorption→BET、water flux→separation_flux 等）。**实测：修复前 189 条
     中 80 条被拒（42%）→ 修复后 211 条中仅 1 条（0.5%）**。
   - 只有定性结论、无定量 metrics 的 characterization 原被拒 → 改为允许入库
     （`metrics=[]` + `qualitative=True`），既无数值又无结论才拒。
   - LLM 会输出 SMILES 为空的 monomer_pair，原子入库导致整批 400 且看不出哪条错：
     新增 `knowledge.annotate_entries`（逐条 `valid/invalid_reason`），`/parse`
     返回 `valid_count/invalid_count`；`add_entries` 报错带「第 n 条（kind=）」；
     前端默认只勾合法条目、非法条目禁用并显示原因。
4. **文献入图谱（第 7 点）**：新增文献节点 `LITP-<paper_id>`（node_type=literature，
   `system`/`innovation` 对齐内置图检索字段），录入确认与元数据回填即写入，
   未解析也能被助手 query_graphrag 命中；组节点补 `reaction_cited_in` 边。
5. **实验时间（问题 5）**：新增 `src/records/dates.py` 容错解析（`2026-9-10`、
   双空格、`26/7/3/10:30`、中文日期；笔误 `2026-8-123`/「第N天」跳过取下一个），
   store 派生 `experiment_date/date_source/date_label`（date 语义不变）、按实验时间
   排序；前端 6 处展示 + Word 导出 + 助手工具统一。实测 36 条真实记录 36 命中 0 回退。
6. **助手读完整流程（问题 4.1）**：`read_experiment_records` 补 `process_notes` +
   全部时间点（旧实现仅最近 5 条且无流程）；新增 `read_experiment_record` 单条全文
   工具（注册 + 研究白名单）；context 注入最近一条流程摘要。
7. **多附件 + SI（问题 2）**：新增 `src/literature/attachments.py`（留存 +
   sha1 去重 + ≤20MB/≤8 份 + role=main/si）与 4 个端点；`parse` 支持多文件、
   `use_stored` 复用、`store_files=false` 只解析不留存；主文与 SI 分别解析后合并
   去重，条目带 `source_file` 与 `[SI n]` 证据前缀；前端多选/角色切换/来源与扫描件
   提示/卡片级附件区。
8. **深度研究上传（问题 4.2）**：`AssistantResearchRequest.attachments`（≤3）→
   `run_research(attachments=)`：文档文本作为已核实本地证据注入计划与每步执行、
   进证据清单与参考文献（source=user_attachment），图片如实标注未解析；前端研究
   分支不再提前 return，支持「只传附件不打字」。
9. **界面可调宽（问题 4.3）**：新增 `useResizableWidth`（Pointer Events + clamp +
   localStorage + 键盘微调 + 双击恢复 + allowZero 首次拖拽取实际宽度）与 `Resizer`
   组件；助手页会话列表（160–480）与对话区（520–1400 或铺满）可拖拽，窄屏保持
   原响应式布局。

**验证**：全量 `pytest` **1075 passed / 2 skipped**（基线 963，+112）；`tsc` + `vite build`
通过；开发模式隔离数据目录真机端到端实测 **21/21 通过**（脚本 `.tmp_xtb/smoke_v193.py`，
隔离 `COF_DATA_DIR`，用真实 12 页主文 + 49 页 SI + 用户真实 LLM 配置）：真实 LLM 补解析
产出 **211 条（主文 120 + SI 91，11 组）**、推理模型预算自动扩容 32000、无降级、元数据
6 字段、条目带 `[主文]/[SI n]` 来源标注、**字段完整条目入库成功并 graph_synced=2**、
附件 sha1 去重/下载/删除、实验时间取时间线首点（2026-07-03）、录入编号递增 1785、
深度研究附件证据事件与计划事件下发。

**UI 美化**：`docs/UI美化方案-20260912.md`（令牌化 → 组件 → 页面三批，含风格/密度/
节奏三问），等用户拍板后实施。**发版待用户指定版本号并确认。**

## 追加：本轮第二轮（真机验证 + 打包链路修复 + UI U1）

1. **开发模式真机端到端实测 21/21 通过**（隔离 `COF_DATA_DIR`，真实 12 页主文 +
   49 页 SI + 用户真实 LLM 配置）：录入编号递增、录入即写文献节点、主文+SI 上传与
   sha1 去重、真实 LLM 补解析（推理模型预算自动扩容 32000、无降级）、条目来源标注
   `[主文]/[SI n]`、条目入库 + `graph_synced`、元数据回填只补空、实验时间取时间线
   首点、附件下载/删除、深度研究附件证据与计划事件下发。脚本 `.tmp_xtb/smoke_v193.py`。
2. **实测又揪出两个真缺陷并修复**（详见 `eeb1bb2`）：① `validate_entry` 把 technique
   `upper()` 后比对白名单，导致 `UVVis/contact_angle/separation_flux/
   separation_selectivity/mechanical/photocatalysis/electrochem/dft` 这 8 类规范写法
   **永远校验失败**（而 prompt 正要求这些写法）→ 新增 `normalize_technique`（大小写/
   连字符/空格 + 别名 XRD→PXRD、IR→FTIR、UV-Vis→UVVis、DSC→TGA 等）。
   **量化：修复前 189 条中 80 条被拒（42%）→ 修复后 211 条中仅 1 条（0.5%）**。
   ② 只有定性结论无 metrics 的 characterization 原被拒 → 允许入库（`qualitative=True`）。
   另加：条目逐条试校验标注（`valid/invalid_reason`）+ 前端默认只勾合法条目 +
   原子入库报错带条目序号。
3. **打包链路修复（发版阻塞级）**：新增 `literature.attachments`、`records.dates`
   双命名空间 hiddenimports；并发现 **PyInstaller 复用陈旧 TOC 缓存**导致冻结产物
   只带旧 `index.html` + 1 个 CSS、**当前前端资源全缺**（打包后白屏）→ 必须
   `Remove-Item build\cof-backend` 清缓存后重编；清缓存后 7 个资源齐全且
   `index.html` 与 `webapp/dist` 哈希一致。
   **发版顺序纪律：先 `npm run build`（前端）→ 清 PyInstaller build 缓存 → 再打后端。**
4. **冻结版自证 9/9 通过**（`dist-backend/cof-backend/cof-backend.exe --port 8901`，
   隔离数据目录）：health、录入编号+文献节点、`literature.attachments`（主文+SI）、
   `annotate_entries` 计数、条目入图、`PATCH /papers` 只补空、
   `records.dates` experiment_date、`assistant.research` 新 attachments 参数无
   ImportError、侧车图含文献节点+组节点+`reaction_cited_in` 边。
   脚本 `.tmp_xtb/frozen_selfcert_v193.py`。
5. **UI U1：语义令牌迁移**（子代理执行 + 我复核）：全前端硬编码 Tailwind 调色板类
   **217 → 2**（215 处替换，21 文件），仅保留 3D 分子视图的双分子图例色（cyan/
   fuchsia，属多序列可视化）；`tsc -b` + `vite build` 通过，产物 CSS 已确认生成
   `bg-success/10`、`border-warning/30` 等 alpha 规则且无 amber/emerald/red 残留。
   复核修正：Psi4 精度档徽标还原为 `text-gold`（`text-gold-foreground` 在浅金底上
   对比度不足）。方案文档已校正真实基座（3 主题 × 明暗，非单主题紫金）。
6. 新录入文献闭环：录入时上传的 PDF 在确认入库后**自动留存为该文献附件**，知识库
   「补解析 → 用已存附件解析」即可，无需重传；补解析界面新增耗时秒数与逐段说明，
   避免长文献数分钟无反馈被误判卡死。

## 追加：本轮第三轮（视觉验收驱动的 UI 修复）

**新增能力：Electron 离屏截图自查**（`.tmp_xtb/shoot.cjs`，`npx electron …`）——
起冻结后端（隔离数据目录 + 真实 data 副本、不含 config 以免误触发付费 LLM），
`webPreferences.offscreen = true` 逐页 `capturePage()` 出图（8 页 × 明暗），
真正「看图改界面」。踩坑：窗口用 `x:-3000` 离屏时 `capturePage` 返回 0×0，
必须用 offscreen 渲染。

逐张看图后定位并修复 4 处（提交 `992cb42`）：

1. **【全站·浅色主题头号缺陷】侧栏菜单几乎不可读**：`AppLayout` 用
   `bg-sidebar-background`，但 `tailwind.config.js` 的 `sidebar` 色组只定义了
   `DEFAULT`（没有 `background` 键）→ 该类不生成 → 侧栏背景透明（露出浅色页面底），
   文字却是 `text-sidebar-foreground`（浅色）→ 浅底浅字；深色模式下因页面底本
   就暗而被掩盖（所以只在浅色露出）。补 `sidebar.background` 后恢复深墨底 + 浅字，
   并在产物 CSS 中确认 `.bg-sidebar-background` 已生成。
2. 「置信度 NaN%」：`payload.confidence` 实际是 `{level, reason}` 对象，Home 却按
   数值 `*100` 渲染。新增 `lib/format.ts::confidenceLevel`（兼容 数字 /
   `{score|value}` / `{level}`）→ 显示「高置信 / 中置信 / 低置信 / 置信度未知」。
3. 收藏卡时间戳裸 ISO（`2026-08-19T08:52:00+08:00`）→ `lib/format.ts::formatDateTime`
   统一 `YYYY-MM-DD HH:mm`（收藏卡 / 详情 / DFT 快照四处）。
4. 收藏卡高矮不齐 + SMILES `break-all` 折三行 → 卡片 flex 等高、SMILES 单行截断
   （`title` 悬停看全串）、徽章与时间行 `mt-auto` 底部对齐。

**结论**：界面「丑」的主要来源不是配色，而是**类名与令牌体系脱节导致的静默失效**
（写了 `bg-sidebar-background` 而该类根本不存在）+ 少量数据展示未格式化。后续 U2/U3
继续用截图自查闭环推进。

## 追加：本轮第四轮（U2 时间/空态一致性 + 助手页与知识库视觉复核）

截图脚本升级：支持**整页捕获**（按 `scrollHeight` 放大离屏窗口）与**局部特写**
（`SHOOT_SCROLL`）；并用**本地哑配置**（base_url 指向保留端口 `127.0.0.1:9`）让助手页
进入「已启用」态以便看到真实对话界面，同时**不会真的调用付费接口**。
踩坑复现：PowerShell `Set-Content -Encoding UTF8` 写入的配置带 BOM → 后端
`json.loads` 失败被判为「未配置 LLM」，改用 Python 写无 BOM 配置后正常。

复核结论与改动：

1. **助手页视觉复核通过**：深色侧栏 + 今日科研日报卡（红/绿语义徽章）+ 会话列表
   （标题/消息数/日期）+ 空态引导 + 输入区（附件、Enter 发送提示）层级清晰，无需返工。
2. **科研知识库视觉复核通过**：录入卡（三种方式 tabs + DOI 输入 + 查询）、知识库头部
   （导入图谱历史条目）、左列表（筛选 + #编号/标题/DOI + 选中高亮）、右详情
   （文献头 + **附件（主文/SI）行** + 上传主文/SI 按钮 + 原文信息/结构化条目/图谱 tabs）
   均正常，本轮新增的多附件入口在界面中可见可用。
3. **时间戳展示统一**：Query 打分历史、DFT 历史、方案卡、方案列表四处的
   `replace('T',' ').slice(0,19)`（带秒、格式各异）统一走 `lib/format.ts::formatDateTime`
   → `YYYY-MM-DD HH:mm`。
4. **空态文案澄清**：知识库「原文信息」在无摘要时由「（无摘要：可点右上『补解析』上传
   全文提取）」改为虚线框提示「该文献暂无摘要。点右上『补解析』上传主文 PDF（可加补充
   信息 SI），即可同时提取摘要等元数据与结构化条目。」——明确「补解析」同时产出元数据与条目。

验证：`tsc -b` + `vite build` 通过；截图复核 assistant / mine（整页 + 知识库特写）/
settings 三页；`pytest` 全绿。

## 追加：本轮第五轮（U3 第一批：实验记录 + 方案迭代）

截图复核两页，抓到并修掉一个**全量可见的数据展示缺陷**：

1. **`OOD: [Object object]`**：实验记录列表与详情把 `prediction_snapshot.ood`
   （实际是 `{level: 'in'|'warn'|'out', reasons: []}` 对象）直接模板插值渲染，
   于是每条带预测快照的记录都显示 `OOD：[Object object]`。新增
   `lib/format.ts::oodInfo`（兼容 字符串 / `{level|status}`，映射
   in→分布内 / warn→分布外警告 / out→分布外），记录列表与详情两处改用；
   批量页/打分页/结果卡原本就用 `ood.level`，无此问题。
   截图验证：`OOD：[Object object]` → **`OOD：分布外警告`**。
2. 复核结论：实验记录页（左录入表单 9 项条件 + 右记录卡列表：实验时间/编号/失败徽章/
   操作按钮/条件行/预测快照）与方案迭代页（选择收藏 + 锚定记录 + 问题输入 + 生成建议 +
   空态 + 已生成方案卡）布局与层级正常；时间戳已统一为 `YYYY-MM-DD HH:mm`。
   记录的日期显示为**实验时间**（如 A6+TAPT 录入 09-12、显示 09-10），问题 5 的修复
   在界面上确认生效。

验证：`tsc -b` + `vite build` 通过；`pytest` 全绿。

## 追加：本轮第六/七轮（U3 收尾：九页复核 + 实验记录竖向时间轴）

第六轮：补齐复核 查询打分 / 批量排序 / DFT / 设置 四页 —— **无缺陷**（`开始打分`
淡化是未填 SMILES 的禁用态；DFT 右侧留白经查为「结果区未计算时的引导占位」，属设计
意图，未改）。至此 **9 页全覆盖复核完毕**，累计由截图发现并修掉 **6 处真实缺陷**
（侧栏类名未生成、置信度 NaN%、ISO 时间戳裸渲 5 处、收藏卡不齐+SMILES 硬换行、
`OOD：[Object object]`、知识库空态文案）。

同轮完成**问题 1 的界面级验证**：用交互式截图（`SHOOT_JS` 点开含深度研究报告的会话）
扫描真实 DOM —— 渲染 14 个外链、**0 个脏链接**，且含成对括号的复杂 DOI
（`.../Websites_(Inorganic_Chemistry)/...`）完整保留，证明链接修复在真实界面生效。

第七轮：**实验记录改为竖向时间轴**（用户已确认的 UI 项）：左侧导轨 + 结果色节点
（成膜主色 / 部分成膜金色 / 失败中性灰，草稿金色空心）+ 页面底描边防止与导轨糊在一起；
每条记录头部改为两行（① 等宽加粗「实验时间」+ 右侧操作按钮 ② 单体对 + 结果徽章 +
编号），条件摘要与预测快照保持原样。截图验证浅色渲染符合预期。
截图脚本入库 `scripts/ui_screenshot.cjs`（开发用，不随包分发），并记录已知小坑
（应用主题 hook 会覆盖手动注入的 `.dark`，深色验收需在设置页切换主题）。

## 追加：本轮第八/九轮（安全加固 + 发版准备 + 界面密度）

**第八轮（安全 + 发版链路验穿）**：
- 发现本地发版脚本硬编码 GitHub PAT，且仓库为 **public** → 完整排查：已跟踪文件中
  **无完整 token**（历史日报只有 4 字符前缀 `github_pat_11CB...`，不可用；用唯一子串
  搜索 `git ls-files` 结果 = 0 命中；`git log -S` 无记录）。真实暴露面仅是本地
  gitignored 脚本 → 9 个脚本改为 `_load_github_token()`（env `GITHUB_TOKEN` 或
  `.tmp_xtb/github_token.txt`），明文归零；新增 `tests/test_no_secrets.py` 回归防线
  （扫 git 跟踪文件，命中完整密钥即失败）。**建议用户轮换该 PAT**（零成本保险）。
- 发版链路验穿：干净重建冻产后**产物与前端 8 文件清单一致、index.html 哈希一致**；
  冻结自证 **9/9 通过**。踩坑：残留 `cof-backend` 进程锁住产物 → `[WinError 5]`；
  PyInstaller 成功退出码为 0（之前的 1 来自同命令行清缓存失败）。
- 交付 `docs/发版checklist.md`：版本号三处同步 → 前端 build → **清 PyInstaller 缓存**
  （不清会白屏，本轮实测）→ 产物哈希核对 → 冻结自证 → electron-builder →
  tag/Release/上传顺序（exe→zip→latest.yml 最后）→ 本机安装验收 → 回滚。

**第九轮（UI 收尾：全局界面密度）**：新增 `hooks/use-density.ts` + 设置页「界面密度」卡
（舒适/紧凑），经 `<html data-density>` 生效，紧凑档收紧卡片内边距（`--density-card-p`）
与所有表格行高（`--density-cell-y`）。首版踩坑：hook 只挂在设置页 → 其他页面从不设置该
属性（截图对比发现两档一样）→ 改为挂在常驻的 `AppLayout`，并广播事件做跨组件同步；
重新截图确认紧凑档同屏可多显示约一条记录。`scripts/ui_screenshot.cjs` 增加
`SHOOT_DENSITY` 开关用于两档对比截图。

## 追加：v1.9.3 发版完成（Release 387495529）

用户确认「发 v1.9.3版」后按 `docs/发版checklist.md` 执行：

1. **版本号三处同步**：`README.md` / `api/__init__.py` / `webapp/package.json` → 1.9.3
   （`package-lock.json` 根版本历史遗留 1.0.2，不属发版纪律，未动）。
2. **前端 build** 通过（`tsc -b && vite build`，exit 0）。
3. **清 PyInstaller 缓存后打包后端**：产物与 `webapp/dist` **8 文件清单一致、
   index.html 哈希一致**（上次白屏的坑已闭环），PyInstaller exit 0。
4. **冻结版自证 9/9 通过**，`version=1.9.3`。
5. **electron-builder** → `cof-film-recommend-setup-1.9.3.exe`（456.6MB，FileVersion
   1.9.3）+ `latest.yml`（version: 1.9.3）。踩坑：`-c.directories.output=` 被当成配置
   文件路径 → 改为清理旧产物 + 默认输出目录 + `--config.directories.output`。
6. **zip**：`COF-Assistant-Setup-1.9.3-win-x64.zip`（456.5MB，内含安装包）。
7. **master 快进合并**（合并前确认 master 无独有提交）+ tag `v1.9.3` + 推送。
8. **GitHub Release**：`https://github.com/Ckx-z/kimi-tianxuan-seek-APP/releases/tag/v1.9.3`
   （id **387495529**，非 draft/prerelease）；资产上传顺序 exe → zip → **latest.yml 最后**，
   复核 3/3 `uploaded`。
9. **本机安装**：先关闭旧版进程（升级必要）→ UAC 批准 → `E:\COFapp` 1.9.2 → **1.9.3**
   （FileVersion 1.9.3）→ 启动实测：`/api/health version=1.9.3`、文献 1711 篇、
   知识库条目 **11019 条保留**、新端点 `/attachments` 可用（上限 8）、记录 36 条且
   `rec_20260912_002` 显示**实验时间 2026-09-10（来源 timeline）**、助手 enabled；
   并对安装版截图确认**界面正常渲染**（深色侧栏可读、置信度「高/中置信」、无白屏）。

**发版过程顺带修复**：上一轮脱敏脚本的引号 bug（`pat = "_load_github_token()"` 把调用
写成了字符串字面量，会让认证把这段文本当 token）→ 已修正 10 个脚本为
`pat = _load_github_token()`；`upload_asset.py` 改为从 `RELEASE_ID` 环境变量取 Release id。
