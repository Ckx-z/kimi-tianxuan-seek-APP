# 日报 2026-08-29（人版）

## 一句话总结

**v1.2.0 已完整发布上线**——接续 k3 昨天没做完的收尾，把版本号正式提交、把打错的
标签修正好，并逐项核对了打包产物（Psi4 精度档、导出 Word/文献录入修复、PDF 录入
全部真机验证通过），GitHub 上三样下载文件也已确认完好。

## 各项交付

1. **版本号正式入库**：k3 昨天改了版本号但没提交，我已提交并推送到远端（`d3417f5`）。
2. **标签修正**：k3 昨天把 v1.2.0 标签打在了不含版本号的那次提交上（照此 checkout
   会拿到 1.1.0 的代码），我已强制修正到正确提交。
3. **打包产物真机自证**：把打包好的后端程序真的跑起来验了一遍——版本号 1.2.0 ✓、
   首页缓存头 ✓、科研助手 ✓、Psi4 精度档（含 B3LYP 文献口径档）✓、文献模块 ✓、
   DFT 真实计算跑通 ✓。
4. **下载文件核验**：GitHub 上的 exe 和 zip 与我本地的逐字节一致（sha256 完全匹配），
   自动更新元数据 latest.yml 内容正确且确实是最后上传的。

## 命名决策（已拍板）

安装包压缩包沿用 k3 起的**英文名** `COF-Assistant-Setup-1.2.0-win-x64.zip`（用户
已确认保留），README 里的下载文件名引用已同步改为英文名。

## 另提醒（与上次日报相同）

- GitHub 上还有 13 条文献 DOI 待人工回填。
- 你之前说过的**删除 GitHub PAT** 还没删（它仍存在这台电脑的凭据管理器里），确认
  不再需要后建议删掉。

## 追加（下午）：发版一致性核对与文档致谢

- 核对了最新 Release v1.2.0 与 master 最新提交：Release 指向 `d3417f5`（版本号提交），
  master 领先 2 个**纯文档**提交，无代码改动，故**无需再发新版**。
- README 已注明「本项目由 DeepSeek 协助完成」，本次操作亦由 DeepSeek 协助完成。

## 追加（晚间）：DFT 计算页「切页丢状态」Bug 修复（由 DeepSeek 协助完成）

- **修复了你反馈的问题**：DFT 计算中途切到别的页面再回来，之前填的参数和计算进度
  会全丢。现在改为把「表单草稿 + 任务状态」存到你电脑本地（和收藏夹、实验记录同一
  套存储），切页/刷新/后端重启后都能恢复；计算还在跑就续进度，跑完了直接出结果，
  后端重启导致中断会明确提示并可一键重提。
- 方案文档存于 `docs/DFT状态保持修复方案.md`；全部测试 792 项通过。
- **纯本机实现，不需要服务器，不上传任何数据**，对你电脑的负担可忽略（多写一个
  几十 KB 的小文件）。
- 注意：**此修复要等下一次发版（打进安装包）才对下载用户生效**；如果你想先在本机
  验证，我可以按发版纪律跑一遍构建链出新安装包。

## 追加（2026-08-31 会话续）：v1.2.1 已发布（由 DeepSeek 协助完成）

- 你验收通过后，按发版纪律完成 **v1.2.1**：版本号三处同步 → vite/PyInstaller/
  electron-builder 三步构建 → 打包版真机自证（版本 1.2.1、新界面指纹、草稿端点、
  真实 DFT 任务全部通过）→ tag → GitHub Release 三资产（exe / zip / latest.yml）。
- 中途踩坑并已记录：你的 Watt 加速器会让 Node 类工具报证书错误，构建时加了
  `NODE_OPTIONS=--use-system-ca` 解决（保留安全校验，只是改用 Windows 证书库）。
- 已安装的用户打开应用会收到自动更新提示（或覆盖安装新包）；用户数据不受影响。

## 追加（2026-08-31 夜）：界面精修 + v1.3.0 发布（由 DeepSeek 协助完成）

- **界面焕新（学术紫金 · 精修版）**：主紫色更柔和、金色更温暖；补齐成功/警告/
  信息三种状态色；阴影更柔和；「开始计算」「开始打分」等关键按钮改为暖金强调，
  提交时带转圈防重复点击；结合能与打分数字改为等宽字体，读数更整齐。
- **DFT 全局任务进度 + 完成通知**（上一轮完成、本轮一并发布）：计算后右下角有
  全局悬浮徽标，任何页面都能看进度；完成/失败会弹窗 + 系统通知提醒。
- 版本 **v1.3.0** 已发布：三资产（exe / zip / latest.yml）上传完毕，已安装用户
  打开应用会自动收到更新提示，用户数据不受影响。
