# 日报 2026-07-23（AI 版）

## 今日工作：阶段 23 — 前端迁移收尾 + Phase 3 桌面分发全链路

### 上午~下午：前端迁移收尾

1. **字体调整**：webapp 全局字体改为 中文宋体 + 英文 Times New Roman（tailwind.config.js fontFamily），构建通过（提交 bfc9d03）
2. **阶段二 新旧双版对照验证**（`scripts/phase2_parity_check.py` + `reports/phase2_parity.md`）
   - 13 组用例：✅11 / ⚠️2 / ❌0，结论**可切换**
   - tree_score 逐位一致；OOD 三级判定全对上；收藏/实验记录 CRUD、方案卡、页⑤迭代契约全过；基线 346 passed 未破
   - 仅 gnn_score 0.013–0.016 波动（GNN subprocess 固有抖动，新旧同源同幅，非链路问题）

### 下午~傍晚：Phase 3 桌面分发（用户确认目标：分发课题组多人使用）

**技术路线决策**：机器无 Rust → 弃 Tauri 改 Electron；主环境已有 networkx 3.5 → GraphRAG 可并入主进程，分发无需 E:\python3.12；GNN（dphuanjing py3.8+torch+PyG）不可分发 → 定为可选增强、缺失自动降级。

**波次 1（并行）**
- **任务 A 运行时配置化**：新增 `src/runtime_config.py`（环境变量 > config/runtime.local.json > 自动探测 > 标记不可用）；iterate.py / gnn_model.py / silent_launch.py 去硬编码；新增 `GET /api/llm/env-status` 能力自检；GraphRAG 无主进程外解释器时主进程内 import 兜底；346 基线未破
- **任务 C Electron 壳 + 静态托管**：api/main.py 挂载 webapp/dist（SPA fallback，修复 starlette HTTPException 捕获方向 bug）；webapp/electron/main.cjs（自动选端口 18765 起、健康检查轮询、taskkill 进程树清理、COF 图标）；Electron 二进制经 npmmirror 镜像装上；dev 实测预测出分

**波次 2：PyInstaller 后端 exe**
- `scripts/backend_entry.py`（--port/--host）+ `scripts/cof-backend.spec`；onedir 产物 `dist-backend/cof-backend/` **927MB**（从 1.4GB 裁掉 panel/botocore/numba/bokeh 等误拉依赖与 MKL avx512 变体）
- runtime_config 补 frozen 路径解析：只读资源根 sys._MEIPASS，可写数据根 %APPDATA%\COF-Film-Recommend（COF_DATA_DIR 可覆盖）；14 个模块路径常量改造
- 干净目录实测：health / predict（GNN 降级 gnn_score=null 不崩）/ SPA / iterate suggest 主进程内 GraphRAG 出建议 ✓；打包后源码基线 346 passed ✓

**波次 3（并行）**
- **任务 D1 NSIS 安装包**：`webapp/release/COF成膜推荐系统 Setup 0.0.0.exe` **357MB**（lzma）；真实安装烟测：静默安装→快捷方式启动→8s 就绪→预测出分 tree 0.5293→env-status 正常→关窗 0 进程残留→静默卸载干净 ✓。坑：中文路径下 electron-builder 必失败（改 ASCII 输出目录构建）；PATH 需补 System32
- **任务 D2 文档**：README 重写（安装/首用/研究背景整合/开发模式）、docs/用户手册.md（六功能页操作+FAQ）、docs/分发说明.md（转发 checklist+数据隔离）、llm_settings.example.json 对齐

**发布准备**：安装包压缩 `COF成膜推荐系统-Setup-0.1.0-win-x64.zip`（356MB）；tag `v0.1.0` 已推送；提交 877dceb（打包产物经 gitignore 拦截未入库——357MB 超 GitHub 单文件 100MB 限制，曾误入提交已 amend 移除）

### 验证

- pytest 346 passed（多轮）；双版对照 13 用例；PyInstaller 干净目录实测；NSIS 真实安装烟测全过

### 遗留 / 待办

- [ ] **GitHub Release 发布被凭证卡住**：git 走 SSH、MCP OAuth 未开放 create_release 权限——需用户提供 PAT 或重连 GitHub 插件授更大 scope，或手动网页 Draft release 上传 zip
- [ ] **Mac 支持**：当前仅 Win x64。Mac 需 macOS 环境重新打包（PyInstaller 不跨平台）；可选方案：GitHub Actions macos runner CI 打包（资产均在仓库内，可行，待评估）
- [ ] 分发实测：找一台课题组成员机器装一遍全流程
- [ ] shiyandiedai 仓库手动归档（用户操作）
- [ ] 模型侧遗留（积压）：双留出 fold3 诊断、概率校准、双模型策略复盘点
