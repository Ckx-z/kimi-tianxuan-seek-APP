# 日报 2026-07-24（人版）

## 一句话总结

你昨天实测提的 **11 项改进全部做完**，并且打出了包含全部改进的 **v0.2.0 新安装包**——卸载旧版装新版即可体验。

## 今日成果

1. **性质卡修好了**：之前 LLM 配好却加载失败，根因是 longcat"先想再答"把回复字数预算吃光了，已调大预算并统一了配置口径，现在真实生成解读。
2. **实验记录大升级**：没做完的实验可以"暂时保存为草稿"（金色卡片标记，随时继续编辑）；新增"完整实验流程"书写区和**时间点记录**——每条含时间、过程描述、可传照片/附件（图片可缩略图放大看）。
3. **方案迭代页两处修复**：不再误报"LLM 不可用"；建议现在按单体组分开，选哪组只看哪组的迭代建议。
4. **浅色模式字太浅**：整体加深，现在清晰可读。
5. **防多开**：连续双击图标不会再开一堆窗口，只会唤起已开的那个。
6. **结构图回来了**：单体结构图 + 二聚体（醛胺缩合产物）结构图都正常显示。
7. **自动更新已接好**：以后发了新版本，软件会弹窗提示、确认后自动下载安装（等 GitHub Release 打通即生效）。
8. **收藏页两个 bug 修复**：不再错误显示"未打分"（之前打过的分自动补上）；点开放大看到的内容和查询打分页一模一样，不用再重查。
9. **打分理由**：预测结果现在带解释卡——哪个因素起决定作用、影响多大，清清楚楚。
10. **查询历史**：每次打分自动记录，点一下就能完整回看当时的输入和结果。
11. **收藏按钮**：已收藏会显示"已收藏"，不会重复收藏，也可点击取消。

## 验证

- 自动化测试 373 项全过（含新增 27 项）；新安装包真实冒烟：启动、预测出分、结构图、关闭无残留全部正常。

## 下一步建议

1. **卸载旧版 → 安装 v0.2.0**（安装包在 `webapp\release\` 下，zip 也备好了），把 11 项逐项过一遍。
2. 装完后双击仓库根的 `更新桌面图标.bat`，把桌面旧图标指向新版。

## 阻塞点

- ~~GitHub Release 上传还是差授权~~ → 已解决，见下方傍晚追加。
- 今天运行时重启了三次导致打包中断了几回，最后由我直接接管完成，没有影响结果。

---

## 傍晚追加：软件正式"上架"了

**v0.2.0 已经发布到 GitHub Releases**：https://github.com/Ckx-z/kimi-tianxuan-seek-APP/releases/latest

1. **你给的 PAT 一次通关**：安装包（357MB）、转发用 zip（356MB）、自动更新清单三个文件全部传好。
2. **自动更新真的通了**：我模拟软件的更新检查流程实测——能正确读到新版本信息。也就是说从现在开始，**以后每次发新版，你和同学们的软件都会自己弹窗提示升级**，再也不用重下重装。
3. **README 首页换上了"同学快速上手（5 分钟）"指引**：下载→安装→自检→配 LLM→开始用，四步写清，直接把仓库链接发群里同学就能照着装。
4. **小插曲**：你的 Watt Toolkit（进程名 Steam++）会接管 GitHub 的访问，今天摸清了它的脾气——上传走它的通道有 2.6MB/s，git 推送偶尔被它掐断时我也有备用通道，以后都稳了。

## 明天你可以做的三件事

1. 装 v0.2.0 把 11 项改进过一遍（重点：性质卡、实验记录草稿和时间线、结构图、历史回显）。
2. 把仓库链接或 zip 发课题组群，找一位同学的电脑装一遍——这是分发前最后一关。
3. 顺手去 GitHub 把今天那个 PAT 删了（它的活儿干完了，删掉最干净）。
