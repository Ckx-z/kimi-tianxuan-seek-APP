# 日报 2026-08-24（AI 版）

## 今日工作：阶段 27 — 遗留清理 + v0.3.0 四项 bug 修复 + v1.0.0/科研助手V2 双方案

### 已完成

**1. 遗留清理（04ceecd）**
- README/PROJECT_STATE 已被用户先行同步至阶段 26（4b5a662），核对版本号 v0.3.0、测试基线 420 均正确
- 补收尾：`data/assistant/`（科研助手会话，每机独立的运行时数据）加入 .gitignore；删除发版临时脚本 `_mk_payload.py`/`_release_payload.json`；提交再生成的 test_report.docx

**2. 四项 bug 修复（e7d4179，441 passed = 420 基线 + 21 新增）**
- **查询打分历史恢复**：`GET /api/predict/history`（limit/offset 分页、容错空列表）+ Query 页历史面板点击回显
- **收藏"未打分"修复**：收藏时打分快照（score/ood/score_policy 等）随 FavoriteCreate 一并落库 `latest_prediction`；旧收藏保留"未打分"徽章 + 卡片/详情双入口"一键打分"（后端自动回写快照）
- **实验记录单次保存**：ProcessPanel 支持受控模式，RecordEditDialog 的「保存草稿/保存修改/转为正式」一次 PUT 提交流程+时间线，失败中文提示且已填内容不丢
- **科研助手附件**：新增 `src/assistant/attachments.py`——图片（png/jpg/jpeg/webp）走 OpenAI vision base64，文档（txt/md/json/csv/docx/pdf）提取文本注入上下文（docx→python-docx，pdf→pypdf 缺失时 PyMuPDF 回退）；vision 被端点 4xx 拒绝自动降级提示"模型不支持看图"；≤10MB、≤3 个、落 user_data_root；前端回形针+chips+历史回显+断线复用
- **方案库模板管理**：plan_templates 读写分离（内置只读/用户模板写 user_data_root）；新增 `DELETE /api/plan-templates/{id}`（内置 403）；「我的」方案库加"添加方案模板"对话框（docx 上传+LLM 提取+即时刷新+删除确认）；Query 页模板选择器经查已正常
- 前端 `npx vite build` 通过

**3. 双方案设计（待用户审查）**
- `docs/v1.0.0改版方案.md`：品牌更名（对外 COF科研助手/系统内 COF科研系统，appId 与数据目录不动保更新链）；工具箱父模块（查询打分/批量排序迁入）；**DFT 计算模块选型 grimme-lab/xtb（GFN2-xTB 半经验，conda-forge 有 win-64 包，LGPL-3.0 可捆绑二进制，~90MB）**；收藏夹体系（Folder 实体+迁移到"收藏夹1"+双确认删除+打分/DFT 交叉合并快照）；实验记录关联扩展；五批次实施计划约 3.5-4 天
- `docs/科研助手V2方案.md`：工具集读→写闭环（收藏管理/方案卡生成/DFT 查询等 6 新工具，写操作二次确认）；记忆编译（会话级压缩+用户级 memory.md，本机存储可清空）；主动能力（实验日报卡/连续失败提醒）；V2.0-V2.3 分期

### 风险沉淀

- pypdf 未装于 E:/ANACONDA，pdf 提取实测走 PyMuPDF 回退——打包环境需确认两者其一
- vision 降级依赖端点返回 4xx；静默忽略图片的模型不会有显式提示
- 附件孤儿文件未自动清理（可后续加定期清理）

### 明日待办

- [ ] 用户审查双方案 → 确认后开工（建议 v1.0.0 P1 更名+工具箱先行，V2.0/V2.1 可并行）
- [ ] 用户删除 PAT
- [ ] Mac 版 CI、shiyandiedai 归档、模型侧 fold3/概率校准（中期）
