# 日报 2026-08-24（AI 版）

## 今日工作：阶段 27 — 遗留清理 + v0.3.0 四项 bug 修复 + v1.0.0/科研助手V2 双方案

### 已完成

**1. 遗留清理（04ceecd）**
- README/PROJECT_STATE 已被用户先行同步至阶段 26（4b5a662），核对版本号 v0.3.0、测试基线 420 均正确
- 补收尾：`data/assistant/`（科研助手会话，每机独立的运行时数据）加入 .gitignore；删除发版临时脚本 `_mk_payload.py`/`_release_payload.json`；提交再生成的 test_report.docx

**2. 四项 bug 修复（e7d4179，441 passed = 420 基线 + 21 新增）**
- **查询打分历史恢复**：`GET /api/predict/history`（limit/offset 分页、容错空列表）+ Query 页历史面板点击回显
- **收藏"未打分"修复**：收藏时打分快照（score/ood/score_policy 等）随 FavoriteCreate 一并落库 `latest_prediction`；旧收藏保留"未打分"徽章 + 卡片/详情双入口"一键打分"（后端自动回写快照）
- **实验记录单次保存**：ProcessPanel 支持受控模式，RecordEditDialog 的「保存草稿/保存修改/转为正式」一次 PUT 提交流程+时间线，失败中文提示且已填内容不丢
- **科研助手附件**：新增 `src/assistant/attachments.py`——图片（png/jpg/jpeg/webp）走 OpenAI vision base64，文档（txt/md/json/csv/docx/pdf）提取文本注入上下文（docx→python-docx，pdf→pypdf 缺失时 PyMuPDF 回退）；vision 被端点 4xx 拒绝自动降级提示"模型不支持看图"；≤10MB、≤3 个、落 user_data_root；前端回形针+chips+历史回显+断线复用
- **方案库模板管理**：plan_templates 读写分离（内置只读/用户模板写 user_data_root）；新增 `DELETE /api/plan-templates/{id}`（内置 403）；「我的」方案库加"添加方案模板"对话框（docx 上传+LLM 提取+即时刷新+删除确认）；Query 页模板选择器经查已正常
- 前端 `npx vite build` 通过

**3. 双方案设计（待用户审查）**
- `docs/v1.0.0改版方案.md`：品牌更名（对外 COF科研助手/系统内 COF科研系统，appId 与数据目录不动保更新链）；工具箱父模块（查询打分/批量排序迁入）；**DFT 计算模块选型 grimme-lab/xtb（GFN2-xTB 半经验，conda-forge 有 win-64 包，LGPL-3.0 可捆绑二进制，~90MB）**；收藏夹体系（Folder 实体+迁移到"收藏夹1"+双确认删除+打分/DFT 交叉合并快照）；实验记录关联扩展；五批次实施计划约 3.5-4 天
- `docs/科研助手V2方案.md`：工具集读→写闭环（收藏管理/方案卡生成/DFT 查询等 6 新工具，写操作二次确认）；记忆编译（会话级压缩+用户级 memory.md，本机存储可清空）；主动能力（实验日报卡/连续失败提醒）；V2.0-V2.3 分期

### 风险沉淀

- pypdf 未装于 E:/ANACONDA，pdf 提取实测走 PyMuPDF 回退——打包环境需确认两者其一
- vision 降级依赖端点返回 4xx；静默忽略图片的模型不会有显式提示
- 附件孤儿文件未自动清理（可后续加定期清理）

### 明日待办

- [ ] 用户审查双方案 → 确认后开工（建议 v1.0.0 P1 更名+工具箱先行，V2.0/V2.1 可并行）
- [ ] 用户删除 PAT
- [ ] Mac 版 CI、shiyandiedai 归档、模型侧 fold3/概率校准（中期）

---

## 下午：v1.0.0 全量实施与发布（阶段 27 主体）

用户审查双方案后确认开工。六个批次一天内完成：

### P1 更名+工具箱骨架（8309e18）
- productName/窗口标题/侧栏 → COF 科研助手；系统内文案 → COF 科研系统；appId/name/数据目录不动保更新链
- 工具箱父菜单收纳查询打分/批量排序 + DFT 占位页；旧路由 /query /batch 重定向兼容
- 顺手修掉 assistant/api.ts 既有 tsc 错误（npm run build 阻塞点）

### P2 收藏夹体系（543d0a6 + fadf849 数据出库修正）
- Folder 实体（favorite_folders.json）+ 幂等迁移（旧收藏归入 folder_default「收藏夹1」）
- 夹 CRUD：重名 400、删夹连带删收藏、最后一个夹保护；收藏支持移夹（PATCH）
- 409 重复收藏拦截（RDKit 规范化单体对比较，detail.existing 带夹名/快照状态）；前端「我的」收藏夹视图 + 非空夹删须输名确认
- 教训：迁移测试把运行时 favorite_folders.json 跑进了仓库，当即 git rm + gitignore——**发版纪律外再加一条：新运行时数据文件第一commit前必须检查 git status**

### P3 DFT 计算模块（6d9b1fb + 018a33f + 2c47a65）
- xtb 选型落地：官方 v6.7.0 Windows 静态包（ghfast.top 镜像 4MB/s 下载，直连仅 46KB/s）；**缺 libiomp5md.dll 从 ANACONDA 补齐实测苯单点通过**；vendor/xtb 58MB 不入库，scripts/fetch_xtb.py 可重建（含 intel-openmp conda 包提取 DLL）
- src/dft/ 引擎：ETKDG+UFF → xtb --opt ×3 → E_bind；**调试抓到真 bug：dimer.py 式整体嵌入会把小分子嵌进苯环心优化塌成共价融合（E_bind 虚高+122），改为单体各自3D化+多取向不重叠摆放取最低**
- 注意：`normal termination of xtb` 在 **stderr** 非 stdout
- 异步任务/缓存（sha1 key）/dft_log 历史/503 引擎缺失降级；真实冒烟 苯·甲醛 gfn2 −0.27 kcal/mol
- 510 passed

### P4 整合（1c2a7bf）
- 导出 Gaussian .gjf / ORCA .inp（RFC5987 中文文件名）；收藏卡 DFT 徽章真实化+详情摘要+重算预填跳转；迭代 RAG 注入 DFT 数据（dft_data 引用白名单，快照→缓存双路，静默降级）；530 passed

### 科研助手 V2.0（a254b8a）+ V2.1（0b87c2d）
- 9 工具读写闭环：list_favorites/list_prediction_history/manage_favorite/generate_plan_card/draft_experiment_record/query_dft（动态确认：缓存命中免确认）；写操作 tool_confirm 令牌（一次性/300s/参数摘要绑定/会话绑定），SSE confirm 端点续跑
- 记忆编译：会话 >20 轮 LLM 摘要压缩（进程内缓存+硬截断降级）；用户级 memory.md（新会话自动收尾上一会话、开局注入最近30条、100条合并去重、LLM 失败全静默）；设置页「助手记忆」开关/查看编辑/清空
- 578 passed

### P5 发版（0e07667 + tag v1.0.0 + Release 375469672）
- **artifactName 模板固定为 cof-film-recommend-setup-${version}.exe——从此 latest.yml url 与资产名天生一致，改名之舞终结**
- 构建链：vite build（bundle index-DF5gt6so）→ PyInstaller（--distpath dist-backend，**直接跑 spec 会输出到默认 dist/ 的坑**）→ electron-builder（两次跑完，第一次 300s 超时属正常，续跑即可）
- 自证清单：打包版后端真机启动——health 新 bundle、no-cache 头在位、assistant enabled、**内置 xtb 真跑 DFT 任务 done（苯·甲醛 gfnff −0.17）**；asar version=1.0.0；xtb 在包
- Release 三资产命令行直传成功（今天 Watt 通道 6.7MB/s）；latest.yml 最后传、下载链路验证通过
- **自动更新端到端第二次验收待用户做：v0.3.0 → v1.0.0**

### 今日测试数轨迹
420 → 441（bug 修复）→ 471（P2）→ 510（P3）→ 530（P4）→ 558（V2.0）→ 578（V2.1）
