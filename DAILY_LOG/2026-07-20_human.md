# 工作日报 2026-07-20（人版）

## 今日工作摘要

完成 **阶段 11：双留出评估 + tree_v4 建模 + 前端打分理由 + 静默启动器**，以及 **阶段 12：表征路线检验（指纹/校准证伪、交互特征排除疑点）+ 双模型路由上线**。模型评估体系更诚实了，前端能用大白话解释"为什么给这个组合打这个分"，App 启动双击即用，现在还会按"单体是否见过"自动选择最合适的模型并告知原因。

---

## 完成事项

| 序号 | 成果 | 说明 |
|---|---|---|
| 1 | 新模型 tree_v4 | 引入"单体历史成膜率"统计先验 + 频率降权 + 正则化，已知单体场景 PR-AUC **0.761 → 0.878** |
| 2 | 更诚实的评估协议 | 新增"醛胺双留出"：两个单体都没见过时的真实泛化能力，今后与旧协议同时报告 |
| 3 | 默认模型保持 tree_v3 | tree_v4 在"双未见"场景未稳定优于 v3，按既定门槛不切换，v4 保留为可选模型 |
| 4 | 前端「打分理由」 | App 新板块：SHAP 归因解释哪个特征推高/拉低成膜分，全中文，热态仅 0.03-0.05 秒 |
| 5 | 静默启动器 | 双击 `start_app.vbs` 即可：无黑窗口、自动开浏览器、失败弹窗提示、带日志 |
| 6 | base 环境补齐 shap | 打分理由在 App 运行环境实际可用，冒烟测试通过 |
| 7 | 文档同步 | PROJECT_STATE / SESSION_START / 实验记录 exp_005 / 决策 D21 全部更新 |
| 8 | GitHub 同步 | 2 个提交已推送到远端 `master`（`6b45f62` + `740dc4e`），本地与远端一致 |
| 9 | 结果展示化学结构图 | App 预测后并排展示醛/胺单体 2D 结构图 + 亚胺缩合产物骨架图；Word 报告同步内嵌结构图；非法 SMILES 优雅降级 |
| 10 | 分子指纹路线证伪 | 通用指纹（Morgan/MACCS）入模不能突破"双未见"瓶颈，反而帮模型"背单体"——此方向关闭，不再投入 |
| 11 | 交互特征排除疑点 | 确认醛-胺交互特征携带真实化学信号而非"背配对"，予以保留；双留出差的 fold3 查明为"难折"（大芳香低成膜单体），非数据问题 |
| 12 | 概率校准证伪 | 两种标准校准方法都让打分的平均误差变得更差——校准路线关闭，模型打分维持原样 |
| 13 | **双模型路由上线** | App 现在按"单体是否见过"自动选模型：**老组合用带历史先验的 tree_v4，含新单体自动切换外推模式 tree_v4_noTE**，前端明确标注用了哪个模型、为什么 |
| 14 | **路由规则优化（routed_strict）** | 复盘后放宽规则：**只有"两个单体都全新"才用外推模式，"一新一熟"改用带先验的 tree_v4**——这类查询打分质量提升约 0.024-0.031，且所有测试场景无一下降 |
| 15 | **逐折评估报告成标配** | 外推评估报告现在固定附带每一折的明细与波动（最难折自动标出），避免"平均分及格"掩盖个别折崩盘——已在历史报告上免训练补齐 |

---

## 关键成果解读

### tree_v4：进步与诚实并存

- **好消息**：把"这个单体历史上多容易成膜"作为特征后，模型在常规场景（单体池内推荐，即 App 主场景）的打分质量大幅提升，**首次超过"纯看历史频率"的基线**（0.878 vs 0.864）；过拟合间隙也减半。
- **诚实的另一面**：用更严格的"两个单体都全新"考核时，tree_v4（3 个分组种子平均 0.642）并没有赢过 tree_v3（0.653）——统计先验在全新单体上没有信息，反而挤占了模型学化学的容量。
- **结论**：按事先定好的切换门槛，**App 继续默认 tree_v3**；tree_v4 存档备用。这个"不切换"本身就是新评估协议的价值——旧协议会高估泛化 0.13 分。

### 前端「打分理由」长什么样

以经典组合 Tp+Pa 为例（树模型打分 0.999）：

- 各组贡献：醛-胺交互匹配 29.5% > 醛单体结构 25.8% > 醛 3D 14.5% > …
- 主要推高因素：醛的芳香原子占比、醛×胺极性表面积交互、醛的 3D 伸展程度等
- 主导方：醛侧单体（关键官能团：醛基）

### 双模型路由：App 现在会"看情况选模型"（阶段 12 收尾 + 收官放宽）

- **怎么选**：输入的醛和胺**都**没训练过（双新）→ 自动切换 tree_v4_noTE（外推模式，不靠历史先验、靠化学泛化）；其余情况（老组合、一新一熟）→ 用带历史先验的 tree_v4。前端会明确写出用了哪个模型、为什么。
- **为什么这样更好**：验证显示路由策略在所有测试场景下都不差于原来的单一 tree_v3（池内 +0.007、新醛场景 +0.031、"双新"场景 +0.056），打分误差也更小——两个模型各管自己擅长的场景。
- **收官放宽（D23）**：原规则把"一个新单体 + 一个老单体"也判给外推模式，复盘发现这类查询用 tree_v4 明显更好（+0.024~0.031），且放宽后所有已测场景无一下降——已于今日切换。唯一的观察项是这类查询的平均打分误差有 0.0075 的轻微上升，已记录在案持续观察。

### App 启动方式（新）

**推荐：双击 `start_app.vbs`** —— 没有黑色终端窗口，浏览器自动打开 `http://127.0.0.1:7860`；启动失败会弹窗提示，日志存在 `logs/` 目录。

---

## 关键决策

**D21：双留出评估协议 + 统计先验入模（tree_v4）；默认模型保持 tree_v3**
- 今后"外推能力"以双留出协议为准，且必须报多个分组种子（单种子噪声 ±0.03）。

**D22：双模型路由上线；指纹/校准路线关闭**
- 池内 tree_v4 + 外推 tree_v4_noTE，路由规则与验证依据见上；池内模型保留交互特征版（v4_int），交互解释价值 + 噪声内的差异不足以切换。
- 何时复盘：积累真实使用记录后，评审"一新一熟"查询是否应改用 tree_v4（routed_strict 候选）。**→ 已复盘（当日收官，见 D23）**

**D23：路由规则放宽为 routed_strict；逐折报告标配化**
- 仅"双新"查询走外推模式，"一新一熟"改用 tree_v4——所有测试场景无一下降，混合查询 +0.024~0.031。
- 外推评估报告固定附带逐折明细与波动（最难折自动标出）；实测 fold 间波动 0.21，最难折 fold3 仅 0.334，证实"平均分会掩盖难折"。

---

## 下一步建议

1. **明日第一优先：GNN embedding 迁移可行性评估**——攻真正的瓶颈：两个单体都全新时所有模型只有 0.63-0.68，指纹路线已证伪，下一步看 GNN 学到的表征能否迁移进树模型。
2. **fold3 型难折专项切片**：对"区域漂移大单体"单独计分（逐折标配已就位，可直接挂在最难折上）。
3. **持续观察**：App 实际查询中"双新"占比、外推模式的打分误差优势是否兑现；"一新一熟"打分误差轻微上升（0.0075）是否需要处理。

---

## 阻塞点

无。

---

*本日报由 AI 自动生成。*
