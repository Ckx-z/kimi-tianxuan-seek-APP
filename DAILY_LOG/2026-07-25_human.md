# 日报 2026-07-25（人版）

## 一句话总结

你提的两个问题（CAS 解析失败、设置页加检查更新）都已修复并发布了 **v0.2.5 修正版**——中途还发现并纠正了一次"打错包"事故，以后每版发布前都会过自证清单，不会再发生。

## 今日成果

1. **CAS 解析修好了（两处病根都治了）**
   - 病根一：内置库只有 17 条、PubChem 国内连不上 → 新增 LLM 兜底解析，而且有**防呆双校验**：让 AI 用两种方法各查一次，结构一致才采纳，答错就安全拒绝（宁可查不出也不给错结构）。
   - 病根二（你实装后才发现）：前端原来一直是浏览器直连 PubChem，根本没走我们后端 → 新加了后端 CAS 接口，前端已改走它。实测 `100-52-7` 秒出苯甲醛。
2. **设置页有了「软件更新」卡**：显示当前版本，点"检查更新"→ 发现新版 → 一键下载（带进度条）→ 重启安装。
3. **一次事故的坦白**：第一次发的 v0.2.5 其实装的是旧文件（构建环节没重新打包），所以你装了看不到新功能。已重新构建——这次做了三项自证（新文件时间戳、包内含新功能、运行时 CAS 真实命中），GitHub 上的安装包也已全部换新。**请重新覆盖安装一次 `webapp\release\` 里的 Setup 0.2.5**（数据不受影响）。
4. **自动更新的真相**：你机器上的 v0.2.0 缺一个更新配置文件（当时的构建机制问题），所以它检测不到 v0.2.5——这次手动装；**v0.2.5 已带齐配置，从下下个版本开始就能真正自动更新了**。

## 验证

- 自动化测试 382 项全过（新增 9 项）；打包版真实冒烟：CAS 解析、预测出分、结构图、更新配置全部在位。

## 明天两件事

1. 重装 v0.2.5 后验证两点：设置页出现「软件更新」卡；CAS 输 `100-52-7` 解析出苯甲醛。
2. 都正常的话，就可以把 zip 发课题组群了（让同学开着 Watt Toolkit 下载更稳）。

## 阻塞点

无硬阻塞。小事：GitHub 的资产下载 CDN 今天在国内网络不太稳，同学若下载慢可开加速器；你的 PAT 用完后记得删。
