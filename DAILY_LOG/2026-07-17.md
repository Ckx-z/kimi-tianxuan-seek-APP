# 日报 2026-07-17（AI 版）

## 今日工作：阶段 10 — tree_v3 归因与 App 接入

### 已完成

1. **生成 tree_v3 SHAP 归因报告 `reports/attribution_v3.md`**
   - 改造 `src/models/attribution.py`：
     - `classify_feature` 新增 3D 分组（`aldehyde_3d` / `amine_3d` / `dimer_3d`，先于通用 `ald_`/`amine_` 前缀判断）
     - `explain_single` / `summarize_pair_interaction` 增加 `use_3d` / `use_dimer` / `n_confs` 参数（否则 3D 特征缺省为 0，归因失真）
     - `group_contributions` 分组字典补齐 3D 键（防 KeyError）
   - 泛化 `scripts/generate_attribution_report.py`：
     - 增加 `--model` / `--data` / `--out` 参数，默认 `models/tree_v3.pkl`
     - 特征开关自动从模型 pkl 内的 `metrics` 读取，缺失回退 v2 配置
     - 新增「3D 特征贡献」小节（3D 占比 + Top-10 3D 特征及全局排名）
   - 全量 3094 行特征化 + SHAP 约 8 分钟（3D 全部命中缓存）

2. **App/predictor 默认模型切换 tree_v3（D20）**
   - `src/predictor/__init__.py`：`FilmPredictor` 默认树模型 `tree_baseline.pkl` → `models/tree_v3.pkl`；预测结果新增 `tree_model_name`
   - `src/predictor/tree_model.py`：
     - `load()` 从 pkl 的 `metrics` 恢复特征开关（`use_rules`/`reduced_rules`/`use_interaction`/`use_3d`/`use_dimer`/`n_confs`），旧 pkl 无记录则回退默认
     - `predict()` 用 `reindex(columns=feature_cols)` 对齐特征列，3D 失败样本补 0 不报错
     - 预测输出按概率语义 `clip(0, 1)`（修复回归略超 1 的历史遗留问题，如 Tp+Pa 预测 1.0032）
   - `app/gradio_app.py`：概率输出显示 `树模型 (tree_v3)`
   - `TreeFilmPredictor` 默认路径保持 `tree_baseline.pkl` 不变，避免测试快速训练覆写主模型

3. **验证**
   - tree_v3 预测 Tp+Pa = 0.9987，142 特征，开关恢复正确
   - 旧模型向后兼容：tree_baseline（无 metrics）/ tree_v2（无 3D 开关）均正常预测
   - App 回调端到端通过（base 环境 `E:\ANACONDA\python.exe`，含条件推荐）
   - `pytest tests/`：**9 passed**（base 环境；`.venv` 未装 pytest）
   - 新旧代码路径对同一模型预测值差异为 0（确认测试失败为历史遗留，非本次改动引入）

### 归因报告关键结论

- **3D 特征累计占全局 SHAP 的 29.8%**（醛 3D 18.1% + 胺 3D 11.8%），与 interaction（27.3%）、aldehyde（23.0%）相当
- Top 3D 特征：`ald_3d_radius_ratio`（全局第 3）、`ald_3d_pmi_i2_i3`（第 4）、`ald_3d_mol_volume`（第 7）
- 全局前二仍为 `ald_n_aromatic_rings_per_site`、`int_hadamard_tpsa_per_site`
- 单样本归因合理：Tp+Pa 0.999（醛主导），含 F 胺被 `rule_F_on_amine` 显著惩罚（SHAP -0.0607）

### 关键决策

**D20：App/predictor 默认模型切换为 tree_v3，特征开关从 pkl 自动恢复**
- 原因：tree_v3 有 142 维特征（含单体 3D），预测时必须按训练配置生成特征；开关存进 pkl 后模型自描述，切换模型不改代码
- 何时复盘：后续切换主模型（如 tree_v4）只需改 `DEFAULT_TREE_MODEL`

### 明日待办

- [ ] （可选）消融实验：分别关闭 3D、二聚体、交互特征，分析各自贡献
- [ ] 异常诊断：条件特征实验 D PR-AUC 0.9112 是否标签泄漏
- [ ] SMILES 清洗诊断：解析失败样本（聚合物占位、未闭合环等）是否需清洗
- [ ] （可选）LightGBM 与 XGBoost 对比

### 阻塞点

无。
