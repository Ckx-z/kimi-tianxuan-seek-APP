# 日报 2026-07-21（AI 版）

## 今日工作：阶段 13 阶段 A — GNN embedding 迁移可行性评估（exp_009，D24）

### 背景

双未见单体泛化 0.63-0.68 是项目第一瓶颈（指纹 exp_006 已证伪）。今日任务 = GNN embedding 迁移阶段 A（便宜证伪优先）：全量 GNN v5.3 embedding 直接入 XGBoost，无提升即关闭路线，有提升立项阶段 B（逐折重训）。

### 已完成

1. **提取脚本 `scripts/stage13_extract_gnn_emb.py`（新增，dphuanjing 环境运行）**
   - `sys.path.insert` 旧项目根（只读，D07），按 predict_pair.py 方式重建 v5.3（`ckpt['config']` + `use_3d/use_rules/dim_rules=RULE_DIM` 强制写入 + `load_state_dict` + eval，**无 MC dropout** 保证确定性）
   - 行过滤复刻 `stage11_dual_holdout.load_df_y()`（去 hard_rule* + dropna SMILES/标签 + reset_index）→ 3094 行
   - 自检：Tp+Pa 确定性概率 0.6474 ≈ 历史 MC 均值 0.665（容差 0.08 内）✓
   - 每行提取两套 embedding（`Batch.from_data_list`，batch=64，CUDA）：
     - **pair_emb 512 维**：`model._get_features(ba, bb, ald_batch, amine_batch, batch_size)` → `[ea‖eb‖ea⊙eb‖e_pair]`（交叉注意力后，配对相关，即 FilmHead 的 GNN 部分输入）
     - **mono_emb 256 维**：`model.encoder.encoder(batch)` 原子级输出（交叉注意力前）`global_mean_pool`，醛 128 + 胺 128
   - 147/3094（4.8%）脏 SMILES 图构建失败补零向量（与树管线 3D 失败补 0 一致；首次运行因 1% 阈值过严失败，放宽至 10% 防灾难性故障 + `RDLogger.DisableLog` 静默）
   - 落盘（data/interim/，不入 git）：`gnn_emb_v53_pair.npy`（3094×512 float32）、`gnn_emb_v53_mono.npy`（3094×256）、`gnn_emb_v53_index.json`（行号+SMILES+is_film 对齐断言用）
   - 全程 ~10s 前向（GPU），总耗时约 1 分钟（含 featurize）

2. **评估脚本 `scripts/stage13_gnn_embedding_eval.py`（新增，.venv 运行）**
   - import stage11 复用 `load_xy / make_folds / run_cv / V4_CANDIDATES`；index.json 对缓存 df 逐行 SMILES 断言对齐 ✓
   - 4 变体（统一 v4_mild 参数 + 频率降权，无 TE）：emb_base(142) / emb_pair_only(512) / emb_pair_plus(654) / emb_mono_plus(398)
   - 协议：LOGO（GroupKFold-10）+ 双留出 5 折 × 3 种子（42/123/7），per_fold + fold_summary 标配
   - gain 分组诊断（emb_pair_plus 全量训练，importance_type=gain）：embedding 维占 **81.1%**、描述符 18.9%
   - 报告 `reports/gnn_embedding_eval.json`；总耗时 9.3 分钟

3. **结果与判定（事先约定门槛：3 种子均值提升 ≥ +0.03 且逐种子一致）**

| variant | LOGO | 双留出 s42 | s123 | s7 | 3 种子均值 | Δ | 判定 |
|---|---|---|---|---|---|---|---|
| emb_base | 0.7900 | 0.6694 | 0.7075 | 0.6703 | 0.6824 | — | 基线逐位吻合 ✓ |
| emb_pair_only | **0.9022** | 0.8188 | 0.8321 | 0.8784 | 0.8431 | +0.1607 | **PASS** |
| emb_pair_plus | 0.8953 | 0.8376 | 0.8242 | 0.8709 | **0.8442** | +0.1618 | **PASS** |
| emb_mono_plus | 0.7762 | 0.6539 | 0.6715 | 0.6903 | 0.6719 | −0.0105 | 证伪 |

   - 基线复跑与历史 v4_mild_noTE 三个种子值逐一相同（0.6694/0.7075/0.6703）——协议对齐无误
   - fold3 难折（s42，n=203）：0.3640 → 0.6472（pair_only）/ 0.6823（pair_plus）；fold 级 std s42 0.198→0.104
   - LOGO pair_only 0.9022 已超 tree_v4 的 0.8784（且无 TE）
   - **泄漏警告**：阶段 A 是乐观上界（GNN 训练集 6392 行与本数据单体几乎完全重叠），+0.16 与配对标签记忆形态量级不可区分 → 阶段 B 逐折重训是唯一鉴别手段

4. **文档收尾**
   - `EXPERIMENTS/exp_009.md`（设计/泄漏警告/结果/判定/阶段 B 预案）
   - `DECISIONS.md` D24（索引补 D23 行 + 追加 D24；修复一次误删附录标题的编辑事故）
   - `PROJECT_STATE.md`（阶段 13 标记进行中 + 阶段 A 小节 + 下一步/待决策刷新）
   - `SESSION_START.md` 速览更新至 2026-07-21

### 验证

- 提取自检 Tp+Pa 0.6474 ≈ 0.665 ✓；embedding 无 NaN；对齐断言通过
- 基线变体复跑逐位吻合历史 0.6824 ✓
- `E:\ANACONDA\python.exe -m pytest tests/ -q`：**57 passed**（85.7s），无新增失败

### 明日待办 / 遗留

- [ ] **第一优先：阶段 B**——调研旧项目 GNN 训练管线（`scripts/train_v4.py` / v5 训练脚本），实现逐折重训 + embedding 提取 + 树评估闭环；先 1 种子 5 折摸底训练成本，再 3 种子全量（15 次训练）
- [ ] 阶段 B 确认后：pair_emb 接入路由两臂重测（D23 遗留 v4/noTE 两协议冲突同口径复测）；App 预测时 embedding 提取（dphuanjing subprocess 已有先例）
- [ ] fold3 型区域漂移切片（exp_009 显示 pair_emb 使 fold3 0.364→0.65+，阶段 B 确认后重点复测）
- [ ] pair_only vs pair_plus 打平（0.8431 vs 0.8442）：确认后可评纯 embedding 替代描述符
- [ ] 更早候选仍有效：实验 D 泄漏诊断（0.9112）、SMILES 清洗、LightGBM 对比、仅二聚体 3D 消融

### 阻塞点

无。

---

## 追加：阶段 13 阶段 B —— 逐折重训 GNN（闭卷）判定（exp_010，D25）

### 已完成

1. **旧项目训练管线调研**（只读）：`scripts/train_v4.py` 为 v4/v5 全系训练脚本；`PairDataset(csv_path, use_3d, use_rules, freq_weights)` + `collate_fn` + `V4Trainer` + `FocalLoss` 全部可 import 复用；两个静默失效确认——频率降权与 fold 分层均因 `"1"` vs `"1.0"` 字符串 bug 从未生效（freq_weights=None 即等效复刻）；v5.3 训练时 3D 即全零（训练 CSV 无 3D 列，scaler 恒等）；连续标签直接进 soft-label FocalLoss，验证指标 ≥0.5 二值化；实测 2.5–6.8 min/折（早停 19–66 epoch，GPU）。

2. **阶段 B 脚本**：
   - `scripts/stage13b_fold_retrain.py`（dphuanjing）：逐折从零重训 GNN（**绝不加载 v5.3 权重**）→ 闭卷 pair_emb `gnn_emb_foldb_s{seed}_f{k}.npy` + 折内 GNN 直接外推概率 + meta；断点续跑（npy 存在即跳过）
   - `scripts/stage13b_fold_eval.py`（.venv）：闭卷 embedding 同口径树评估（v4_mild + 频率降权），base/pair_only/pair_plus 同折同参 + gnn_direct 诊断 → `reports/gnn_embedding_foldb_eval.json`
   - **严重 bug 修复**：`dual_holdout_folds` 折分组依赖 pandas `value_counts` 顺序，dphuanjing（py3.8）与 .venv（py3.12）折分配漂移（fold0 验证集 104 vs 122）——折定义单一真源改为 .venv 侧 stage11 原版函数落盘 `data/interim/stage13b_folds.json`（与阶段 A 评估日志逐位一致），跨环境脚本只读该文件

3. **结果（闭卷双留出 PR-AUC，3 种子，15 折 GNN ~80 min）**：

| variant | s42 | s123 | s7 | 均值 | Δ |
|---|---|---|---|---|---|
| base | 0.6694 | 0.7075 | 0.6703 | 0.6824 | — |
| pair_only | 0.6381 | 0.6443 | 0.6698 | 0.6507 | −0.032 |
| pair_plus | 0.6306 | 0.6467 | 0.6668 | 0.6480 | −0.034 |
| gnn_direct | 0.6661 | 0.5850 | 0.6440 | 0.6317 | −0.051 |

   - 逐种子 delta（pair_plus）：−0.039 / −0.061 / −0.004，**3 种子全负** → **FALSIFIED**（事先约定对称门槛）
   - **阶段 A 的 +0.1618 判定为配对标签记忆**，路线关闭
   - 闭卷对照：GNN 端到端 0.6317 < 描述符树 0.6824——双未见场景树模型胜
   - fold3 闭卷全线 ~0.36（s42：树 0.364/GNN 0.360/emb 0.37）——区域漂移折对一切"从已见分子学习"的方法是死穴
   - 基线复跑逐位吻合历史 ✓

4. **文档**：`EXPERIMENTS/exp_010.md`、DECISIONS D25（索引 + 正文）、PROJECT_STATE（阶段 13 收官 + 阶段 B 小节 + 下一步刷新）、SESSION_START 速览、README 问题清单

### 验证

- 折索引 JSON 与阶段 A 评估日志逐位一致（s42: 2062/122, 2133/85, 2083/98, 1689/203, 1950/127 ✓）
- 闭卷基线与历史 v4_mild_noTE 三个种子值逐位相同（0.6694/0.7075/0.6703）✓
- 本次仅新增离线脚本（src/App 未动）；今日早前 pytest 57 passed 仍有效

### 明日待办 / 遗留（刷新）

- [ ] **第一优先：fold3 型区域漂移切片与检测**（任务 2；raw-feature 包络外样本识别 + 单独计分，挂在 fold_summary 最难折上）
- [ ] 可选立项：外部数据预训练表征（MolFormer/ChemBERTa 类，与本数据无重叠天然闭卷；阶段 B 管线直接复用，同源 GNN 对照组已就位）
- [ ] 遗留观察：双未见 v4/noTE 两协议冲突（噪声量级）；A 混合桶 MAE −0.0075（D23）；实验 D 条件泄漏 0.9112 诊断
- [ ] 工程增强：案例库提取、GNN 免 subprocess、LightGBM 对比、仅二聚体 3D 消融

### 阻塞点

无。

---

## 追加（晚间）：阶段 14 真实回测（exp_011 / D26）

### 完成事项

1. **外部文件入区**：`实验方案_含氟COF薄膜合成_v3.9_20260626.docx` + `实验ABCDEF.docx` 复制到 `data/experimental_refs/`（只动副本；ABCDEF 9.6MB 加入 .gitignore 不入 git）
2. **单体解析**（`scripts/stage14_resolve_monomers.py`）：17/21 成功——化学名构建 SMILES + RDKit MW 校验（|Δ|≤0.5）+ 旧池 canonical 互证（9 个完全匹配）；**H1–H4 待用户确认**（方案结构与记录 CAS 的 MW 冲突：H2 590.27 vs 662.3、H3 690.28 vs 918.37）
3. **路由回测**（`scripts/stage14_real_backtest.py`）：生产路由与 canonical 判定 21 组合 100% 一致；已做实验全走 tree_v4 臂；每组出树概率 + 双臂对照 + SHAP Top3
4. **三方对照分析**（`scripts/stage14_analyze_backtest.py`）：
   - 6 组配对（A/A2/B/D/E/F，真实全失败）：旧 GNN 平均 0.687 → 新模型 0.254；Brier 0.489 → 0.112（4.4 倍）
   - 旧 GNN 5/7 组 ≥0.59 虚高全灭；新模型 13 组仅 2 组 ≥0.5（A1/A8），且与唯一相对成功组 A5 同属 TAPT+A6（**该对并列第 1/13**）
   - base rate 对冲：方案 13 组合 0.031–0.699（8/13 ≥0.5），低分由 TE 文献先验驱动（B5 −0.573、A7 −0.590），非只会打低分
5. **文档**：`reports/real_backtest.md/.json` + 两个中间 JSON、`EXPERIMENTS/exp_011.md`、DECISIONS D26、PROJECT_STATE 阶段 14

### 验证

- 单体 MW 校验 17/17 通过；旧池互证 9 个 canonical 完全匹配
- 生产 exact-match 路由与 canonical 化学判定 21 组合零分歧
- pytest 57 passed（见 git 提交前验证）

### 遗留

- [ ] 等用户确认 H1–H4 结构 → 补 C 与 G5 四组预测
- [ ] OOD/不确定性正式输出（对齐建议第 3 条，C 组为现实需求案例）
- [ ] 后续实验出正例后双向验证（G2/G4 高分组）


---

## 追加（深夜）：P2 三件套——打分 + 不确定性 + OOD（exp_012 / D27）

### 完成事项

1. **bagging 集成**（`scripts/stage15_train_ensemble.py`）：v4_mild 配置 5 种子 [42,123,7,2026,555]，池内臂 `models/tree_v4_ens.pkl`（含 TE）+ 外推臂 `tree_v4_noTE_ens.pkl`（自描述 ensemble 格式）；`TreeFilmPredictor` 扩展 `predict_with_std`，单模型 pkl 向后兼容（std=0）；路由默认切换集成版。冷启动加载 3.4s，预测 ~0.09s
2. **集成 CV 验证**（`scripts/stage15_ensemble_cv.py` → `reports/stage15_ensemble_cv.json`）：LOGO 两臂 ensemble ≥ 单模型（0.8784→0.8853 / 0.7900→0.7911）；双留出 3 个划分种子复核，8 格 7 格 ≥成员均值、与 s42 差异 ≤0.011；唯一例外 v4/双留出 s42 −0.047，逐成员诊断确认为 seed42 单模型偏运气单点（该划分成员均值 0.609）——按"不低于单模型平均水平"口径通过，如实披露
3. **OOD 三级检测**（`src/predictor/ood.py`）：(a) 官能团适配性（酰肼/肼/羟胺/无胺基/无醛基 → out）；(b) 双未见新颖性 → warning；(c) 特征区域漂移（16 项关键特征超训练 5%–95% 包络 >10% → warning，包络存 `models/feature_envelope.json` 入库）
4. **App 与报告改造**：「成膜概率」→「成膜打分（倾向性）」+ 语义说明（论文口径）；± std 展示；warning ⚠️ 黄条 / out ⛔ 红条不出分数（GNN 同挂）且不出打分理由；Word 报告加 ± std 与 OOD 说明段
5. **验证**：TFPT+H3 → out（酰肼）、TAPT+A2/Tp+Pa → none、虚构双未见大芳香 → warning 三情形全对；新增 `tests/test_ood_ensemble.py`（20 个）+ 更新 `tests/test_routing.py`；**pytest 77 passed**（57+20，无新失败）
6. **文档**：`EXPERIMENTS/exp_012.md`、DECISIONS D27、PROJECT_STATE 阶段 15

### 遗留

- [ ] SHAP 归因基于集成成员[0] 近似（测试容差 0.05），如需严格可 5 倍成本多成员平均
- [ ] bagging std 校准性未验证（std 大是否错得多）——待 App 日志积累
- [ ] 等用户确认 H1–H4 结构 → C/G5 自动走 OOD out 闭环

---

## 追加：阶段 16 — App 重设计 P1 + minimax RAG 契约对接（17:40）

### 完成（三并行子任务，联调通过）

1. **前端改版**（`app/gradio_app.py` 全量重写 284→870 行）：五标签页骨架（①查询打分 ②批量排序 ③④⑤占位）；gr.themes.Soft 深青/石墨主题（注意：gradio 6.20 主题需设 `_deprecated_theme/_deprecated_css` 内部字段，升级时复查）；色彩语义 ⛔/⚠️/✓；打分大字号
2. **页①**：三种输入（SMILES / CAS→PubChem+缓存 / 内置库 17 单体下拉）；相似成膜案例（Morgan Tanimoto top3，含 paper_id）；保留打分±std/路由/OOD/SHAP/结构图/Word 全部能力
3. **页②**：批量排序（库多选/粘贴/CSV ≤20 对 → Dataframe 排序表 → 导出 CSV 到 reports/exports/）
4. **后端新模块**：`src/utils/cas_lookup.py`（resolve_cas 三路解析）、`src/utils/predict_log.py`（jsonl 日志 schema_version=1）、`src/recommend/similar_cases.py`、`data/builtin_monomers.json`
5. **RAG 对接**：`data/rag_export/`（README 契约 + predictions/records/suggestions 三 schema example）；minimax 侧新增 `docs/COF_APP_CONTRACT.md` + `adapters/cof_app_ingest.py`（dry-run 校验 + --apply 转 feedback_db 表头 CSV，绝不直写 feedback_db.csv；**minimax 改动未 commit 待用户审**）
   - minimax 探查：predict/（GNN 最小运行集）+ experiment/（feedback_db.csv 14 条 + 失败判据 A–G）+ bridge/（5 路召回 RAG + docx 生成器）；CAS 中心 vs 我们 SMILES 中心 → 契约单体对象双字段并存；score 语义隔离（倾向性打分进备注列，不写 tianxuan_预测概率 列防误用）

### 验证

- pytest **113 passed**（77 + 新增 36）；端到端冒烟（不起服务）：CAS 14544-47-9→TAPT（builtin 离线）、A1×B1 单组 0.666±0.029 走 noTE 臂+⚠️、批量排序/导出/日志落盘全 ✓
- .gitignore 增补：prediction_log.jsonl、cas_cache.json、reports/exports/

### 遗留

- [ ] 页③收藏夹（含文献自动匹配）/页④实验记录/方案卡防错清单 → P2 期
- [ ] 页⑤ RAG 建议回显 → 待 minimax 主迭代；suggestions/ 写入逻辑属 minimax 侧开发
- [ ] minimax 改动（adapters/、docs/ 两新目录）待用户审阅后 commit
- [ ] 批量 20 对 SHAP 一句话耗时可加 gr.Progress；相似案例非内置单体可改缩略结构图
