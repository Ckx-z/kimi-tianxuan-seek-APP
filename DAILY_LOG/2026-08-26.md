# 日报 2026-08-26（AI 版）

## 今日工作：阶段 29 — DFT pair 模式 + 3D 构象 + 文献体系 + 助手 V2.2 + 缓存根治 + v1.1.0 发布

### 已完成

**1. v1.0.2 "还是旧版"排查（结论：安装无问题，做根治包）**
- 实证：E:\COFapp 安装 ProductVersion=1.0.2.0、包内 bundle 哈希=修复版、注册表单一安装、唯一快捷方式指向正确、.app-version 标记=1.0.2（自清缓存已执行）
- 结论：安装/文件/缓存均正常；最可能残留场景是**更新安装时旧进程未被杀 → 单实例锁把新实例弹回旧窗口** → 做根治包（见 5）

**2. DFT 任意双分子模式 + 3D 构象（agent-113，668 passed）**
- pair 模式：任意两分子 A···B 直接算结合能（跳过二聚体生成器）；缓存 key 含 mode 与 dimer 模式隔离；pair 禁收藏（无单体组归属）
- 3D 结合构象：3Dmol.js（npm 3dmol@2.5.5）懒加载折叠区块，fragment_ranges {a:[0,n], b:[n,total]} 片段着色（主体 cyan/客体 magenta）+ 半透明 VDW 表面开关 + xyz 下载
- 真实 xTB 冒烟：苯+苯酚 gfnff E_bind=-0.236 kcal/mol，片段区间正确

**3. 文献体系（agent-114/115 + 我，704 passed）**
- resolver：paper_id → 真实标题/DOI/https://doi.org 链接；收藏 references 响应 enrichment（内存视图）；auto_match 新写入直接带解析结果；GraphRAG/助手引用同步；domain_rules 加引用纪律
- Crossref 录入审核流：POST /api/literature/lookup（doi/title→待审草稿）+ confirm（可改后入库，409 防重复，in_training:false 不影响打分；审计流水 literature_intake.jsonl）
- DOI 回填：40 条空 DOI → Crossref 标题检索（相似度>0.85）回填 27 条（6 条 .s00x 补充材料 DOI 已更正为主文），13 条待人工；**最终空缺率 0.8%**（目标 <10% 大幅超标达成）
- frozen 可写化：文献库迁 user_data_root/literature/ overlay（copy-on-first-write）；我修正为**逐条合并读**（内置库打底+用户库覆盖），未来包内库升级老用户不丢新条目
- 过程事故：agent-114 首轮测试 4 条草稿误入真实库，已清除核对恢复 1711 条，fixture 加固

**4. 科研助手 V2.2（agent-116，717 passed）**
- GET /api/assistant/daily-brief：当日实验记录/DFT/收藏/新文献聚合 + ming 点评（LLM 未配置只回结构化）；冒烟实测点评正常生成
- GET /api/assistant/nudges：同 favorite 连续失败 ≥2 次主动提醒（outcome 集合 + 中文失败关键词兜底；正式记录才计）；dismiss 按自然日存 user_data_root
- get_daily_brief 工具注册；前端日报卡 + 提醒条（点击预填对话不自动发）

**5. 缓存根治包（agent-117，718 passed）**
- NSIS customInit：安装前静默 taskkill COF科研助手.exe/cof-backend.exe/COF成膜推荐系统.exe（含历史名兼容）
- 版本看门狗：拿锁失败时读 .running-instance 标记，版本不同→taskkill 旧实例进程树→1s 后重拿锁继续启动（不再聚焦旧窗口）；正常退出删标记
- /api/health 加 version；Electron 握手比对，不一致全页红横幅
- 设置页版本三显：界面（IPC）/后端（health.version）/前端构建哈希（vite define __BUILD_HASH__=git short hash）

**6. v1.1.0 发布（cf0c09b + tag）**
- 全流程：三处版本号 → vite → PyInstaller → 打包版自证（health version=1.1.0 ✓、新 bundle ✓、画板 chunk 在包 ✓、nudges ✓、pair 模式真实算通 ✓）→ electron-builder → zip 441MB → Release 三资产（exe 442MB/zip 441MB/latest.yml）
- tsc -b 清理：raphael.d.ts 声明 + FavoritesSection 未用变量删除

### 遗留

- 13 条文献 DOI 待人工（#105/#1441/#1450/#1452/#1454/#1456/#1457/#1463/#1467/#1469/#889/#892/#899，可 Crossref 高级检索再压一轮）
- 记录"当日更新"按文件 mtime 判定（无 updated_at 字段）
- 日报卡/提醒条只在 LLM 启用时渲染（后端结构化数据不要求 LLM）
- fchk 答复：xTB 半经验不出 chk/fchk；可出 molden（--molden），fchk 需走 ORCA/Gaussian 复算（已有导出输入文件功能）；sobereva Multiwfn 可做 molden→fchk 转换参考
- 用户验收清单：v1.0.2→v1.1.0 升级后版本三显一致、pair 模式、3D 构象、文献录入全流程、日报卡/提醒条、DFT 追加到收藏
